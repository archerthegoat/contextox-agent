--- a/开发路径图.md
+++ b/开发路径图.md
@@ -61,12 +61,12 @@
 3. 通过拖拽或文件选择批量导入授权资料，或显式绑定一个只读本地文件夹/Git 仓库根；看到逐文件类型、大小、hash、解析状态、失败原因和来源版本。绑定只授权该规范化根路径，不能扩展到父目录、符号链接目标或其他 Workspace。
 4. V0 至少区分文档、结构化数据和 SQL/DDL；支持格式与限制在实现 checkpoint 前根据公开样例精确冻结。
 5. 对结构化数据展示表、字段、类型、样例、缺失、唯一性和基础异常；对文档展示可引用的提取片段。任何解析失败保持 `partial` 或 `blocked`，不伪造成功。
-6. 创建一个业务定义 Mission；它绑定不可变的来源快照和任务目标。
-7. Workbench 以业务定义任务为主线：左侧导航资料与任务，中间查看资料、关系、字段草案与待决问题，右侧与当前任务的 Agent 连续对话。切换任务内的资料或结果不另建对话；引用对象与资料使用范围明确可见。Agent 默认展示解释、证据、澄清、结果入口和简明进度，详细模型/工具事件收进任务执行历史；隐藏模型私有推理，不伪造“思维链”。
+6. 从右侧连续对话开始，可先讨论目标和资料；用户发出明确工作指令且目标、资料范围足够时，由这次发送建立业务定义 Mission 并开始分析。Mission 绑定原始用户指令与精确来源快照，不再要求用户先操作任务草案确认或独立 Run 表单。
+7. Workbench 以 Agent 对话主导：左侧合并工作区、对话/任务与资料导航；中间顶部展示真实任务进度，下方联动资料、关系、字段草案与待决问题；右侧始终可以输入，支持任务前讨论及待澄清期间解释和回答整理。右栏可拖动或用键盘调宽并保存本机偏好，窄屏默认显示对话；手动查看中间资料时不被新事件强制切换。同一会话延续明确选定的资料版本，每次发送重新校验权限和范围；详细模型/工具事件收进执行历史，不显示模型私有推理。
 8. Agent 只能读取授权来源、检查结构化数据、搜索已批准 Context、创建澄清、更新结构化定义草案和提交审核。
 9. 遇到证据不足或业务选择时，Agent 创建澄清表单，至少说明问题、为什么要问、期望回答结构、建议回答角色、相关证据、示例/选项、阻塞影响和关联定义。
 10. 使用者可以在 Workbench 填写，也可以复制或导出澄清包交给对应人员；V0 不依赖对方安装 ContextOx，也不要求飞书集成。
-11. 回答先成为带来源与回答者声明的候选，不自动成为事实。本地使用者在既定 `local-owner` 信任边界内，针对精确版本和哈希批准、要求修改或拒绝；职位名称不授予业务裁决权，练习中的确认不代表真实企业批准。
+11. 用户可在对话中追问和用自然语言回答；Agent 整理成可修改、带依据与回答来源的候选卡片，缺失信息继续询问。整份回答仍由本地使用者在既定 `local-owner` 信任边界内针对精确版本和哈希批准、要求修改或拒绝；“确认并继续”可串联保存、批准和一次续接，并分别回读每步结果。普通聊天或模型整理不自动成为事实，职位名称不授予业务裁决权，练习中的确认不代表真实企业批准。
 12. 获批回答被编译为新的 ContractVersion 和可复用的 Workspace Context 候选；只有再次批准的 Context 版本可供后续 Mission 使用。
 13. 最终 Contract 至少包含业务目的、实体、粒度、身份、时间语义、来源、字段映射、计算规则、未知、例外、正反例、责任人、验收条件和变更记录。
 14. 只有显式完成转换引用获批 Contract 和所需证据时，Mission 才能进入 `completed`；`partial`、`blocked`、`failed`、`cancelled` 与 `PENDING` 保持区分。
@@ -123,7 +123,7 @@

 - 复核并整合已有 Workspace 实现，保留已认可的资料预览与字段检查体验；调整任务页、Agent 对话与执行历史的分工，避免以运行控制表单代替业务定义工作页。
 - 实现本案例所需的最小来源准入、不可变 SourceRevision、Schema、样例和基础 profiling；首批格式与限额在实施前明确。
-- 按已批准架构实现任务草案、人工确认、正式 Mission、来源快照和显式启动 Run。
+- 沿用旧任务草案/确认记录及接口；新会话按用户明确发送的工作指令与可见资料范围建立 Mission 并衔接分析，免去独立任务表单和 Start 操作。任务前连续对话、讨论回执、任务来源与显式迁移按 Agent 主导 Workbench 增量实施。
 - 实现单 Provider 薄 Agent Loop、必要的领域工具、预算、取消、公开事件、持久状态与终止收据。
 - 持久化当前任务中明确发送的用户消息、受控公开答复及结果引用，使刷新后可回读并支持后续追问；产品对话与单次 Run 的临时模型消息分开，不保存原始 Provider transcript 或隐藏推理。消息/执行的关联、幂等、有限上下文和权限合同在实现前精确评审。
 - 在 Workbench 联动资料、实际关系图、字段草案和任务对话；用户可显式引用字段或关系提问，并从答复定位证据。无草案时显示明确空状态；关系与业务语义分别标明已知、候选、冲突和未知。
@@ -135,6 +135,14 @@
 通过门：在获准输入与调用预算内，真实模型在同一任务中完成至少两轮用户可见交互，并产出可追溯至实际来源的关系说明、字段定义草案，以及存在未决问题时的具体澄清；资料、答复和结果引用可相互定位，刷新不丢已发送对话、不重复启动，跨 Workspace 保持隔离。fake model 测试是回归检查，不能代替该里程碑；仅有输入框、流式文字或工具日志不算任务对话通过。存在未决业务问题时保持待确认或 partial，不声明 Mission 完成。

 补充验收要求：关键事实覆盖、关键主张的证据支持、关键未知识别、候选可用性及具体澄清分别评分；安全违规为零。原始案例与未用于调优的保留案例按冻结版本重复验证。合法 partial、自动测试、构建及浏览器可用不能替代真实语义质量；数字阈值与真实试验材料、总预算按上述验收稿另行冻结。
+
+### 路径 2/3 的 Agent 主导交互增量（2026-09-10）
+
+采用 `docs/proposals/Agent主导Workbench-实施合同.md` 的精确内容（SHA256：`006ff8930e05464d7a1be76f564f9a08bd8eb18ea4769ea9adcafaaff65ee34f`）。其原型与实施顺序为：合成交互原型 → 会话/讨论与显式迁移 → 自动任务建立 → 回答交接 → Workbench 接入及整体验证。该文件保留批准前 PROPOSED 标记作为精确采用快照，生效以本采用条款的人为批准为准。
+
+新旧会话保持 Workspace 隔离及明确来源范围。首次发送可建立默认本地工作区；模型缺少配置时在对话中引导一次连接，保留待发送内容，不能隐瞒外发和费用。读资料、讨论与候选整理在已授权范围内推进；业务裁决、范围变化和未知结果仍保留对应校验。“确认并继续”简化交互，不省略整份回答批准和一次执行的独立记录。
+
+本增量不改变正式 Contract、Mission 完成、跨任务批准知识复用及路径 5 的价值验证要求，不增加 Provider、依赖、队列或云能力。真实模型调用预算与人的验收另行批准和记录；本次工程交付为本地分支与提交，不包含 push、main merge 或发布。

 ### 路径 3：跑通高潜客户定义的完整闭环

--- a/docs/架构与迁移报告.md
+++ b/docs/架构与迁移报告.md
@@ -254,7 +254,7 @@

 - 正式 Run 生命周期：

-1. 无正式 Mission 时，每次用户发起草案生成最多发起一次 Provider 请求，命名为 `MissionDraftAttempt`；它使用一次受限的 DeepSeek `non-streaming` JSON 结构化调用，只接收 P0 与当前输入，不读取 Source、跨 Mission 聊天或提供 tools，也不算正式 `AgentRun`。本地持久化不可变原始用户输入、candidate draft、version/hash/status 和 `ProviderReceipt` ref。该 attempt 不自动 retry；人工 retry 必须新建 attempt。只有人确认精确 version/hash 后，才能事务创建 Mission，并创建首个 `MissionMessage` 使其引用该不可变原始用户输入；只有明确的 start action 才创建首个 AgentRun（UI 可提供“确认并开始”，但语义仍是两步）；失败、拒绝或取消不创建 Mission；
+1. 旧任务草案入口继续保持以下规则；新 Agent 主导会话入口采用第 17.3 节，二者不得伪造或互换来源。旧入口无正式 Mission 时，每次用户发起草案生成最多发起一次 Provider 请求，命名为 `MissionDraftAttempt`；它使用一次受限的 DeepSeek `non-streaming` JSON 结构化调用，只接收 P0 与当前输入，不读取 Source、跨 Mission 聊天或提供 tools，也不算正式 `AgentRun`。本地持久化不可变原始用户输入、candidate draft、version/hash/status 和 `ProviderReceipt` ref。该 attempt 不自动 retry；人工 retry 必须新建 attempt。只有人确认精确 version/hash 后，才能事务创建 Mission，并创建首个 `MissionMessage` 使其引用该不可变原始用户输入；只有明确的 start action 才创建首个 AgentRun（UI 可提供“确认并开始”，但语义仍是两步）；失败、拒绝或取消不创建 Mission；
 2. 已有 Mission 时，Run Coordinator 按 `preflight → Context Packet → 模型/串行领域工具循环 → 显式 terminal tool` 编排；
 3. 同一 Mission 同时最多一个 active Run；不得并行绕过这一约束；
 4. 显式 terminal tools 只有 `create_clarification`、`submit_for_review`、`finish_run`；自然停止、`agent_end` 或普通文本输出都不能替代 terminal result；
@@ -557,7 +557,7 @@

 - Workspace：客户、自有业务或明确标记的练习 Workspace 的创建、切换与隔离状态；
 - Sources：批量上传、逐文件状态、文档/表格/DDL 预览与来源版本；
-- Mission Workbench：左侧导航资料与任务，中间审阅资料、关系、字段草案与待决问题，右侧提供当前任务的连续对话、结果入口和简明活动卡；详细运行事件在任务执行历史中按需展开，窄窗口可切换结果与Agent视图；
+- Mission Workbench：左侧合并工作区、对话/任务和资料导航；中间常驻真实进度并联动证据、问题和成果；右侧为始终可输入、可拖动和键盘调宽的主要协作区，支持任务前讨论及待澄清期间的受限讨论。会话内延续精确资料范围，逐次发送重新校验；手动浏览不被自动切换打断，窄窗口默认对话并可切换工作区。详细运行事件仍在执行历史按需展开；
 - Clarifications：待回答、已回答、冲突与阻塞影响；
 - Contract：结构化定义、字段级来源、版本 Diff、审批与导出；
 - Run details：模型、预算、时间、工具收据、公开事件和终态。
@@ -593,7 +593,7 @@

 任务消息与单次模型临时上下文分开。复用mission_messages保存明确发送的消息与受控公开答复，v4只新增run_message_inputs以关联输入、显式引用、所选历史与唯一Run；旧消息、Run与manifest不重写。通过不可变Run输入和请求hash校验每轮消息上下文，不隐含加载整个Workspace或聊天历史。

-正常追问用新的有预算Run执行；运行中只编辑草稿，不排队或热注入。partial后的合法继续按该契约校验；等待业务裁决或上次外部结果未知时不通过泛用消息绕过。新消息Run以现有finish_run.reason形成公开答复，仍为partial，不接受裸模型stop作为合法终止或Mission完成。业务澄清批准按后续独立契约交付。
+正常追问用新的有预算Run执行；运行中只编辑草稿，不排队或热注入。partial后的合法继续按该契约校验；等待业务裁决或上次外部结果未知时不通过泛用消息绕过。第 17.3 节允许等待业务裁决时使用独立受限讨论回合解释问题和整理回答候选；这不解锁原 Run 消息入口，也不消费未经批准的回答。新消息Run以现有finish_run.reason形成公开答复，仍为partial，不接受裸模型stop作为合法终止或Mission完成。业务澄清批准按后续独立契约交付。

 只读消息/历史查询可先在v3交付；新发送及v4增量必须通过合成库迁移、回滚、幂等、状态转换、权限和终态验证。真实Provider调用、个人/客户库迁移、push、发布与部署不由本节自动授权。

@@ -790,6 +790,19 @@

 最小 Demo 补充：显式 demo-fast 配置使用同一 deepseek-v4-flash 的非思考模式，生产默认仍为 high。Demo Run 在完整返回且仅存在可修复格式或 Schema 错误时允许一次模型纠正，两次请求共享原 Run 时间上限。候选草案允许保留未知维度及未完全覆盖的澄清，新增 draft_only 动作；完整性要求保留在正式提交审阅阶段。选中说明文档及明确引用必须提供有界正文与精确证据定位。纯模型生成失败在确认执行已停止、未进入领域写入且无领域收据后，可以通过新的显式发送在原任务重新生成；原未知回执保留。权限、证据归属、版本、批准和事务原子性约束不变，不自动重试未知结果。本补充优先适用于与上述行为冲突的旧约束，不改变历史验收记录。最小 Demo 的两轮交互验收先于完整质量与稳定性评测；20 次性能样本、完整案例评测、正式 Contract 和跨任务复用保留为后续工作。

+### 17.3 Agent 主导 Workbench 连续会话与交接合同（2026-09-10）
+
+采用 `docs/proposals/Agent主导Workbench-实施合同.md` 的精确内容（SHA256：`006ff8930e05464d7a1be76f564f9a08bd8eb18ea4769ea9adcafaaff65ee34f`）；原文件 PROPOSED 标记保留为批准前快照。本节与该合同只在其精确增量内取代旧任务草拟/确认前置、任务前不读来源、等待澄清不允许任何对话、以及必须分别点击批准与续接的产品要求；旧 P0/R2 原文和历史执行证据保持不变。
+
+- 新增绑定 workspace_id、可暂不关联 Mission 的连续会话与受限讨论回合。新任务从用户明确工作指令及精确来源快照建立，旧 Mission 仍从真实 confirmed Attempt 回读；两种来源严格区分，不能伪造旧 Attempt 或 ProviderReceipt。
+- 讨论复用现有单 Provider 与 Runtime 全局活动槽，独立收据绑定会话、回合和请求快照。每回合最多一次非流式 JSON 请求，无自动重试，请求 64 KiB、输出 4096 tokens、Provider 70 秒/回合 75 秒；不会获得领域写工具或业务批准能力。旧 Run、收据和 hash 语义不变。
+- 普通任务分析复用已有消息/Run 路径；待澄清讨论只能解释问题或形成可修改的回答建议。整份保存和精确批准仍使用 R2 约束；“确认并继续”通过可回读交接记录串联保存、批准和一次执行，保留批准成功但执行失败等分段状态。
+- HTTP 新增会话列表/创建/快照、消息发送/分页、提交核对、取消及交接组合/回读入口。类型、限制、作用域和失败规则依采用合同生成 Pydantic/OpenAPI/TypeScript，旧接口与记录保持兼容。
+- 显式 v6→v7 迁移新增会话、讨论收据、时间线关联及交接记录，并扩展 Mission 的来源约束；迁移前停服、备份及检查活动工作，事务执行并核验精确 schema/外键/完整性。全部已知版本检查成套适配，不自动迁移用户资料库，不接受未知未来版本，不通过覆盖旧备份丢弃新历史。
+- 本地设置保留 Keychain、显式环境/文件配置及不回显秘密的边界；仅保存设置不调用模型。“保存并发送”必须绑定已展示的原消息与资料范围，不能在刷新、浏览或连接恢复时暗中发送。
+
+该增量的原型、合成测试、真实模型和人的验收分别记录；正式 Contract、Mission 完成、跨任务知识复用、外部系统和发布权限不扩大。真实调用需独立案例与预算，本次 Git 交付限本地分支及逐片提交。
+
 ## 18. 来源证据

 - [Waku README](https://github.com/ShenSeanChen/waku-agent)：Python 自有 Loop、本地 Dashboard、领域工具与 plain static frontend。
