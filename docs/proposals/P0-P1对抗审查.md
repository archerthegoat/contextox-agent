# P0契约与P1合成诊断：主Agent对抗审查

2026-09-06；审查者：本任务主Agent，直接审查，无委派。精确契约SHA256 `6a90167c33e356da857ad9b104d5589fed2ef0325c3184cb3c19f82be5786df1`；两份待批准diff的基线/候选hash见P0-提案版本.json。架构与共享契约原文未写入补丁。

| 定位 | 反例与影响 | 处理与结论 |
| --- | --- | --- |
| §2/§5 | 误建第二套聊天正文或重写旧消息导致历史失真 | 接受复用mission_messages，仅一张输入关联表；旧消息references为空，不从流式日志补造 |
| §2 | 使用时间排序在同时间戳/时钟回退时跳页；直接把rowid当外部游标易随迁移失效 | 固定内部插入顺序，以对象ID重新解析位置；v4不重建旧表。分页字节上限不能跳过记录，单项过大报错 |
| §3 | 用户主动重复发同一句话被错误当作网络重试 | FIXED：显式新发送生成新ID；同一次请求核对才复用旧ID，不以内容相同判断动作身份 |
| §3/§5 | user消息已保存但Run未创建，或重复发送产生两次模型调用 | 同事务保存四类记录；runtime单活动槽；已有ID先读回；线程启动失败仍保留同一回执。具体实现待批准后以故障注入验证 |
| §4 | 只放开partial标签却越过未回答问题或审核中草案 | 状态校验包含底层待决对象，不只看Mission.status；waiting_for_human泛用发送暂拒绝，P5不被提前实现 |
| §4 | cancellation/error缺usage时误以为未发送，重复外部请求 | 采用保守的回执与model_started核对；数据不足拒绝继续。代价：部分已结束但用量缺失的任务需后续核对能力，首版不伪造该能力 |
| §4 | finish_run.reason变成任意模型stop成功或业务完成 | 七工具不变，仍需有效TerminalReceipt，仍partial；新消息Run仅取reason为公开答复，旧Run不改写 |
| §5/§6 | 不在manifest加消息字段会失去上下文追溯 | 依赖不可变run_id→request_sha→关联输入/历史hash/来源链；每回合读回重建校验。后续实现若不能验证任一环，不允许降级跳过；需重新提案 |
| §6 | 用户引用旧assistant答复间接带入本次未授权资料 | 校验其完整Run来源范围为当前选中范围子集，失败要求明确调整；所选历史不自动截断或放宽 |
| §5 | 提案SQL空库测试被说成迁移已经安全完成 | 只记录DDL/事务/外键初检；带历史数据、真正迁移器、回滚恢复和v4读写都NOT RUN，个人库未动 |
| §8 | 分片实验被断言为历史真实失败根因，或直接删除传输守卫 | 只确认分片敏感性；历史根因UNKNOWN；不改变现有限额，后续独立决策才可分离预算 |
| §10 | 一次契约确认被扩张为真实模型预算/实际数据库迁移/发布授权 | 明确只覆盖A–D实现与本地检查点，不包含外部调用、push/merge、个人库迁移 |

## 复查与可交付状态

上述FIXED已反映在精确契约。补丁逐行基线匹配并重建候选全文，hash一致；没有修改受保护的架构报告/共享契约。获批路线图已单独提交8b77368，匹配此前展示的候选hash。

可将本文与两份精确补丁交给人评审。尚待人选择的是接口/限额、单表v4增量、受控答复和保守续接边界；当前不将这些写成已批准或已实现。只读UI与新发送的实施须待相应具体契约获批。

## 验证记录

- 6组预算合成观察完成：单/多SSE帧对照、语义缓冲、跨轮公开累积、IPC、请求前限额；0次外部调用。
- 空合成v3库中提案DDL语法、回滚到v3、外键拒绝、旧表不变、旧应用拒绝v4均符合预期；不是完整迁移验收。
- 本次没有改产品代码，无需重跑上轮155/33完整测试；仅检查本次文档差异、可复算探针和精确补丁。
- 真实模型NOT RUN（本轮），人工产品验收PENDING；既有真实运行未被本实验重新判定。
