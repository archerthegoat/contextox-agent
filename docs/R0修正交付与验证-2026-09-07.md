# R0 修正交付与验证

日期：2026-09-07。范围：已批准的 R0 修正与草案按 key 更新修复，本地分支与提交。未包含推送、合并、R2 新接口或迁移、真实 Provider、私有资料库操作。

## 版本与授权

- 基线：`771f7de427d805240c06fabfef803e9a6a368cb9`，开始和收尾读回时本地 main、origin/main 与远端 main 一致。
- 分支：`codex/r0-workbench-baseline`，由当前任务维护，没有新建工作树。
- `87c55a6`：按 field_key / relationship_key 更新时保留未提交候选及历史版本。
- `222b3d5963f953276e06cd1cde11d1326ae2a778`：R0 界面、诊断、说明与验证；本记录为后续纯文档检查点。
- `开发路径图.md`、`docs/架构与迁移报告.md`、`docs/路径二共享契约.md`、AGENTS.md、Pydantic 模型、生成类型和依赖锁文件均未改动。

## 实现与核对结果

| 范围 | 修改及验证 |
| --- | --- |
| 草案完整性 | 同 key 完整替换，新增 key 追加，未提交项保留；unresolved_items 仍完整替换。用合并后内容计算 hash，保留 CAS 和 100 项上限。回归覆盖只改 a 保留 b 与关系 c、关系替换、空输入、过期版本、字段和关系合并超量拒绝、历史版本不变。 |
| 任务标题 | 主标题跟随选中任务；空状态为“任务工作区”；关系与字段、执行历史使用各自标题。浏览器核对两个合成任务与无 Workspace 状态。 |
| 用户状态 | 任务页与对话区共用描述性状态判断，区分待继续、待回答、待审核、未知、失败与取消。只有已保存答复和终止收据等事实齐备才显示“待继续分析”。后台状态与动作守卫保持原有语义。 |
| 来源与技术身份 | 解析 ready 显示“解析完成”；来源说明对应实际导入能力与模型发送边界。Workspace、Mission 身份移入可展开详情；批准任务时的版本与 hash 仍可核对。 |
| 等待历史 | waiting_for_human 无公开答复时显示“已产生待回应事项”，不把结构化澄清写成答复丢失。 |
| 诊断与说明 | README 区分当前实现与 N1 历史记录；doctor 对 Provider 与业务资料保持 not_run，数据库版本文案来自实际读回。保留现有 scope=n2a 合同，未扩展接口。 |
| 本机启动 | UV 将现有 .venv 改为已安装的同版本 Python 3.14.7，保留原包；locked offline sync 无依赖变化。未修改全局配置。 |

涉及文件：`src/contextox/store.py`、`src/contextox/cli.py`、`src/contextox/api.py`、`web/src/App.tsx`、`web/src/Path2Workbench.tsx`、`web/src/TaskDialogue.tsx`、README.md 及对应四个测试文件。

## 验证分层

| 证据 | 结果与范围 |
| --- | --- |
| 锁定环境 | UV `sync --locked --offline --no-python-downloads` 通过，14 packages 无变更；Python 3.14.7。未重装 npm 依赖或修改锁文件。 |
| 后端 | 标准库测试 168/168；补充关系超量用例后定向 Agent 测试 45/45；诊断文案修正后 CLI 3/3 与实际 doctor 读回通过；compileall 通过。 |
| 前端 | 最终 typecheck、38/38 测试、Vite build 通过；OpenAPI → TypeScript drift 通过，生成文件无变更。 |
| 静态范围 | git diff --check 通过；新增行敏感模式扫描无匹配，仅代表限定模式检查；受控文档、依赖与数据合同未改动。 |
| CLI 运行 | 最终代码在仓库外新临时目录经 contextox start 启动；health、OpenAPI（20 路径）、root 与引用资产均 200，SSE 首事件可读。doctor 整体 partial，实际 schema v4，Provider/customer_data 为 not_run。 |
| 浏览器自动检查 | 1280×720 与 775×900；空态、两任务切换、资料文案、详情展开、连续两次无 reload 续问、澄清后禁用发送、执行历史均检查。只使用合成资料与 fake Provider。 |
| 真实 Provider / 私有迁移 | NOT RUN。 |
| 人工验收 / 用户价值 | PENDING / NOT VERIFIED。用户授权实施不等于对最终构建的验收。 |

浏览器过程中出现一次对话读取未完成的提示，刷新后恢复。接口核对确认澄清、消息和执行历史已持久化。本批修正了提示归属：读取未核对完成不再笼统声明“发送结果未知”；没有把这次提示文案修正宣称为读取调度问题已彻底消除。发送或用量未知仍保留原请求核对提示，未开放盲重试。

## 本地证据与预览

证据目录：`/Users/archer/.codex/visualizations/2026/09/07/01a079c6-5057-7f90-9540-186c128ccadd/r0-delivery/`。

- `runtime-smoke.json`、`doctor.json`：最终 CLI 启动与诊断证据。
- `scope-build-check.json`：代码提交、文件范围和最终静态构建 hash。
- `01`–`07` 截图：实施中验证及提示问题证据；`08`、`09` 截图：最终提示修正构建的窄屏等待与历史界面。不同检查点截图不能视为全部来自最终构建。
- `browser-console.json`：本次检查未捕获 warn/error 控制台记录；不等于没有应用内提示。
- `index.html`：截图与检查记录入口。

最终静态资源：

| 文件 | SHA256 |
| --- | --- |
| index.html | `9b16cbf3d11781f90ec2fcc0b1a6375bed5de44d02ccf4fa7744fadd4a0a504d` |
| index-BMBNTbMW.js | `6378c6643fc6d00c6453049ab3c83c7823052947b5105f32c0b94438f4a318bb` |
| index-COb4L8W4.css | `2a2f9c7fa942cda6a03ffbde19bb275e76752fc081e0c1d47b757b426a3d94c5` |

合成预览保留在 `http://127.0.0.1:56343/`，数据为 `/private/tmp/contextox-r0-20260907-wup4q1w1`，静态目录为 `/private/tmp/contextox-r0-20260907-build`。该浏览器进程在最后一处 doctor 数据库版本文案修正前启动；其领域行为和最终前端已核对，最终 CLI 另用新进程完成启动检查，不能把旧预览进程冒充所有模块的最终启动证据。

已停止本任务 56344 CLI 烟测进程。用户正在查看的 56342 澄清原型继续保留，56338/56339/56340 既有服务未修改。原 56341 评审预览已在前一阶段停止。

## 恢复与后续边界

代码可按上述独立提交回退，没有新增 schema 或数据迁移。草案修复只影响后续新版本；不会自动恢复旧缺陷已经遗漏的候选，也不删除现存历史。回退草案修复会重新引入遗漏风险，不应作为数据恢复手段。

环境修复前的 pyvenv.cfg 与解释器链接记录保留在 `/private/tmp/contextox-r0-venv-backup-20260907`。仅在排障确需时参考，旧指向已损坏，不建议恢复旧解释器入口。现有包未删除，依赖锁文件未变更。

本地检查点已完成。分支未推送、未合入 main；`PENDING_MERGE` / `PENDING_MAIN_PUSH` 的原因是本批未授权这些动作。无远端写入、部署、标签或分支清理。既有 detached 工作树 `3ff6`（0bdd995）和 `89d4`（fd429a8）非本任务所有，保持原状。当前分支有尚未进入 main 的交付提交，因此保留用于评审。

R2 已确认采用“整份回答一起批准”。新接口、回答版本、续接与迁移的精确合同仍待审核批准；当前提交未实施这些能力。真实 R1 案例与 R2 合同工作沿用已讨论的顺序，未自动修改人工路线图。
