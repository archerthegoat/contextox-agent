# R1 G1 组件检查点

本页保留首个组件阶段的历史状态。当前工程结果以《R1-G1工程交付与验证-2026-09-07.md》为准。

日期：2026-09-07。基线 main@636adf4，任务分支 codex/r1-g1-tool-contract。

## 授权与范围

人已批准 G0 精确架构/合同及 G1 本地实施、检查点和完成后的本地整合；不包括真实模型调用、push 或部署。架构补丁按批准文本原样应用，原 G0 合同的历史状态文字不覆盖本条批准记录。

## 当前交付

- model_tools.py：严格的六维候选输入、无损 unknown 转换、来源目录、Run 内带类型句柄、真实表列检查、固定版本 draft_token 和累计候选数量预校验。
- agent.py：把上一代精确 P0/工具指纹对固定为历史配对，准备新协议升级；当前实际 P0/工具 Schema 尚未切换。
- Workbench：识别两个执行前拒绝码，显示“已拒绝，未执行”，不误报已执行。
- 新增六项组件测试覆盖语义转换、非法结构/Key、Run 隔离、旧 token、不重复/超限候选、目录及历史字段非法引用。

独立只读审查发现字段投影可直接注册未选来源的列；已统一改为受检 column/table 路径，并用外 Workspace、同 Workspace 未选来源、不存在表/列的反例验证，主 Agent 已复核。

## 尚未完成

新组件尚未接入 Agent 主循环，七工具新协议、错误纠正与提议计数、上下文替换、公开接缝完整任务和运行验收未完成。组件测试和现有回归不构成 G1 完成，更不构成 G2 真实语义或人工验收通过。

原合同仅提供关系的左右表句柄，未提供选择关联列的输入；现有 TableKey 要求显式 columns，程序不能代选业务关联键。人已确认精确补充：关系检查和关系候选增加 left_column_handles/right_column_handles 两个必填列表，并校验各列属于对应表。合同补充已应用，关系实现与主循环接入继续推进；本记录仅描述首个组件检查点。

无依赖或数据库变更，无真实模型调用，无服务重启或远端写入。本检查点未合并 main；主循环接入和完整 G1 检查完成后再按已批准范围整合。回退可撤销本检查点，不涉及历史数据/回执删除。
