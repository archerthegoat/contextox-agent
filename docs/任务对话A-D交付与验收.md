# 任务对话 A–D 交付与验收

> 后续独立验收更正（2026-09-06）：对 `83ecd44` 的实际连续操作发现任务版本未刷新、答复保存状态不同步、引用发送前范围提示不足。整体验收为 `FAILED`，不能将下文早先的单项浏览器 PASS 解读为连续对话已通过。复现与当前结论见 [独立验收记录](任务对话独立验收-2026-09-06.md)。

记录日期：2026-09-06。实现状态：已完成本地实现与自动验证；人工验收 `PENDING`，真实 Provider `NOT RUN`，用户价值 `NOT VERIFIED`。

## 授权与精确版本

用户确认了 P0 契约、两份受控文档的精确补丁，以及 A–D 实施和本地提交；随后明确要求推送远端。本次包含分支推送和已在 main 上的积压提交推送，不包含将新增开发分支合入 main、发布或部署。

- 批准契约：`docs/proposals/P0-任务对话与执行续接契约-v1.md`。
- 契约 SHA256：`6a90167c33e356da857ad9b104d5589fed2ef0325c3184cb3c19f82be5786df1`。
- `09dce3f`：仅应用已批准的架构报告和路径二共享契约补丁。
- `b8d6157`：后端持久消息、幂等发送、续接、预算诊断及 schema v4。
- `c447869`：Workbench 对话、结果与执行历史、引用定位、关系连线及响应式界面。
- main 基线：`e78492277c72271cb7a63595f6f6bffad59c141a`，已推送并读回一致。

提案文件及提案版本 JSON 保留其原始快照，不改写已批准哈希或历史状态。本报告记录本次确认后的实施状态。`开发路径图.md` 本轮没有改动。

## 实际行为与边界

| 增量 | 已实现行为 | 限制 |
| --- | --- | --- |
| A 读取 | Workspace/Mission 内消息和执行历史分页；消息按保存顺序展示；历史执行可查看收据及公开事件 | 不跨任务拼接；旧 v3 可读取，不能静默启用新写入 |
| B 诊断 | 在固定预算阶段记录受限计数，沿用现有预算与公开错误码 | 不保存模型正文，不提高预算；未证明此前真实调用失败的根因 |
| C 发送续接 | 单事务保存消息、执行及关联；原请求重复返回同一收据；最多 4 条明确历史、8 个引用；已知 partial 可继续 | 结果或用量未知时不自动重试；等待裁决时不把普通消息当批准 |
| D 展示 | 右侧连续任务对话；中间关系、字段和执行历史；实际草案关系连线、基数与证据状态；资料/字段/关系可引用 | 图中候选关系仍是候选；旧草案引用不可用时显示版本差异，不自动换成新版 |

每条明确发送都会形成新请求标识。网络中断或写入结果未知时，界面保留原标识，刷新后先查询原提交。浏览资料只影响浏览位置；发送范围由显式选择控制。切换任务会清空未提交草稿与资料选择，切换 Workspace 不显示旧任务内容。

P5 结构化回答、裁决与批准入口未实现。`agent_end`、公开答复和 partial 都不是 Mission 完成。旧的直接启动入口仍可用于兼容操作，详细执行信息收纳在历史与折叠区域。

## 复核与修复记录

以下是对精确实现的主 Agent 对抗性复核。均在本轮修复并进行相应复验；没有借此扩展已批准产品或架构范围。

| 位置 | 反例与影响 | 处理和证据 |
| --- | --- | --- |
| `web/src/TaskDialogue.tsx` 的 submit/reconcile | 服务已提交后返回 `state_write_outcome_unknown`，若清除请求标识会造成重复提交 | 保留未知结果的原标识；浏览器模拟 422 未知结果与刷新核对通过；另一次网络响应丢失核对通过 |
| `web/src/TaskDialogue.tsx` 的加载与 scope | 切换任务期间输入可能被后到响应清空，或旧任务结果串入 | 加载完成前禁用输入、按作用域和代次接收结果；同 Workspace 切换和跨 Workspace 检查通过 |
| `web/src/Path2Workbench.tsx:2818` | 浏览来源隐式扩大下轮模型发送范围 | 浏览与勾选分离；浏览器验证从 0 份资料开始浏览仍为 0，再明确勾选 |
| `web/src/App.tsx` 的区域与标签导航 | 查看资料后点击执行历史只切标签，没有回到任务区域 | 同步区域选择；浏览器检查引用返回、历史与结果切换 |
| `src/contextox/store.py:1813` | 相同请求 ID 更换正文、跨任务历史、变造哈希会破坏幂等或隔离 | 规范化请求哈希、事务校验和当前作用域验证；标准库测试通过 |
| `src/contextox/store.py:1661` | 启动时静默迁移或迁移失败留下半个 schema | 显式迁移开关、备份、完整 schema 校验和回滚；合成 v3 迁移成功/失败测试通过 |
| `src/contextox/api.py` 的消息错误映射 | 新消息冲突码改动误伤旧接口依赖的 422 | 限定新接口映射，旧接口保留原状态；API 回归通过 |
| `src/contextox/agent.py` 的历史契约验证 | 独立接受旧 prompt/hash 集合会允许未经批准的组合 | 只接受精确历史配对；相关测试通过 |

明确保留的限制：自动预览用 fake Provider；真实模型表现与现有私有资料迁移尚无本轮证据。Doctor 中 provider/customer_data 的旧阶段说明仍是 `not_implemented`，不能把 Doctor 当成 Agent 可用性的判断。

## 验证证据

| 证据层 | 结果 | 范围 |
| --- | --- | --- |
| Python 标准库测试 | `PASS`，166 项 | 完整现有套件，包含消息幂等、隔离、失败与迁移 |
| TypeScript / 前端测试 | `PASS`，33 项 | 类型检查与现有前端回归 |
| 生成类型漂移 | `PASS` | `npm --prefix web run check:api`，生成文件未手工修改 |
| 编译与生产构建 | `PASS` | Python compile、Vite build；依赖与锁文件未变化 |
| 差异与范围检查 | `PASS` | diff check、改动文件敏感模式扫描、限定批准范围 |
| HTTP 运行读回 | `PASS` | 56339 的 health、OpenAPI、新消息路由与 root 资源；只监听 127.0.0.1 |
| 浏览器自动检查 | `PASS` | 两次明确发送使用不同请求 ID；持久答复/刷新；引用定位；任务与 Workspace 隔离；未知结果核对；关系连线；1440/775 宽度；无脚本错误 |
| Doctor | `partial` | 环境、schema 与静态资源检查可用，provider/customer_data 仍显示旧阶段未实现说明 |
| 真实 Provider / 私有数据迁移 | `NOT RUN` | 本轮没有读取 .env、调用真实模型或迁移原资料库 |
| 人工验收 / 用户价值 | `PENDING` / `NOT VERIFIED` | 不从构建、合成运行或浏览器结果推导 |

本次沿用已安装的锁定依赖，没有新增安装或重做 N1 首次交付。浏览器使用已有 Playwright 和隔离的 Chrome 配置，不操作用户浏览器资料。终止执行的公开事件通过既有 SSE 回放实现；消息和执行列表以 REST 持久读回为准。

## 本地预览和验收步骤

新预览：`http://127.0.0.1:56339/`，使用合成 Workspace、两项合成任务和 fake Provider。对应前端源码 `c447869`、后端源码 `b8d6157`，运行数据位于仓库外的本任务临时目录。

精确静态构建 SHA256：

- index.html：`e0737849d67a4a16994729cde02bc71f959208c0fdf415d1be035fe3f49a03d8`
- index-Ci3oGO9L.js：`daefdf30a2601e76b26362cc2f6cbbd77ca9d317a65d14be41bfae66420144b6`
- index-Bs5hBIT9.css：`328e24044c4c7b6bac06a846e801183d96a70ff067ade79e665a88c97978f49f`

原 56338 服务继续保留其后端和资料库。开发中发现两个预览曾共用静态构建目录，已将新版改为独立目录，并恢复 56338 对应 main 的静态资源；两端 HTML/资源引用已分别读回。之后构建应使用独立输出目录，避免覆盖运行中旧版。

人工清单（每项待人记录，当前均 `PENDING`）：

1. 打开 56339，选择带合成验收说明的 Workspace 和已有任务，确认任务标题与对话一致。
2. 查看“关系与字段”：核对两条关系连线、基数、候选状态及来源；点击引用，查看定位内容。
3. 将字段或关系加入下一条消息，核对携带历史和资料范围后发送。预期显示用户消息、运行状态、保存的答复。
4. 在中间打开“执行历史”，查看对应轮次、终止收据和公开执行事件；回到对话后内容仍在。
5. 切换同一 Workspace 内任务，再切换 Workspace。预期不出现上一个任务草稿或资料授权。
6. 用 775 像素宽度检查结果/Agent 切换、键盘输入、焦点和滚动；刷新后消息仍可读取。

保留运行数据库、浏览器脚本、验证输出和截图作本轮证据。已停止本任务不再监听的旧合成预览进程；当前 56339 和原 56338 保留。没有清理其他任务的资料或工作树。

## 迁移、回退与 Git 交付

已有 v3 的对话写入需先停止旧实例，再用 `start --migrate-task-dialogue` 显式迁移。备份位于授权 data_dir 内的私有子目录，包含数据库、已知来源文件及校验清单。迁移前拒绝活动执行；失败保留证据并回滚事务。备份创建中失败可能留有不完整目录，必须核验清单，不能将目录存在当成可恢复证明。

尚未产生 v4 新写入时，可在停止服务并核验备份后恢复迁移前数据与匹配代码。已经写入新消息后，不得直接恢复旧备份丢弃新数据；应保留 v4 数据并前向修复或采用兼容 v4 的回退版本。原私有资料库未执行此迁移。

当前交付分支 `codex/task-dialogue` 从已验证 main 基线建立，由本任务唯一写入；本轮未新建工作树。后端和前端分为独立检查点，可逐项审阅。本报告之后推送该分支并读回远端 HEAD；精确最终提交以交付回复为准。新增分支合入 main 为 `PENDING_MERGE`，不在本轮既有批准范围。

保留的既有工作树：`3ff6` detached `0bdd995`，`89d4` detached `fd429a8`，均非本轮所有且未修改。既有 `codex/path2-real-case` 提交 `ec3142a` 已可从 main 到达，分支未删除。没有发布、打标签、远端删除、重写历史或部署。
