# R1 非流式等待修正与校准结果

本轮按用户“另开分支解决 R2，这里继续先解决 R1”执行。R1 工作分支 codex/g1-semantic-review，校准基线 2eb52b1；R2 在独立工作树/分支准备合同，不混入本提交。

## 真实校准结果

沿用原 A/B 合成材料与冻结输入，联合最多 34 请求，不设金额限制。实际仅执行 A，共 8 请求：草案 1、第一轮 3、第二轮 4。A 失败后停止，B NOT RUN，没有重发旧结果未知请求。

第一轮 partial；主审和独立自动审查认为关键事实 F01–F07 满足，完整说明/JSON读取和关系检查有工具回执。整体语义及人工验收不据此通过。第二轮完成两个请求后，第 3 请求在 SSE wire 4194399 > 4194304 字节时失败；第 4 请求正确采用非流式替代，约 60.028 秒后 provider_timeout_unknown。没有持久字段、关系草案或澄清。

共 8 回执，6 份用量已知、2 份缺失。已知输入 36999、输出 6230 tokens；完整总量未知，不补零。浏览器回读第一轮答复、来源、第二轮失败和缺用量提示，无新增 Run。现有“可查看结构化结果”提示对本次无产物场景仍不够准确，保留为界面缺口。本次临时服务已关闭，数据库 integrity_check=ok、Run=2；私有运行目录及失败回执保留，不写原始 Provider payload。

## 修正依据及实现

Provider 的父进程监督、HTTP 响应头及首正文读取均使用 first_event_ms。流式默认 60 秒，但非流式回答可能直到生成结束才返回。真实 60.028 秒与该机制相符；现有错误未标具体超时层，不能排除上游同时超时，不能宣称全部根因已确定。

agent.py 仅对非流式 fallback 使用 first_event_ms = min(原单请求 total_timeout_ms, Run 剩余时间)。流式仍沿用 60 秒。原请求总限 120 秒、Run 总限 300 秒、最多 8 请求、连接/idle/取消及有效内容边界不变；任务草案请求行为不变。没有 P0、工具合同、HTTP Schema、依赖或迁移变化。

局限：如果上游先发空白正文保活，之后停顿，仍受现有 idle 限制；本修正不保证上游更快，也不保证恢复后语义合格。

## 验证与审查

- 224 项标准库测试通过，compileall 与 OpenAPI drift 通过，diff 检查通过。
- 新增真实本地 HTTP + spawn 子进程反例，缩短时间比例：流式首响应超时仍失败；同样延迟的非流式在总限前成功；超过总限仍失败。使用合成凭据/响应，绝无真实模型请求。
- 真实 Agent/Store 消息接缝验证：剩余 90 秒时，stream 首限 60 秒、fallback 首限 90 秒，两者总限均为 90 秒，连接/idle不变。
- 父进程/响应头/首正文三处时限经独立只读审查；主 Agent 对照修正检查。原取消、连接、idle、总期限回归继续通过。
- 前端产物及锁文件未改，沿用 2eb52b1 的 49 项前端、类型及构建证据；未把这次纯后端修正宣称为新浏览器成功恢复。

## 交付与剩余

本提交是 R1 本地工程检查点，未自动合并/推送。回退为仅撤销本次等待策略和测试，不修改任何旧回执。R2 合同位于独立分支 codex/r2-clarification-loop，须精确批准后实施。

本次真实 R1 仍未通过，B NOT RUN，人工验收 PENDING。修正后的真实表现尚未验证；下一批必须冻结新提交并独立取得真实执行许可，不能改变本批版本后续跑或拼接成绩。

## f578366 新批次结果（用户批准后，2026-09-08）

新批次固定 f5783668c65af8fc4df84c1c9a416493a3363906，沿用相同 A/B 材料、模型、P0、工具合同及 34 请求上限。A 实际 9 请求：草案 1、第一轮 4、第二轮 4；全部成功且用量已知，输入 83796、输出 23281 tokens。没有触发非流式回退，因此本批没有真实验证回退等待修正；只证明此版本的一次 A 执行可以完整到达待澄清状态。

第一轮耗时约 26 秒，partial，F01–F07 经主审及独立只读复核通过；第二轮约 131 秒，waiting_for_human，持久保存草案 v1（10 个字段、1 条关系）和 1 份澄清请求（9 题）。四项关键未知均有具体问题、影响、角色、所需证据和提供定义/选择政策的动作；10 个字段的 null_handling 均正确保留 null 及未知原因。关系保留 customer_id 关联及样本边界，没有批准业务语义或完成 Mission。

**整例仍失败，B NOT RUN。** 主审和独立复核一致确认两项违反冻结 round2_requirements[2] 的六维一致性门槛：

| 对象/维度 | 观察到的错误 | 证据边界 |
| --- | --- | --- |
| f_orders_ordered_on.time_basis | 非空值将其定义为订单发生日期，同时写着时间语义未确认；unknowns 仅 null_handling | 日期字符串及样本日期范围不能确定业务时间含义；这一维应保留未知 |
| f_orders_amount.time_basis | 非空值断言金额对应 ordered_on 的发生日期且没有独立时间维度 | 两列同处订单行不能证明金额确认时点或时间归属规则 |

两项是模型生成的业务语义问题，非字段数据丢失或程序版本冲突。字段层 observed 不证明各维度均有依据；非空值里附“未确认”也不等于结构化 unknown。它们已足以触发停止条件，整体原子引用支持率未评分，不能记录 95% 或整例 PASS。唯一性措辞虽局部宽泛，同字段及关系已明确生产唯一性未确认，不将其另记为已宣称生产保证。

浏览器读取同一提交的持久字段与关系、全部九题、来源入口及待业务裁决状态；页面忠实展示了上述时间维度错误，未通过页面写入新消息。第二轮携带精确第一轮两条消息，API 历史回读正常。临时服务及检查标签页已关闭，库完整性 ok。两批失败记录分别保留，没有修改旧 Mission 或继续运行 B。

本批私有执行证据位于库外 r1-total-deadline-calibration-execution，含冻结授权、请求计数、两轮回读、主审/独立复核及汇总；原始 Provider payload 不进入 Git。此段为本地验收证据更新，不更改路线图或验收标准，不授权新一批调用、合并或推送。R2 已获用户批准在独立分支实现，本批 R1 语义失败不阻止 R2 的本地工程工作；真实 G3 和人工验收仍未通过。

## 六维证据判断纠正（本地实现，真实效果待验证）

沿用户继续解决 R1 的范围，仅调整 P0 对既有六维合同的表达：各维度独立判断依据；字段 observed、合法引用、列名、存储类型或相邻列不能共同替代对每个业务维度的支持。尚不能确定的维度须使用 null + unknown_reason，不能把“未确认”附在肯定值后冒充 unknown；无日期列不自动代表无时间基准，样本无缺失不代表缺失政策。已明确的单位、类型和规则仍保留已知。澄清用于决定某维度时，草案不能同时肯定其答案。

独立审查指出“条件候选”可能被误用在 observed 字段，已修正为 observed 的非空值必须有事实支持，条件提案须标 candidate，也不能代填未决业务政策。此修正无业务答案、具体列名特判、额外模型审查调用或自然语言关键词拦截器，不把提示指导等同于确定性语义校验。

新 P0 hash 为 d4f6eb2efe8878d07a06ee9d9eb0f60e81cde55882a81d92d164b645f213d3db；工具 schema 仍为 acaf4fda820b343181fcb19d5efa739b75f54cfa8cb15529f1d3ced74c64657d。新增旧 72c86fef… 与该工具 schema 的精确受支持配对，保留历史读取，不放宽新写入合同或任意交叉组合。

使用本批合成数据库的临时副本，在新代码下回读两轮旧 Run，完整 JSON（含旧 ProviderReceipt、草案与澄清）与冻结回读逐项一致；未修改原失败数据。无 API、存储、依赖、预算或前端变更，OpenAPI 漂移及编译通过。程序测试用于检查运行和历史兼容，不证明模型遵循新提示；新批次真实调用尚未执行。

最终 224 项标准库测试通过。中途一次关闭测试未等待 Provider 实际进入便断言，读首个 SSE 事件与 Provider 进入并不同步；单独复查通过后，4f9a731 将该测试前置条件改为最多 3 秒等待实际进入标记，原 5 秒关闭上限、取消回执及库完整性断言不变。修正后关闭测试 3 项、最终全套 224 项通过，没有删除或忽略失败测试。

## 61465f6 六维修正后的独立校准结果

用户对新执行卡回复“可以”后，固定 61465f6b799d37d32f1a5d3805e3d516c86adc63、P0 d4f6eb2e…、原模型/工具/材料/输入/评分及 34 请求上限，使用独立临时库执行 A。实际 7 次请求（草案1、第一轮2、第二轮4），全部 succeeded、用量已知，输入 96533、输出 30148 tokens；未触发非流式回退，没有重发旧请求。此次没有新增代码、依赖、预算或评分变更。

第一轮约34.2秒，partial，完整说明与两个数组相关内容覆盖，F01–F07 经主审及独立复核通过。第二轮约175.5秒，waiting_for_human，持久草案v1含10字段、1关系；1份澄清请求含6题。草案hash为 20202368009c11394a02aa9c20b839ccaff5085b6bf55c86d6eb41f797398f37。

**整例 FAIL，B NOT RUN。** 上批时间语义错误未重现：10字段的 time_basis 均为 null 并有未知原因；金额单位“元”保留已知，条件指标明确为 candidate，窗口/阈值/退款未代填。但本批违反冻结 round2_requirements[3]：四项关键未知必须逐项有具体澄清、影响、对应角色、所需证据及下一动作。

| 关键未知 | 本批澄清覆盖 |
| --- | --- |
| 观察窗口 | 第1题，业务负责人、窗口及归属证据 |
| 判定规则与阈值 | 第2题，业务负责人、规则公式或数值 |
| 退款处理 | 第3题，业务/财务负责人、退款定义及金额来源 |
| 缺失值处理 | **缺失**：10字段均保留 null_handling=null，草案 unresolved_items 也明确列出该卡点；但全部6题中没有它的决定请求、解决方、证据或下一动作 |

其余3题分别询问 ordered_on 时间语义、金额与状态口径、生产键/基数及展示名。关键卡点的具体澄清覆盖为3/4；保留 unknown 不等于完成业务交接。整体原子引用支持率因强制门失败未评分，不能记录95%或整例PASS。另保留两项非决定性歧义：币种选项混合已知单位与未知折算；候选合计的类型描述含“观测”措辞，但完整上下文仍为候选，不将其直接升级为已完成计算或关键已知误报未知。

来源与实现复核确认：P0 已要求缺失业务决定生成具体问题；问题列表上限20，而本次仅6题；Run未耗尽请求或时间预算。Store在 create_clarification 时校验草案版本/hash及引用身份，未核对每个已识别卡点是否被问题覆盖。证据支持的缺口是“已识别未知→澄清交接”的覆盖控制，不是读取失败或容量上限。本次不据此擅改问题合同、终态语义或 R2 接口，也不追加一条样例专用规则后直接重跑；后续修正需围绕通用覆盖关系与遗漏反馈审查，保留此失败反例。

API刷新回读与持久数据核对：仅两个Run，第二轮固定第一轮两条精确消息身份/hash，草案、澄清与任务读取稳定；浏览器回读完整来源、字段时间未知、已知金额单位、全部6题及待业务裁决状态，与同版持久结果一致。无浏览器发起模型请求。库完整性ok，任务临时服务与标签已关闭，原有各批数据未改。

独立执行证据位于库外 r1-dimension-evidence-calibration-execution，含授权、计数、两轮回读、主审/独立复核结论及summary。当前为校准结果和本地文档检查点，不属于正式27案例成绩；人工验收PENDING。没有merge/push、部署或原库迁移，受控路线图和架构报告未改。回退边界仅为本段结果记录，不删除或改写失败运行；后续真实新批次不在本卡剩余请求额度中自动授权。
