# R1 非流式等待修正与校准结果

本轮按用户“另开分支解决 R2，这里继续先解决 R1”执行。R1 工作分支 codex/g1-semantic-review，校准基线 2eb52b1；R2 在独立工作树/分支准备合同，不混入本提交。

## 真实校准结果

沿用原 A/B 合成材料与冻结输入，联合最多 34 请求，不设金额限制。实际仅执行 A，共 8 请求：草案 1、第一轮 3、第二轮 4。A 失败后停止，B NOT RUN，没有重发旧结果未知请求。

第一轮 partial；主审和独立自动审查认为关键事实 F01–F07 满足，完整说明/JSON读取和关系检查有工具回执。整体语义及人工验收不据此通过。第二轮完成两个请求后，第 3 请求在 SSE wire 4194399 > 4194304 字节时失败；第 4 请求正确采用非流式替代，约 60.028 秒后 provider_timeout_unknown。没有持久字段、关系草案或澄清。

共 8 回执，6 份用量已知、2 份缺失。已知输入 36999、输出 6230 tokens；完整总量未知，不补零。浏览器回读第一轮答复、来源、第二轮失败和缺用量提示，无新增 Run。现有“可查看结构化结果”提示对本次无产物场景仍不够准确，保留为界面缺口。本次临时服务已关闭，数据库 integrity_check=ok、Run=2；私有运行目录及失败回执保留，不写原始 Provider payload。

## 修正依据及实现

Provider 的父进程监督、HTTP 响应头及首正文读取均使用 first_event_ms。流式默认 60 秒，但非流式回答可能直到生成结束才返回。真实 60.028 秒与该机制相符；现有错误未标具体超时层，不能排除上游同时超时，不能宣称全部根因已确定。

agent.py 仅对非流式 fallback 使用 first_event_ms = min(原单请求 total_timeout_ms, Run 剩余时间)。流式仍沿用 60 秒。原请求总限 120 秒、Run 总限 300 秒、最多 8 请求、连接/idle/取消及有效内容边界不变；任务草案请求行为不变。没有 P0、工具合同、HTTP Schema、依赖或迁移变化。

局限：如果上游先发空白正文保活，之后停顿，仍受现有 idle 限制；本修正不保证上游更快，也不保证恢复后语义合格。

## 验证与审查

- 224 项标准库测试通过，compileall 与 OpenAPI drift 通过，diff 检查通过。
- 新增真实本地 HTTP + spawn 子进程反例，缩短时间比例：流式首响应超时仍失败；同样延迟的非流式在总限前成功；超过总限仍失败。使用合成凭据/响应，绝无真实模型请求。
- 真实 Agent/Store 消息接缝验证：剩余 90 秒时，stream 首限 60 秒、fallback 首限 90 秒，两者总限均为 90 秒，连接/idle不变。
- 父进程/响应头/首正文三处时限经独立只读审查；主 Agent 对照修正检查。原取消、连接、idle、总期限回归继续通过。
- 前端产物及锁文件未改，沿用 2eb52b1 的 49 项前端、类型及构建证据；未把这次纯后端修正宣称为新浏览器成功恢复。

## 交付与剩余

本提交是 R1 本地工程检查点，未自动合并/推送。回退为仅撤销本次等待策略和测试，不修改任何旧回执。R2 合同位于独立分支 codex/r2-clarification-loop，须精确批准后实施。

本次真实 R1 仍未通过，B NOT RUN，人工验收 PENDING。修正后的真实表现尚未验证；下一批必须冻结新提交并独立取得真实执行许可，不能改变本批版本后续跑或拼接成绩。

## f578366 新批次结果（用户批准后，2026-09-08）

新批次固定 f5783668c65af8fc4df84c1c9a416493a3363906，沿用相同 A/B 材料、模型、P0、工具合同及 34 请求上限。A 实际 9 请求：草案 1、第一轮 4、第二轮 4；全部成功且用量已知，输入 83796、输出 23281 tokens。没有触发非流式回退，因此本批没有真实验证回退等待修正；只证明此版本的一次 A 执行可以完整到达待澄清状态。

第一轮耗时约 26 秒，partial，F01–F07 经主审及独立只读复核通过；第二轮约 131 秒，waiting_for_human，持久保存草案 v1（10 个字段、1 条关系）和 1 份澄清请求（9 题）。四项关键未知均有具体问题、影响、角色、所需证据和提供定义/选择政策的动作；10 个字段的 null_handling 均正确保留 null 及未知原因。关系保留 customer_id 关联及样本边界，没有批准业务语义或完成 Mission。

**整例仍失败，B NOT RUN。** 主审和独立复核一致确认两项违反冻结 round2_requirements[2] 的六维一致性门槛：

| 对象/维度 | 观察到的错误 | 证据边界 |
| --- | --- | --- |
| f_orders_ordered_on.time_basis | 非空值将其定义为订单发生日期，同时写着时间语义未确认；unknowns 仅 null_handling | 日期字符串及样本日期范围不能确定业务时间含义；这一维应保留未知 |
| f_orders_amount.time_basis | 非空值断言金额对应 ordered_on 的发生日期且没有独立时间维度 | 两列同处订单行不能证明金额确认时点或时间归属规则 |

两项是模型生成的业务语义问题，非字段数据丢失或程序版本冲突。字段层 observed 不证明各维度均有依据；非空值里附“未确认”也不等于结构化 unknown。它们已足以触发停止条件，整体原子引用支持率未评分，不能记录 95% 或整例 PASS。唯一性措辞虽局部宽泛，同字段及关系已明确生产唯一性未确认，不将其另记为已宣称生产保证。

浏览器读取同一提交的持久字段与关系、全部九题、来源入口及待业务裁决状态；页面忠实展示了上述时间维度错误，未通过页面写入新消息。第二轮携带精确第一轮两条消息，API 历史回读正常。临时服务及检查标签页已关闭，库完整性 ok。两批失败记录分别保留，没有修改旧 Mission 或继续运行 B。

本批私有执行证据位于库外 r1-total-deadline-calibration-execution，含冻结授权、请求计数、两轮回读、主审/独立复核及汇总；原始 Provider payload 不进入 Git。此段为本地验收证据更新，不更改路线图或验收标准，不授权新一批调用、合并或推送。R2 已获用户批准在独立分支实现，本批 R1 语义失败不阻止 R2 的本地工程工作；真实 G3 和人工验收仍未通过。

## 六维证据判断纠正（本地实现，真实效果待验证）

沿用户继续解决 R1 的范围，仅调整 P0 对既有六维合同的表达：各维度独立判断依据；字段 observed、合法引用、列名、存储类型或相邻列不能共同替代对每个业务维度的支持。尚不能确定的维度须使用 null + unknown_reason，不能把“未确认”附在肯定值后冒充 unknown；无日期列不自动代表无时间基准，样本无缺失不代表缺失政策。已明确的单位、类型和规则仍保留已知。澄清用于决定某维度时，草案不能同时肯定其答案。

独立审查指出“条件候选”可能被误用在 observed 字段，已修正为 observed 的非空值必须有事实支持，条件提案须标 candidate，也不能代填未决业务政策。此修正无业务答案、具体列名特判、额外模型审查调用或自然语言关键词拦截器，不把提示指导等同于确定性语义校验。

新 P0 hash 为 d4f6eb2efe8878d07a06ee9d9eb0f60e81cde55882a81d92d164b645f213d3db；工具 schema 仍为 acaf4fda820b343181fcb19d5efa739b75f54cfa8cb15529f1d3ced74c64657d。新增旧 72c86fef… 与该工具 schema 的精确受支持配对，保留历史读取，不放宽新写入合同或任意交叉组合。

使用本批合成数据库的临时副本，在新代码下回读两轮旧 Run，完整 JSON（含旧 ProviderReceipt、草案与澄清）与冻结回读逐项一致；未修改原失败数据。无 API、存储、依赖、预算或前端变更，OpenAPI 漂移及编译通过。程序测试用于检查运行和历史兼容，不证明模型遵循新提示；新批次真实调用尚未执行。

最终 224 项标准库测试通过。中途一次关闭测试未等待 Provider 实际进入便断言，读首个 SSE 事件与 Provider 进入并不同步；单独复查通过后，4f9a731 将该测试前置条件改为最多 3 秒等待实际进入标记，原 5 秒关闭上限、取消回执及库完整性断言不变。修正后关闭测试 3 项、最终全套 224 项通过，没有删除或忽略失败测试。

## 61465f6 六维修正后的独立校准结果

用户对新执行卡回复“可以”后，固定 61465f6b799d37d32f1a5d3805e3d516c86adc63、P0 d4f6eb2e…、原模型/工具/材料/输入/评分及 34 请求上限，使用独立临时库执行 A。实际 7 次请求（草案1、第一轮2、第二轮4），全部 succeeded、用量已知，输入 96533、输出 30148 tokens；未触发非流式回退，没有重发旧请求。此次没有新增代码、依赖、预算或评分变更。

第一轮约34.2秒，partial，完整说明与两个数组相关内容覆盖，F01–F07 经主审及独立复核通过。第二轮约175.5秒，waiting_for_human，持久草案v1含10字段、1关系；1份澄清请求含6题。草案hash为 20202368009c11394a02aa9c20b839ccaff5085b6bf55c86d6eb41f797398f37。

**整例 FAIL，B NOT RUN。** 上批时间语义错误未重现：10字段的 time_basis 均为 null 并有未知原因；金额单位“元”保留已知，条件指标明确为 candidate，窗口/阈值/退款未代填。但本批违反冻结 round2_requirements[3]：四项关键未知必须逐项有具体澄清、影响、对应角色、所需证据及下一动作。

| 关键未知 | 本批澄清覆盖 |
| --- | --- |
| 观察窗口 | 第1题，业务负责人、窗口及归属证据 |
| 判定规则与阈值 | 第2题，业务负责人、规则公式或数值 |
| 退款处理 | 第3题，业务/财务负责人、退款定义及金额来源 |
| 缺失值处理 | **缺失**：10字段均保留 null_handling=null，草案 unresolved_items 也明确列出该卡点；但全部6题中没有它的决定请求、解决方、证据或下一动作 |

其余3题分别询问 ordered_on 时间语义、金额与状态口径、生产键/基数及展示名。关键卡点的具体澄清覆盖为3/4；保留 unknown 不等于完成业务交接。整体原子引用支持率因强制门失败未评分，不能记录95%或整例PASS。另保留两项非决定性歧义：币种选项混合已知单位与未知折算；候选合计的类型描述含“观测”措辞，但完整上下文仍为候选，不将其直接升级为已完成计算或关键已知误报未知。

来源与实现复核确认：P0 已要求缺失业务决定生成具体问题；问题列表上限20，而本次仅6题；Run未耗尽请求或时间预算。Store在 create_clarification 时校验草案版本/hash及引用身份，未核对每个已识别卡点是否被问题覆盖。证据支持的缺口是“已识别未知→澄清交接”的覆盖控制，不是读取失败或容量上限。本次不据此擅改问题合同、终态语义或 R2 接口，也不追加一条样例专用规则后直接重跑；后续修正需围绕通用覆盖关系与遗漏反馈审查，保留此失败反例。

API刷新回读与持久数据核对：仅两个Run，第二轮固定第一轮两条精确消息身份/hash，草案、澄清与任务读取稳定；浏览器回读完整来源、字段时间未知、已知金额单位、全部6题及待业务裁决状态，与同版持久结果一致。无浏览器发起模型请求。库完整性ok，任务临时服务与标签已关闭，原有各批数据未改。

独立执行证据位于库外 r1-dimension-evidence-calibration-execution，含授权、计数、两轮回读、主审/独立复核结论及summary。当前为校准结果和本地文档检查点，不属于正式27案例成绩；人工验收PENDING。没有merge/push、部署或原库迁移，受控路线图和架构报告未改。回退边界仅为本段结果记录，不删除或改写失败运行；后续真实新批次不在本卡剩余请求额度中自动授权。

## 澄清覆盖校验（接续任务批准后的本地工程交付）

用户批准接续任务中的《R1澄清覆盖校验提案》后，在 codex/g1-semantic-review、基线 d774248 实施。该增量承接既有 G0 模型适配层与有界纠正，不替代路线图、架构报告或真实评分；本次批准范围为本地实现、检查与提交，不含真实调用、push 或 merge。

模型草案投影及更新回执新增 clarification_obligations：从字段 unknowns、关系 unknowns 和 unresolved_items 生成当前 Run、精确草案版本/hash 绑定的临时句柄。QuestionInput 必填 covers_obligation_handles；允许额外问题为空、一道问题覆盖多项，不自动合并相似文字。创建澄清前检查全部已识别卡点的覆盖，以及覆盖问题的非空正文、影响、解决方和证据请求。遗漏在领域写入前拒绝，仅反馈最多5个安全属性路径及最多5个遗漏句柄；沿用最多两次纠正、8请求与24工具等原预算。伪造/外部句柄失败关闭，旧版本句柄不能自动升级。

关联转换为原 related_definition_paths，保留题目顺序，正式 ClarificationRequest、HTTP Schema 和 SQLite v4 不变。一般路径沿用 fields.<key>.<dimension> 等形式；超过既有 Key 的128字符限制时使用精确版本内的 <collection>.<index>.unknowns.<index>，避免截断或扩大公开字段限制。unresolved_items 同样使用所绑定版本内的索引。R2 已有UI按原题序呈现路径、批准绑定原请求与草案版本，本次未修改其独立工作树或持久接口；这不是R2集成或真实G3通过。

P0 hash：fd4d113705de9c1bd504759f4d55454d88cf8d966287c9b41c34a83af923707a；模型工具 hash：902fb158bb36fbfdc7bc021db1739400a3ca6f4b2aa87df2dcca0437a29f8c4e。旧 d4f6eb2e… / acaf4fda… 精确配对继续支持，未加入任意交叉组合，未重写历史记录。

验证证据：

- 标准库完整套件231项通过；之后仅补充/加强测试，最终9项覆盖专项全部通过，包括两个新增反例（长路径/关系未知与旧配对持久回读），合计233个不同检查项，不宣称再次执行了完整233项套件。
- 真实Store + 合成Provider：故意漏掉 null_handling，零澄清写入并反馈遗漏；补齐后只保存一份澄清、进入 waiting_for_human，实际工具收据连续且无拒绝调用的假收据。连续遗漏三次停止，草案保留。测试不判断合成问题的真实业务质量。
- 专项覆盖分组、额外问题、同文不同项、非空交接、重复/伪造/其他注册表/旧版本句柄、长路径与关系未知。历史兼容使用旧指纹的合成运行，重新打开Store后完整快照一致；没有重新打开或修改原真实失败数据库。
- 锁定UV同步、编译、生成API漂移、前端类型检查、49项前端测试及Vite构建通过。当前checkout最初缺少node_modules，前端检查因此失败；核对原锁文件111个包、版本、integrity、registry和许可证后用 npm ci --ignore-scripts 恢复既有依赖，未修改锁文件或执行生命周期脚本。UV默认缓存路径受限，改用本任务临时缓存后检查通过。
- 仓库外临时数据库、127.0.0.1服务的health/OpenAPI/SSE/root assets回读通过。doctor 对不存在目录先返回 blocked，初始化的隔离库复查后基础项 ready、总体 partial，Provider/customer_data 仍 NOT RUN。服务与临时库已清理。本次未进行新增浏览器交互验收。
- 差异与敏感模式检查通过；范围为两份生产模块、两份测试和本记录。未修改模型预算、依赖清单、生成类型、人控文档、用户数据库或R2工作树。

主审限制：覆盖句柄只证明模型声明了关联，不能保证问题语义足够或发现了全部未知；原真实逐项评分继续执行。本增量仅约束模型 create_clarification 入口，既有合法 partial 和Store直接领域调用不因此取得覆盖保证。最大20题和原预算不变，无法完成交接时不得丢弃卡点或标记R1通过。未发现需要新增迁移或公开接口的未决实施项。

回退边界：撤销本次适配/提示行为时仍需保留已产生新Run的精确指纹读取兼容；不能直接用不识别新指纹的旧程序打开新Run库，不能删除回执来伪造兼容。本次只有合成临时数据，无原库迁移。真实新批次 NOT RUN，R1仍未通过，人工验收 PENDING；后续真实执行卡需另行批准。

## d65174b 覆盖修复后的 A 单例真实复验

接续任务中用户同意固定 d65174b 验证遗漏发现、纠正与具体澄清。新批仅执行原A两轮，最多17请求；原合成材料、输入、DeepSeek配置、时间/输出预算及评分不变，B不在本次范围。精确执行卡及私有公开产物回读位于本次任务库外 r1-coverage-calibration-card / r1-coverage-calibration-execution；不复用旧Mission，不重发旧未知请求。

实际9请求：草案1、第一轮4、第二轮4；全部 succeeded、用量已知，输入100173、输出33092 tokens。第二轮约209.7秒，无非流式回退、无工具拒绝。第一轮partial，主Agent对照原文及读取回执核对F01–F07和F08；第二轮waiting_for_human，保存草案v1（5字段、1关系）与7道澄清。字段数量不是固定10个的验收项；此版本覆盖客户标识及笔数、金额、最近订单、高潜占位候选，实际业务政策仍待裁决。

本次目标结果：**四项关键卡点交接4/4，先前缺失值漏问未重现。** 第2题询问窗口；第1/3/4题询问定义及阈值；第4/5题询问退款/状态计入；第6题单独询问缺失情形，包含责任角色、所需证据、取值决策，第3题另覆盖无计入订单。所有字段的time_basis/null_handling保持null，金额单位元保留已知。模型第一次澄清提交即通过覆盖检查，因此真实“拒绝遗漏→纠正”分支未触发，只有上一检查点的合成执行证据；不能宣称本批证明了真实纠错能力或稳定性。

完整R1不记PASS，本次校准记partial并保留具体发现，不用新增门槛倒推整例FAIL：

1. **实现缺口（已本地修正，修正后真实未运行）**：关系UnknownItem.property_path已包含关系key，适配层再次拼接，生成 relationships.rel_customers_to_orders.rel_customers_to_orders.*。这是本批代码的关联路径问题，不能归因模型语义或测试过严。适配层现对关系属性路径去掉一次当前对象key前缀；无前缀时沿用原形式，保留unknown原文和旧持久结果。两种输入均有回归。未改模型协议、P0、API、迁移或预算。
2. **事实表述缺口**：f_customer_order_count.value_type写“样本内每客户0–2笔”；原未过滤样本是2、1、1，范围为1–2。若意指待定过滤口径下的可能值，必须明确条件，不能写成已观察样本事实。
3. **语义措辞待核对**：两个字段unknown_reason仍将ordered_on称为“下单日期”，来源只支持日期形态；结构化time_basis仍是null，不将此混同为上一版已填肯定时间语义的错误。
4. **影响分类待核对**：缺失值问题已提出，但标为non_blocking；其影响说明同时保留null_handling未知。它没有被解决，能继续探索也不代表能完成定义。不得将此标签当作业务定稿许可。

本轮没有给出完整原子引用支持率，不能声称达到95%或整例语义PASS。关键项只有主Agent审阅，未取得独立人审；正式重复案例NOT RUN、人工验收PENDING。上述输出表述不通过自动改写原结果掩盖。

刷新后API任务、草案、澄清及消息逐项一致，第二轮使用第一轮精确两条消息，Run仍为2、请求数仍为9。浏览器回读第一轮来源、最终7题（含缺失值问题和重复关系路径）、待业务裁决状态，与持久结果一致；未通过浏览器发送消息。数据库integrity_check=ok。验证页面和专用进程已关闭，合成库与失败/发现证据保留。

路径修正仅两份代码/测试文件：src/contextox/model_tools.py、tests/test_clarification_coverage.py；10项覆盖专项、11项真实Store合成Agent接缝、编译、API漂移与diff检查通过。前端和依赖无变化，沿用d65174b已验证构建；不将这些检查提升为新真实模型复测。本地提交同时记录这次原版复验和修正边界，不push/merge，不改R2或原库。回退撤销该路径归一化即可，历史记录与指纹读取支持保留。

## 额外澄清关联路径校验（本地修复）

用户同意继续排查结论中的修复与原结果评分后，沿用 codex/g1-semantic-review，在 a26740e 上补齐模型入口校验。原问题可用真实Store加合成Provider复现：即便覆盖句柄完整，额外的 fields.nonexistent.rule 仍会进入持久澄清。现以精确当前草案的对象、直接属性及生成的 obligation 路径构造合法集合，拒绝不存在的对象/属性、越界索引和重复关系key路径；保留已知属性与对象级关联。拒绝发生在整批领域执行之前，反馈固定安全路径和修正说明，沿用原有两次纠正预算。

此修复不判断问题语义、不将覆盖句柄等同事实证据，也不追溯修改历史记录。Store直接领域合同、API、P0/工具指纹、数据库、依赖与R2工作树均不变；现有领域入口不取得该模型入口保证。

验证：14项覆盖专项、11项G1运行接缝、7项模型适配检查通过，编译/API类型漂移/diff/新增行敏感模式检查通过。新增真实Store合成Provider反例验证错误路径零写入、改正后唯一澄清和连续收据；通过原领域形态生成含历史非法路径的合成记录，重新打开Store后快照完全一致。新增专项还覆盖不存在对象/属性、大小写、越界索引、重复关系路径，以及合法已知属性/对象关联；原长路径和陈旧句柄检查继续通过。未新增真实调用、前端变更或用户库修改，未将这些测试计为真实模型/人工验收PASS。

该检查点仅本地提交，不push/merge；回退只撤销本次模型入口检查及反馈，保留原历史数据，不涉及迁移。真实修复版本仍NOT RUN；原d65174b结果的逐条评分另作追加记录。

## d65174b 原单例来源复核补全（修复提交02e7ec2之后）

本次没有新模型请求，评分对象仍是d65174b保存的原两轮公开答复、草案与7题澄清。逐项对照原执行卡的合成说明/JSON、精确source hash与locator，并只读核对源画像；库文件hash前后一致。完整清单、摘录位置、引用与重算脚本保存在本任务库外 r1-coverage-calibration-execution/事实来源评分.md、fact-source-score.json、score_existing.py；不覆盖旧review或运行产物，不提交原始输出。

主Agent暂定结果：关键事实7/7，关键引用原子11/11，预定其他事实1/1，关键未知交接4/4，已知关键事实误报未知0，严重违规0。按公开清单的45项去重主张，43项有内容支持，2项不计支持，整体43/45=95.56%。复现相同原文不重复计分，候选方案、问题选项、行动请求和运行数量不增加引用支持分母；候选说明中夹带的来源断言仍评分。业务批准/终态边界同时核对来源与运行产物。这是单一Agent对现有结果的暂定评分，不是人工关键项复核或正式27次成绩。

两项无充分支持的主张已定位：C43“样本每客户0–2笔”未说明过滤条件，未过滤数据实际为2/1/1；C44把ordered_on称为下单日期，来源只提供日期形态。C44在两个字段中重复，去重后只扣一次；结构化time_basis仍为null，不等同上一版直接填写肯定时间口径，但仍是需要修正的语义表述。未改写原结果。

评分机制也有一个具体缺口：原卡固定95%阈值，但没有预先固定原子切分清单。当前按七列类型各计一项得到95.56%；若将七列类型作为一个类型向量计一项，其余保持不变，则37/39=94.87%。这里只展示对切分粒度的敏感性，不替换原口径或追加失败门；不能凭事后选择的百分比裁定正式PASS/FAIL。完整清单使人工复核可落到具体命题；正式重复实验需要事前固定去重/切分规则，本轮不改人控方案。

另外两项检查结论明确：第6题有完整缺失策略交接，但non_blocking与P0“阻止定稿应标blocking”不一致；R2实际未解决判定依赖答复状态与目标，未发现该展示标签导致审批绕过。因此保留4/4交接，同时保留影响分类缺陷。facts.json的partial确实存在，但源画像的唯一issue为/notice不是记录数组；/customers三行和/orders四行均完整。不能把“可能受限”风险语句当成实际业务行截断的证据，也不据此新增严重违规。

02e7ec2修复后，另用原7题公开结果作内存内离线重放：原重复关系路径被拒绝；仅在内存纠正两条路径后，7题与全部26个obligation可正常归一化。数据库hash不变，无Store写入或模型调用。这验证旧结果可定位到当前修复，不能替代真实模型面对新反馈的表现。

结论：已确认主链路两轮执行、持久候选和四项关键业务交接成立；本地路径缺口已修复。原单例保留partial，剩余具体问题是两处来源表述、缺失策略影响分类及评分粒度未事前冻结。数值暂定达到原阈值不能掩盖这些问题，待人复核也不能反过来称功能全部失败。真实修复版本NOT RUN、正式重复实验NOT RUN、人工验收PENDING。此次只追加调查记录并本地提交，不push/merge，不改路线图/架构报告或历史证据；撤销边界仅为本段记录。

## 字段未知的定稿影响与来源表述约束（1ff7b76后的本地修复）

用户同意针对上述语义/分类缺口修正并固定下一批评分口径。本切片沿用当前分支，主Agent串行负责模型适配、提示、回归和本地提交；不新增真实调用、push/merge或R2写入。验收区分可确定的分类不变量与尚需真实验证的自然语言质量。

字段六维语义的unknown表示该字段尚不能定稿。模型临时obligation投影增加required_blocking_impact：字段未知为blocking，其余为null（不等于non_blocking，也不代替模型对关系/其他卡点的判断）。题目覆盖该句柄或直接关联该未知路径时，若分类不是blocking，则整批领域执行前拒绝并说明是“阻塞定义定稿，仍可继续探索”；不会自动改写模型分类或业务值。额外题关联已知属性可为non_blocking。字段长路径索引继续绑定精确草案，持久领域对象、HTTP/API、数据库和R2回答状态不变。该校验不保证模型正确解释所有未识别的业务阻塞。

来源约束扩展到名称、unknown理由、风险和问题正文：业务事件未确认时使用来源中性的列名；派生数字作为观察事实时需说明来源总体、聚合与过滤/窗口，只保留读取证据可复算的值，否则删除该数字断言、保持候选条件化。候选标签不能包住没有依据的事实。这是通用提示约束，不包含案例答案、特定列名或预设计算结果，没有新增语义判官、执行工具或默认业务规则。自然语言误述是否消失仍未验证，不记语义修复PASS。

新P0为32a2a89ad05548171db0963bf8543a1b5c1df3e916798306f7867bc68ac5a3af；工具输入Schema仍为902fb158bb36fbfdc7bc021db1739400a3ca6f4b2aa87df2dcca0437a29f8c4e。显式保留上一fd4d1137…/902fb158…精确配对，既有历史配对不变。临时obligation字段随新P0解释，不作为持久接口或新增输入字段。

验证：17项澄清专项、11项G1接缝、7项模型适配、54项Agent检查，共89项通过；编译、API生成漂移与diff检查通过。公开运行接缝验证非阻塞误标零写入、改正后唯一澄清/连续收据且六维unknown保留；连续三次误标耗尽原恢复额度，草案保留。直接路径不能绕过要求，已知属性和未强制定级的关系问题保留可选分类。上一精确指纹与更早指纹的合成历史运行重新打开后快照一致。Provider接缝仅检查新证据约束实际随请求发送，不把合成响应当作模型学会约束的证据。前端/依赖无变化，沿用此前检查，不重跑安装或构建。

主审反例：不应把所有额外问题强行升级为blocking；不应自动把unknown变已知；提示修改不能证明事实正确；新指纹不能使旧结果不可读。上述边界分别由可选题、unknown保留、显式真实NOT RUN和历史回读覆盖。回退须保留新旧指纹读取兼容，不能直接换回不认识新Run的旧二进制或删除回执。未改变人控路线图/架构报告、旧评分结果与数据；本切片是本地工程交付，人工验收PENDING。

## 下一批评分方法冻结及原题离线核对（a896a5a后）

已完成修复提交a896a5a。随后仅更新现有验收依据《R1系统重规划与验收指标》第3.1节，落实用户同意的拆分/去重规则，标识r1-claims-v1。逐表/逐列画像、范围两端、记录映射和不同业务决策分别拆分；相同来源/对象/属性/总体的重复或矛盾表述合为同一原子，任一最终公开出现缺乏支持则该原子不计支持。包含表面完整性、事实预设、假设过滤、关键性事前标注、精确比例、零分母与争议处理规则。原95%等阈值、必需事实与调用上限不变，不将新规则回套旧45项暂定清单。

主审逐项检查本节10个通用反例，特别核对“各列无缺失”不能再与“逐列类型”使用不同粒度、矛盾句不能被重复正确句冲淡、未注明的过滤不能事后补证据、94.995%不能四舍五入通过。通过的是规则一致性审查，不是自动自然语言抽取/判分器；归一歧义仍需按冻结规则裁决并留痕，未裁决不报评分完成。只有一名主审，人的关键项复核保持PENDING。

库外r1-next-calibration-scoring保存method-v1.md逐字副本、rubric-A-v2.json及freeze.json；新rubric只改版本标识并绑定方法引用，所有旧参考事实/门槛/要求逐项相等。原输入、rubric、两轮产物及评分所记录的hash均复核未变。此工件只冻结评分方法，不是模型执行卡、真实调用授权或正式批成绩；下一批仍需在调用前绑定精确构建/模型/预算与材料。方法若改动必须新修订，不覆盖旧副本。

另对原两轮真实结果的7题做内存离线回放（impact-replay-check.json）：先只纠正上一已定位的关系路径，保留原影响分类，新适配器拒绝；按字段unknown义务检查，第6题缺失策略和第7题客户标识规则/时间属性均需阻塞对应字段定稿。在内存将这两题标blocking后，7题和26个obligation全部归一化成功。原数据库hash前后相同，未回写题目、unknown、规则或任何业务答案，没有新增Provider调用。第7题是同类缺陷的额外实例，不作为新增评分门槛或重写历史结果的理由。

本切片仅文档及库外冻结工件；diff、敏感模式、方法hash读回、旧事实/阈值不变与旧证据hash检查通过。继续本地提交、不push/merge；人控路线图/架构报告未改。回退可恢复本节文档，已冻结版本和历史证据保留。下一轮真实语义质量与人工验收仍分别NOT RUN/PENDING。

## e542986 A单例真实复验：草案入口失败，双轮未运行

用户明确“批准”后，固定e5429869ddf2126bd35993c758c5f37c0c4a2069，沿用原A材料、输入、DeepSeek v4 flash/thinking enabled/high配置和17请求上限，绑定r1-claims-v1评分。调用前核对clean HEAD、来源/输入/前端构建hash、新P0/工具指纹与评分冻结副本全部一致。新批执行卡及授权记录位于本任务库外r1-claims-v1-calibration-card / r1-claims-v1-calibration-execution；没有将旧请求或库复用为新批。

2026-09-08T07:38:54Z开始任务草案请求，实际只发送1次非流式请求（max_tokens=4096），随后停止。MissionDraftAttempt为failed，公开错误码provider_protocol_error；隔离服务安全日志为`Mission draft candidate rejected at safe_stage=finish_reason_not_stop.`。Provider回执failed，用量已知：输入312、输出4096、cache hit 0 / miss 312。数据库只有1个attempt和1个Provider回执，Mission=0、Run=0、candidate=null，完整性检查ok。

**本次完整案例未完成；第一轮、第二轮、候选/澄清检查及引用评分均NOT RUN。** 新修复的实际语义效果NOT TESTED，不能把这一结果称为新的R1字段语义失败，也不能报告单例通过。本次触发在P0_DRAFT=e588954b…的原任务草案入口，该提示与4096上限本轮未修改，尚未执行新P0_RUN和影响分类校验。

直接证据是响应结束原因不为stop，输出token正好达到4096上限。它支持“草案输出预算耗尽”的解释，但当前持久回执未保留finish_reason原值，也未区分这4096中各类输出的构成；因此不能把length或“全部用于推理”写成确证。未保存或读取原始Provider载荷/隐藏推理来补结论。程序拒绝把非正常结束响应确认为任务草案，符合现有完整性边界；残留不足在于错误原因的安全观测粒度。

按冻结停止条件未重试、未增加token预算、未改模型参数，剩余16次额度不自动转成新批授权。保存原失败回执、计数、安全诊断和summary；隔离服务已正常关闭，工具会话退出且127.0.0.1专用端口不可连接，临时合成库保留。未启动浏览器双轮验收，因为没有Mission或Run可核对；人工验收PENDING。无代码、依赖、人控文档、旧证据或R2改动。本次仅追加本地执行记录并提交，不push/merge；回退范围仅此记录，失败证据不删除。

## 草案安全结束诊断（2f9094e后的修复）

用户同意先补齐诊断、按证据决策草案预算再复验。本切片只增加草案候选拒绝的安全诊断，不改4096预算、模型/P0/工具指纹、API错误码、数据库或重试行为。已有safe_stage继续区分非stop、空内容、JSON和字段Schema失败；日志新增独立的白名单finish_reason、output_tokens、output_limit和三态output_limit_reached，同时提供同内容的结构化日志属性供隔离执行器保存。已知类别包含stop/length/tool_calls/content_filter/insufficient_system_resource，其余均为other，绝不回显原始未知值、响应正文、推理或completion身份。用量缺失记unknown/null，不补0；token触顶只记观察，不据此自行判定length。

持久失败回执仍为原provider_protocol_error且保留真实用量，新增细分诊断在安全日志中，不增加存储字段或迁移。以后执行须单独保存这份结构化安全诊断；若只有旧回执，仍不能恢复原finish_reason。此次不能倒推出上一真实失败的精确结束原因。

56项Agent检查通过，含7种结束/用量/格式反例：length触顶、length未触顶、content_filter触顶、资源结束、含私密标记和换行的未知原因、length用量缺失，以及stop后JSON非法。均零候选、单回执、重复worker不重发，重新打开Store后失败状态一致；日志文本和结构化属性不包含敏感标记。另验证stop且4096用量仍能产生合法草案，避免“等于上限就拒绝”的错误启发式。既有字段Schema安全日志/成功确认/历史回读检查继续通过，编译和diff检查通过。测试证明诊断与完整性行为，不证明真实模型稳定性。

主Agent沿用当前分支串行写入并本地提交；无新增依赖、R2改动或人控文档变更。回退仅撤销安全日志增强，不改变数据读取兼容。后续真实复验使用新一次授权身份、原预算与冻结评分，仍单例最多17请求、失败停止；不能用上一批未用额度自动重试，也不在未确认原因前提高预算。

## 7aba207 A单例真实复验：两轮完成，Agent暂定达到单例门槛

用户同意的诊断及复验序列在新身份下执行，固定代码7aba2070df67704bd86e0cda157058b3e1beaef7，沿用原合成材料、输入、模型配置、P0/工具指纹及预算，未提高草案4096上限。调用前绑定r1-claims-v1方法8fd2d036…与rubric ef657cbb…；冻结文件和前端构建hash核对一致。执行卡在本任务库外r1-draft-diagnostic-calibration-card，结果在r1-draft-diagnostic-calibration-execution/A，不改写前几次失败或评分记录。

草案请求ready，输入312、输出919；第一轮用2次请求解释资料并合法partial，第二轮用5次请求，保存6个字段、1个关系、9道澄清后进入waiting_for_human。总计8次请求，输入119591、输出37574，全部用量已知；其中1次草案非流式、7次Run流式，没有非流式回退或Provider自动重试。两轮分别32.750175秒、248.318078秒，均在各300秒上限内。第二轮出现1次tool_arguments_invalid_no_effect，未产生该调用的领域收据；该轮累计7个成功工具调用，拒绝后完成草案与澄清持久化。隔离执行器的日志处理器没有保留这次拒绝的具体字段路径，因此只能证明一次通用有界修正，不能声称真实运行命中了特定影响分类恢复分支。

持久草案v1/hash 40bb41db…：订单计数明确按全部订单为2/1/1，金额合计可复算为300/90/220元，最近日期以ordered_on取值表达且事件语义未知。30个obligation全部有对应问题，其中18个字段unknown均由blocking问题覆盖；9题中8题blocking，1题为样本外生产保证的non_blocking询问。观察窗口、阈值、退款和缺失策略均未被替业务选定，各自有问题、影响、负责人、证据请求和下一动作。未批准任何业务定义，Mission没有被完成。

按冻结规则逐一登记两轮最终公开表面、字段/关系、unknown理由、风险与问题正文；524个文字叶节点（含身份/引用元数据）均有原子映射或排除理由。记录为单一主Agent的暂定语义评审，不是独立人工评审或通用自动判分器：

| 指标 | 本次结果 | 判定 |
| --- | --- | --- |
| 必需关键事实 / 其他事实 | 7/7；1/1 | 达到冻结门槛 |
| 关键引用支持 | 31/31 | 达到100% |
| 整体引用支持 | 100/103，约97.09% | 以整数比较达到95% |
| 四项关键未知召回与交接 | 4/4 | 达到冻结门槛 |
| 关键已知误报未知 / 严重违规 | 0 / 0 | Agent暂定；人的关键项复核PENDING |
| 完整单例 | PROVISIONAL_AGENT_PASS | 校准结果，不计正式重复批 |

3个原子不计支持：关系把left_distinct_keys写成3/4（实际不同键数3，4是行数，不能由评审补释歧义）；首轮称display_name“仅为展示名称”（材料未限定业务用途，后续unknown不能冲掉前句）；金额汇总的unknown理由断言“amount本身无时间属性”（来源不能支持该语义断言）。重复出现只保留同一命题槽位，不添加正确句稀释扣分。它们未选定冻结关键类别中的窗口、阈值、退款/缺失策略或生产键保证；字段仍保留未知且未批准，所以本次未将其升级为已定业务规则的严重违规。分类依据及全部出现位置保存在claims-v1-ledger.json，供人的关键项复核，不隐去争议。

第5题再次请求金额单位确认，措辞冗余；同题明确承认资料已约定元，字段类型也保存元且没有单位unknown，因此未计F02已知误报未知。第2题共同窗口作为待确认的候选范围，未填实际窗口或批准规则；高潜输出粒度未定与已知源表粒度分别核对。这些是按原规则作出的显式裁决，不新增通过或失败标准。候选名称/公式中的观察数值仍入分母；明确设计、例子、运行状态及解析元数据不入分母。JSON的partial仅对应/notice非记录数组，两张业务数组完整，不当成业务行截断。

浏览器由Agent刷新并检查任务、澄清及关系与字段页面；API回读与保存产物逐值一致，v1/hash不变。持久消息共4条：原始任务输入、第一轮用户/助理消息、第二轮用户消息；第二轮终态由系统状态、草案和澄清呈现。第二轮携带的第一轮两条历史ID/hash精确一致，未虚构第二条助理正文。数据库1个attempt、1个Mission、2个Run、8个Provider收据，完整性ok；只读obligation核对前后数据库hash一致。浏览器刷新未新增请求，仍为8次。代理浏览器检查不替代人的Workbench验收。

当前结论是这个固定版本的单例已达到暂定门槛，不能再笼统称为“R1功能完全没有实现”；剩余3处表述质量问题保留，正式27次重复试验NOT RUN、人的关键项复核及Workbench验收PENDING。本次草案在原上限下919 token成功，说明上一失败并非该输入每次必现；它不能反推旧finish_reason，也不足以证明4096长期充足。目前不据此提高预算、不自行追加调用。诊断拒绝分支由56项Agent检查覆盖，本次成功草案没有实际触发该分支。

本切片仅追加执行记录并本地提交，不push/merge，不改路线图/架构报告。临时浏览器标签和隔离服务已关闭、专用端口不可连接，合成数据库及全部证据保留。复验记录回退仅撤销本段，不删除历史证据；代码回退范围仍为7aba207的安全日志增强。下一项验证应是固定版本的重复可靠性检查，其调用预算与人验需各自明确，不把单例或本地提交提升为正式R1验收。

## 独立复核勘误：撤回7aba207单例暂定通过

用户同意重复可靠性检查并要求分步骤Goal。复验前由review_prior_score子Agent独立只读复核，主Agent核对公开Schema、Store与Workbench后接受一个确定反例：第4题的“90元”和第5题“资料仅约定单位元”各自仅引用facts.json根及/orders；JSON没有单位，支持“元”的来源是任务说明第5行。ClarificationRequest没有容器级引用，界面逐题显示question.source_refs；TerminalReceipt的source_refs只是所有题引用的去重并集，没有反向继承到每题的合同。因此原评分以全局来源位置代替每题实际引用，错误地把unit全部出现判为支持。

**撤回上一节PROVISIONAL_AGENT_PASS。** 保持原103原子归一、只纠正已证实的unit反例，整体为99/103、关键为30/31，关键100%门槛已失败；这是充分反例修正，不是全量重裁后的最终分数。原产物、score与ledger不覆盖，库外r1-repeat-reliability-review/prior-score-correction.json保存勘误及旧文件hash。运行两轮、候选/澄清持久化与刷新回读证据仍成立，不能把评分失败写成程序崩溃。

同时确认原ledger存在把整组原子复制给question及why_needed的做法，包含没有实际断言该事实的位置。后续评审必须保存每次出现的实际文字片段、对象自身引用、所需来源位置和内容判断，分开检查身份、定位和内容；已有r1-claims-v1规则不变。其余未知策略的缺引需逐句判断，不能简单以“缺少任务说明引用”批量扣分。原3处无支持表述的非关键分类、单位未被持久化为unknown、共同窗口仍是待确认候选范围等裁决，经独立复核保留。

Goal依次推进：独立复核与勘误；补足逐出现引用核验并明确P0对象自身引用要求；固定修复后版本做最多3次独立A双轮案例；核对结果并本地提交。每例最多17次Provider入场、整批最多51次，原token/time/context预算不变；失败停止后续调用、先定位，不把未用额度当自动重试，也不混合不同版本的最佳结果。旧单例仅属校准，不计入新批。当前尚无新Provider调用，人工验收PENDING；根Agent独占仓库写入，子Agent只读审查或写各自明确归属的库外核验工件，不push/merge。

## 对象自身引用要求与兼容修正（0519176后）

P0_RUN明确每个字段、关系、澄清问题及finish_run答复必须各自携带支持全文事实的evidence_handles；其他位置的读取、兄弟对象引用、关联路径或覆盖义务不能代替。跨材料断言需分别引用支持片段，发表前核对名称、值、unknown理由、风险与问题预设；缺引用时读取并附上已有支持，不把已知事实改成未知，不用无关引用凑数。没有将案例答案写入提示，也没有自动继承引用、扩展语义判官或改变工具/API/存储/预算。

新P0为f1b4912a3bee53feca1bd4b57544c277e491d581bc0def582a9e42423b3c6834，工具指纹保持902fb158…；旧32a2a89a…/902fb158…精确组合加入历史读取兼容，不放宽成任意交叉组合。新增双来源公开接缝验证模型显式给出的两份引用逐题保存，纯请求题只带自己提交的引用，终止回执并集不扩充该题；重新打开Store后产物逐值一致。新提示随实际Provider接口请求送达。历史精确组合的合成旧Run重新打开后收据保持不变。

澄清专项、G1公开接缝与模型适配共36项检查通过，编译、工具指纹不变和diff检查通过；只读子Agent复核未发现阻止提交的问题。上述证据证明提示送达及既有引用边界，不能证明真实模型已经遵循。前端和生成API未变，沿用已有构建；新批需绑定新代码/P0及未变构建hash后再调用。回退需保留已执行新旧指纹的读取兼容，不能删除旧收据。仅本地检查点，不push/merge，人工验收PENDING。

## 4f53d49 固定版本重复检查：A1传输失败，停止A2/A3

按已批准Goal冻结4f53d49、DeepSeek配置、原A材料/双轮输入、r1-claims-v1和预算后，执行第一例A1。执行卡位于本任务库外r1-repeat-reliability-card；计数器、独立隔离库回读和评审分别位于r1-repeat-reliability-execution及r1-repeat-reliability-evaluation。每例17、整批51次Provider入场上限由调用前持久计数约束，没有将余量当自动重试许可。

实际7次入场、7条收据：草案1、第一轮2、第二轮4。草案ready后确认；第一轮partial，主Agent及独立复核的继续门通过。第二轮在生成草案或澄清之前failed/provider_timeout_unknown，draft与final_output均null，clarifications为空，terminal_receipt不存在。A1因此FAIL；A2/A3为NOT RUN，批状态STOPPED_FAILURE。未对不存在的第二轮产物计算语义分数，也未用第一轮评分或旧版本最佳结果代替整例通过。

第二轮第三次请求触发stream_fallback_sse_wire；隔离服务的安全数值日志确认stage=sse_wire、used_bytes=4194566、limit_bytes=4194304。它是原始SSE传输体4 MiB上限，包括协议封装，不能据此称模型上下文或16384输出token耗尽。原有有界回退随后只发一次非流式请求，该请求以provider_timeout_unknown结束；没有第三次重试或不完整工具写入。

事件起止时间显示流式失败约118.95秒，非流式回退约90.42秒，整轮约247.19秒。回退未达到120秒单请求总上限，整轮未达到300秒上限；现有收据不能区分idle、socket中断及其他已发出请求的未知结果，不能将原因补写成已确定的超时阶段。两条失败相关收据usage缺失；已知小计input31896、output10206，不是全部实际用量，未知不填0。

来源、第一轮自己的引用及F01–F07已核对；独立逐出现清单只覆盖实际发布的第一轮答复，另保留将paid标签表述为业务支付事件的非关键无支持断言。它不改变本次由第二轮产物缺失决定的FAIL，也不能据此宣称对象自身引用提示已经在第二轮得到验证。人的关键项复核及Workbench验收仍PENDING，正式27例NOT RUN。

只读API与SQLite回读确认1个attempt、1个Mission、2个Run、7条Provider收据，integrity_check=ok。A1隔离进程已正常终止、60824端口不可连接；数据库和全部历史/失败证据保留，失败后没有新增模型请求。没有push/merge、迁移、依赖变更或人控文档修改。此检查点只记录本批失败及其证据边界；下一步先离线复现非流式前导空白触发idle的候选缺陷，原4 MiB与其他预算均保留。

## 非流式前导空白的超时阶段修正（2939f1e后）

在原预算内定位到可确定复现的实现缺陷：非流式读取把任何非空字节都当作正文开始，包括JSON前导空格、tab、CR和LF，并向父进程发送activity。这样原首响应等待尚未结束，就会切换为更短的idle期限。本机HTTP与默认spawn/子进程接缝测试先返回四种空白，超过idle后、仍在first/total内发送完整合法JSON；修复前在父进程idle检查处报provider_timeout_unknown。

修正只在非流式读取中改变活动判定：首个非JSON空白字节到达后才启用idle；正文开始后的所有字节仍更新idle。空白仍原样保留并计入原body字节上限，first/total仍为绝对期限；没有修改SSE、父进程规则、IPC、模型提示、接口、持久化、时间/token/请求数或4 MiB上限。

必要接缝及边界共9项标准库测试通过：旧失败反例现在成功且内容/usage完整；正文开始后持续空白保持活动；持续前导空白不能延长first或total；真正正文停滞仍idle超时；取消仍记录结果未知并回收子进程；513字节空白在512测试上限处拒绝；SSE的wire/event/aggregate边界独立、TLS读回和Agent原预算缩放继续通过。每个新HTTP场景只有1次本机请求；不调用真实Provider。编译与diff检查通过。

该证据证明前导空白处理缺陷已修复，但A1未记录字节到达时间与具体timeout阶段，不能追认它就是A1约90秒失败的原因。真实修复版仍NOT RUN，原A1保持FAIL、A2/A3保持NOT RUN，人工验收PENDING。回退只撤销这一读取条件与对应测试，不涉及迁移或历史证据；交付仍为本地分支检查点，不push/merge。

## 安全超时阶段诊断（099fc2d后）

为避免后续失败只留下笼统错误，ProviderTimeoutUnknownError增加内部诊断，记录first_response、idle、total或仍未知的读取/已派发/子进程关闭边界，以及观察位置和有界毫秒数。生产child通过独立timeout字段向父进程传递；字段形状、枚举和整数严格白名单，其他错误夹带该字段或任意附加正文均拒绝。原无字段形状和null保持unknown，公开错误码、failed状态、usage未知与持久收据合同不变。

只在最外层complete向调用方抛错时输出一次安全日志，包含stream/nonstream、阶段、位置、elapsed_ms和limit_ms，附相同结构化属性；child自身不重复打印。秘密、正文、URL、身份、路径、headers和异常原文均不在诊断中；未知或被修改的诊断回落unknown，不回显原值。没有新增重试、请求、等待、时间/token/字节预算或存储字段，也不改变P0/工具指纹。

阶段描述仅为边界观察：elapsed_ms是该观察进程内请求已用时间，idle的limit_ms是静默窗口，二者不能直接相减。reader在捕获异常时依现有期限分类，supervisor沿原检查顺序；多个期限同时到期不证明唯一底层原因。read_or_peer_unknown、after_dispatch_unknown、child_closed保留不确定性，旧A1阶段不会被事后补写。

分组执行的30个不同标准库检查通过，覆盖真实child序列化、单次日志、严格字段与私密标记反例、旧形状、父进程及真实socket读取的first/idle/total、HTTP前导空白、TLS、取消和子进程退出、原SSE预算与一次回退/失败停止语义。编译、diff和OpenAPI生成漂移检查通过；后者最初被默认UV缓存权限阻挡，改用本任务临时缓存后通过，依赖与锁文件未变。独立只读审查未发现阻止提交的问题。

真实模型复验仍NOT RUN，A1失败证据及剩余案例停止状态不变；人工验收PENDING。回退可撤销新增内部诊断、IPC可选字段与测试，公开存储无迁移。该切片只本地提交，不push/merge；新的真实执行须另行冻结精确修复版本与预算，不能重新打开旧失败批次或覆盖其成绩。

## 3e941fe 修复版独立复验：A1仍失败，卡点定位到传输上限与回退总等待

用户批准新执行卡后，固定3e941fe352579e6dff5fdd6dc47c877e289182a7、DeepSeek v4 flash / thinking enabled / high、原合成材料、双轮输入、P0和工具指纹、评分方法及预算，启动新的独立批次。新批位于本任务库外r1-timeout-fixed-reliability-execution，评审位于r1-timeout-fixed-reliability-evaluation；旧批及原始评分记录不覆盖。每例17、整批51次新增Provider入场上限保持，失败即停止。

**A1 FAIL，A2/A3 NOT RUN，批状态STOPPED_FAILURE。** 草案ready后确认；第一轮partial并通过继续门；第二轮failed/provider_timeout_unknown，未保存draft、clarifications、final_output或terminal_receipt，Mission为blocked。没有第二轮产物可供语义评分；整例不能由第一轮结果或其他版本的成功补成PASS。

新批实际9次请求及9条回执：草案1、第一轮3、第二轮5。7条用量已知，输入小计55713、输出小计10766；另外2条usage缺失，不能补0，也不是完整实际费用。与前一4f53d49重复批的7次合计16次，这仅是这两批的合计，不是本任务全部历史调用。余下请求容量不自动转成下一批授权。

| 第二轮边界 | 本次证据 | 能支持的结论 |
| --- | --- | --- |
| 第1–3次流式请求 | succeeded | 模型完成前三次响应；随后有7个成功读取/检查工具收据 |
| 第4次流式请求 | stream_fallback_sse_wire；4194883 > 4194304字节；事件至收据约109.047秒 | 原始SSE传输体触及4 MiB上限，包含协议封装；不能推定16384输出token或262144内容上限耗尽 |
| 第5次非流式回退 | provider_timeout_unknown；first_response / supervisor；elapsed_ms=120001、limit_ms=120000 | 父进程已知请求发出，在期限内未观察到有效活动；没有idle阶段诊断 |
| 整轮 | 255.027644秒 | 未到Run的300秒期限；单请求回退已用满120秒 |

事件至收据时间包含调度与持久化，不是精确HTTP传输耗时；超时日志另用单调时钟。当前非流式first_response与total均为120秒，监督器先检查first_response，因此不能把诊断解释成唯一的首响应原因，不能证明网络完全无字节，也不能回填上一批约90秒失败的阶段。前导空白修复确有本机反例依据，但本次真实失败不再支持继续归因于30秒idle缺陷。提高时间或字节预算是否能成功，当前证据均不足。

第二轮首次模型响应中有6个工具提议被无副作用拒绝：4个batch_rejected_no_effect及2个tool_arguments_invalid_no_effect；这些call_id没有started/completed事件或成功工具收据。后续7次读取与检查成功。公开记录不足以确定无效参数的具体字段，不能补写为已定位的旧句柄问题；没有不完整候选被执行或保存。

主Agent与独立Agent分别核对第一轮的F01–F07及对象自身引用。逐出现清单含63个原子、76处实际断言；62/63整体支持，12/12关键引用支持，仅适用于第一轮公开答复。唯一未支持的非关键断言是“是否有退款金额列均未知”：当前完整订单数组的五列中不存在该列，不能把可见来源Schema的不含此列写成未知，也不能由评审事后改释为生产数据情况。该问题不代替本次由第二轮无产物决定的FAIL。

独立运行复核的39项一致性检查通过，包含固定身份/指纹/材料、请求计数、收据、未知用量、停止条件、工具无副作用拒绝和历史消息精确携带。主Agent核对API与SQLite，确认1个attempt、1个Mission、2个Run、9条Provider收据，integrity_check=ok。浏览器刷新显示同一失败Run、需核对上次结果及2份缺失用量，持久消息回读不变，刷新未新增请求；这是失败状态的浏览器检查，不是成功产品验收。

专用服务及浏览器标签已关闭，51967端口不可连接，隔离合成数据库与失败证据保留；只读完整性检查前后数据库hash一致。此切片仅追加执行记录，无代码、依赖、迁移或人控文档改动。本地检查点仍在codex/g1-semantic-review，不push/merge；回退只撤销新增记录段，不删除证据。R1未验收，正式27例NOT RUN，人工关键项复核与Workbench验收PENDING。

下一步在不调用Provider、不改变已冻结预算的前提下，离线核对4 MiB原始SSE上限与事件/聚合内容上限的关系，准备可审查的最小修正及反例。若修改明确冻结的字节预算或启动新批调用，先对确切变更及新增预算取得批准；不重复运行已失败批次，也不将预算调整表述为已经解决模型语义或可靠性问题。

## SSE封装开销离线反例与待决差值

按[DeepSeek官方流式响应字段形状](https://api-docs.deepseek.com/api/create-chat-completion/)构造纯合成公开Provider接缝：16000个单字符delta、正文16000字节、最大事件428字节，累计传输体4784442字节。现有4 MiB边界拒绝；仅在测试进程内临时设8 MiB后，同一合成响应完整读取。另核对8 MiB累计wire、262144单事件和262144聚合内容超限仍分别拒绝，共5项检查通过，网络请求0，仓库常量未改。测试声明的16000输出token并非分词器实测，不能据此证明真实A1在16384 token内的实际输出大小。

该反例证明协议封装可以使内容仍小的流先触及wire边界；没有捕获真实载荷，不能断言它就是A1的片段构成。8 MiB作为单变量诊断候选已形成库外r1-wire-budget-proposal/方案.md，状态PROPOSED_PENDING_APPROVAL；待决差值包括每个流允许接收和解析的传输量翻倍及新的最多51次独立复验调用，其他时间/token/内容限制和业务评分不变。它不是已实施的修复，也不是R1通过证据。

独立审查与主Agent确认一个关键反例：现有sse_wire可触发一次非流式回退，provider_timeout_unknown不能。提高wire后可能先到120秒并直接失败，失去原4 MiB处的回退机会。因此预算增大不保证成功率提高；新A1在约109秒碰到wire，仅约11秒请求余量，不知道完整输出还需多久。该风险在提案中显式保留，不同时新增超时重试、延长等待或改变模型/P0。离线脚本、结果及审查与真实失败证据分别保存；等待确切预算差值批准，不自动续跑旧批。

## 8 MiB传输上限获批实施（af84656后）

用户明确批准库外方案f8e8501a…及新批最多51次调用后，生产实现仅将MAX_SSE_WIRE_BYTES从4 MiB改为8 MiB。请求/事件/聚合内容、token、时间、模型/P0、工具、存储和原一次回退逻辑均不变。仍保留提高wire可能先遇到总超时而失去回退机会的风险，不据预算增大宣称可靠性提升。

在现有Provider测试加入碎片化公开接缝反例：原4 MiB实现报sse_wire（4194372 > 4194304）；批准的8 MiB实现完整读取16000字节、stop及合成声明用量，保持单次请求、16384输出请求参数和连接关闭。原独立边界测试继续拒绝8 MiB累计wire、262144单事件及聚合内容超限。

针对性验证共21项标准库检查通过，涵盖该反例、独立边界、分片UTF-8、读取取消/期限、接收缓冲区、Agent八轮/工具预算、一次回退和失败后不再请求。首次组合命令中两个模块因未设置tests导入路径未加载；补齐PYTHONPATH=src:tests后仅运行这两个模块，15项通过，未改测试断言或生产行为。编译、diff检查及独立只读审查通过；API/前端未变，既有生成与构建证据继续按原范围使用。

本切片只在codex/g1-semantic-review本地提交，不push/merge；回退仅撤销常量与对应回归测试，不改历史数据。新真实批次尚未启动；随后冻结该提交、原材料/评分和8 MiB预算，以独立批身份执行最多3个A案例、每例17/整批51次，失败停止。请求上限不是计划耗尽量，模型工具往返与失败回退分别记账。真实可靠性及人工验收仍PENDING。

## 07f983a新批：草案增加未请求的数量条件，确认前停止

固定07f983a1f1c20ef8251a3eb667bdbe3520d71744及已批准8 MiB差值，原材料/输入/P0/评分不变，新批r1-wire-8mib-07f983a使用独立隔离库、空账本和执行器。启动/Provider入场核对实际wire常量，变更性POST核对AUTHORIZED、ACTIVE、案例、版本和loopback runtime；9项离线准入检查及独立22项冻结核对通过。服务health、OpenAPI和根资源与冻结版本相符；这些不代表真实语义通过。

本批仅发送1次非流式草案请求，ready，输入312、输出377 token，用量已知。候选completion_criteria[3]自行加入“列出至少10个需要业务确认的口径问题”。原输入未规定数量，原rubric要求四项关键未知完整交接并明确允许一题覆盖多项；该最低数量是额外任务验收条件，不是原要求的同义改写。

主Agent与独立语义复核决定不原样confirm，将候选留在未确认阶段。批状态STOPPED_SCOPE_MISMATCH，A1为BLOCKED_UNCONFIRMED_DRAFT，A2/A3为NOT RUN。attempt仍为ready，不写成Provider或传输failed；Mission=0、Run=0、收据=1。没有新增F01–F07失败项、草案引用分母或10题通过门槛，也没有删掉数字后继续固定批次。**两轮及8 MiB真实流式效果均NOT RUN/NOT TESTED。** 新数量可能增加未来输出负担，但本例没有后续调用，不能用它解释旧批超时或推定已经增加实际调用。

当前草案确认请求只接受候选版本/hash与来源，不能原位编辑验收文字；直接改数据库或替换已生成候选会绕开版本合同，本次未做。P0_DRAFT要求按输入生成候选，但未明确禁止新增定量验收条件；ready仅证明结构解析，并不批准新增任务范围。这是草案生成及确认前范围核对的产品缺口，与原4 MiB传输限制分开记录。

专用服务正常退出，56818端口关闭，未打开任务浏览器验收。只读SQLite核对1个attempt、0 Mission、0 Run、1收据，integrity_check=ok，检查前后库hash不变，合成库与候选原文保留。此记录追加并本地提交，不push/merge，不改人控文档或原标准；原三批各自保留7、9、1次入场，不混合成绩。新批剩余50次未使用，按照已批准失败停止规则不自动重开批次。

## 草案范围保持修复与共享剩余额度获批实施

用户明确批准库外r1-scope-preservation-proposal/方案.md（659d8613…）及精确生产差值cb74b93a…：P0_DRAFT保持原任务范围和排除项，禁止自行新增最低数量、期限或配额，保留用户明确给出的数字。新提示指纹8bdff48c…；P0_RUN、工具协议、模型、API、Schema、依赖及其他预算不变。

提示指纹变化曾在隔离反例中导致旧草案收据无法读取。本次只将精确旧指纹e588954b…加入历史读取支持；ready/confirmed/failed等历史结果只有原候选、状态、错误码和完整收据逐值一致才可幂等返回，实际新写入仍要求当前提示指纹。未知指纹、篡改重放及新写旧指纹继续拒绝；无迁移或原记录改写。

60项标准库测试通过，包含新增4项范围送达/历史兼容回归及现有Agent测试。新增测试中SQLite连接关闭警告已通过显式关闭修正，并单独以ResourceWarning作为错误重验受影响一项通过。编译与diff检查通过。测试只证明提示送达和持久化兼容，不证明真实模型遵守范围或R1通过。

当前切片归属于既有R1 Goal，主Agent为唯一仓库写入者，独立Agent审查代码与库外执行器。按批准将本轮51次额度中的已用1次保留计数，剩余50次由后续修复验证共享；跨批/版本不重置，工具往返、回退和未知结果均入账。失败即停，只有有依据的范围内实现修复、离线检查及本地提交后才能冻结新批；禁止原实现反复重抽。最终仍须同一冻结批次3个独立案例完整通过，额度耗尽保持未完成。

交付仍限codex/g1-semantic-review本地提交，不push/merge；人控文档不改。旧停止批及隔离数据库保持不变。本切片尚无新真实调用；后续在提交后冻结版本执行。回退不得删除原证据或破坏已生成的新/旧指纹记录读取兼容；正式27例NOT RUN，人工验收PENDING。

## 9a4e44f共享额度批：范围修复有效于本例，第二轮总时限失败

固定9a4e44f5b4542102bea657b76898c70d2074f6d0，新批r1-scope-9a4e44f继承原模型、资料、评分、P0_RUN、工具及8 MiB等预算，只采用已批准的新P0_DRAFT。共享账本保留上一批1次为种子，先持久化计数再放行；12项离线执行器检查在python -O下通过，覆盖跨进程最后一个额度、每例17次、未知不退款、记账失败不外发、重复POST和候选范围门。独立复核发现的并发开批、前例身份及记账异常门禁问题均在冻结前修正，未调用Provider探测。

本例草案的三项验收条件保持原输入的证据解释、关系/字段草案及业务确认问题，没有再添加最低题数。主Agent与独立Agent核对后原样确认候选，不改数据库或降低原要求。这是该例范围保持的真实证据，不代表所有草案必然遵守提示。

第一轮合法partial，3次模型请求、7次成功工具调用，完整读取说明与JSON。公开答复及其自身持久引用回读一致；独立逐出现复核为70/70原子支持、关键16/16、共95次出现，主Agent核对关键事实、完整正文及样本/业务范围区分。未知业务status全集、display_name唯一性按业务允许范围和生产约束解释，不把样本取值当已确认保证。原第一轮继续门通过；第二轮只携带精确第一轮user/assistant消息ID与hash。

第二轮首个流式请求在120秒总时限失败：provider_timeout_unknown，安全诊断mode=stream、phase=total、origin=supervisor、elapsed_ms=120001、limit_ms=120000。该Run没有成功工具收据、关系/字段草案、澄清、公开答复或terminal receipt；Mission读回blocked。监督器total分支表明先前有响应活动，不能将本次写成首响应或30秒idle超时；公开证据仍不能区分上游推理/生成、传输及其他未观测耗时。没有本次wire超限记录，不证明8 MiB已解决真实可靠性。

本批共5次Provider入口：草案1、第一轮3、第二轮1；没有回退或自动重试。5条入口与5条收据一致，已知输入22533/输出5592 token仅为可计量下界，失败请求另有1条usage missing，不按零或退款处理。共享累计6/51、剩余45；批次STOPPED_FAILURE、A1 FAIL、A2/A3 NOT RUN。第二轮语义评分NOT RUN且分母null，不能用第一轮通过补足整例。

为检验本地解析/拼接/IPC耗时解释，在当前代码运行纯合成离线反例：18003个SSE事件、4374691字节wire、192012字节工具参数及16000字节reasoning；直接FakeTransport约0.090秒，真实监督子进程/IPC加FakeTransport约0.270秒，完整解析结果与声明用量，网络请求0。合成token为声明值而非分词实测。该反例未复现120秒本地处理问题，不排除未覆盖的本地因素，也不能还原真实响应大小或上游时序；因此当前没有已定位的新增实现修复可支持开下一批，不用文档或诊断提交冒充行为修复。

浏览器刷新显示相同历史、第一轮答复及引用、第二轮问题、失败和需核对状态；API消息逐值不变、账本仍6条，没有由刷新新增调用。专用服务正常退出、60686端口与临时浏览器关闭；只读SQLite完整性ok，1个attempt、1个Mission、2个Run、5条收据，检查前后数据库hash一致。独立操作复核35项一致性检查通过，失败证据与合成库保留。

本切片仅追加执行记录并本地提交，不push/merge，不改原阈值、人控路线图或架构文档。R1/Goal未完成，正式27例NOT RUN，人工验收PENDING。剩余45次不自动使用：当前授权只允许有依据的实际修复后开新批；改变模型、请求时间或执行策略需先形成有证据的具体决定，不能靠剩余额度反复重跑。

## 已批准的一次流进度诊断：观测实现

用户明确批准《R1：一次流进度诊断提案》（SHA256 6474a7c99b69c8eba7df201c7bdaabef0e471e0a586ff9a47825b51133223fbf），仅在原共享剩余45次中增加最多1次diagnostic_only用途；不重开三例验证、不改模型或时限、不执行返回工具、不fallback或重试，不push/merge。诊断只基于原持久证据近似重建第二轮首请求，临时handle映射未保存，不能声称逐字节重放。

Provider新增固定大小的数字/布尔快照，包含wire、有效SSE事件、reasoning/content/tool-arguments各自UTF-8字节、首次时间及finish_reason/[DONE]标志。子进程按250ms采样，首字节/首有效事件/首次结束标志及收尾即时报告；父进程保留最近收到的快照，独立progress IPC消息不更新任何请求阶段或活动时钟。无快照时保留null，不当作零响应。公开API、SSE Schema、模型参数、持久化和前端均未改。

计数解释：wire是交给SSE解析器的HTTP响应体字节，不是TLS或HTTP首包；valid_sse_events统计经现有协议解析接受的非空JSON事件，不含注释和[DONE]。分类字节为已接受事件的字段内容，计数不证明有效任务进展。first_*和observed_elapsed_ms相对child请求开始；snapshot_received_age_ms从父进程收到快照起计算，不等于最后真实上游活动时间，末尾数据可能尚未被下一快照覆盖。

7项新标准库检查通过：UTF-8逐字节分片与分类计数、完整结束标志、真实子进程被终止后快照保留、progress不延长first/idle/total、DONE后本地close阻塞可见、持续progress期间取消、部分/坏流保持原错误、非法字段拒绝及敏感哨兵不进入诊断。若一个测试包含多个子情形，仍按7个测试方法计数。原有Provider与timeout/budget元数据51项检查通过；编译与diff检查通过。本阶段真实调用0；上述只证明观测及相关失败边界，不证明R1可靠性、第二轮语义或人工验收。

## 7c8426b一次诊断结果：约54秒结束，历史超时未复现

诊断固定实现7c8426bda4d3fa3cf87cdf19461c258fcb1079fe。一次性入口12项标准库离线检查在python -O下通过；独立审查的冻结文件指纹问题已修正为调用前缓存并复核。真实dry-run只读重建24448字节请求、2条历史消息及2个来源，调用0次。正式诊断以新的临时handle重建；实际请求hash d482ce631146b0cdcdb29f26be4ced4d7db59de33de54785d740a7cbf2b4504a，只标识该次近似请求。

唯一一次stream调用返回SUCCEEDED，finish_reason=tool_calls、工具调用数5，未执行工具。child最后快照observed_elapsed_ms=53950；首响应体字节/首有效SSE均184ms，wire=2097232字节，有效JSON事件6589，reasoning=30112字节，tool-arguments=391字节，content=0；finish_reason与[DONE]均已见。父进程快照接收年龄4ms，无timeout诊断或非法观测记录。用量为输入6878、输出6730 token（输入缓存命中4352、未命中2526），本次用量已知；不覆盖旧失败请求的未知用量。

结论：该路径能在现有120秒时限内完成同类首请求，本次大部分字段字节属于推理文本。未复现历史120秒失败，累计字节也不能将54秒逐项归因到推理、网络或本地处理。因此只能把“推理强度与时延预算是否匹配”列为下一项值得验证的假设，不能断言历史根因已解决。没有工具执行，也就没有新的关系、字段、澄清或第二轮语义成绩。

调用登记为独立diagnostic_only，共享账本仅6→7、剩余44；移除本次条目和注册元数据后，可还原冻结前账本hash。旧批原5条entry、STOPPED_FAILURE/A1 FAIL及usage missing保持不变，24个冻结文件与旧SQLite hash全部匹配。进程正常退出，服务互斥锁已释放，返回工具执行/fallback/后续调用均0。原三例验证继续A1 FAIL、A2/A3 NOT RUN；正式27例NOT RUN，人工验收PENDING。

后续候选变量已只读核对[DeepSeek官方思考模式文档](https://api-docs.deepseek.com/guides/thinking_mode/)：支持low/high/max，medium实际映射high。若后续决定验证low，必须同时保留原语义标准并检查质量退化；这次high已成功也构成“high必然失败”的反例，低强度更快或更可靠仍未验证。当前代码和模型配置保持high，本次批准的最多1次诊断已用完，不自动开始下一批或更改参数。上述为诊断结论与候选取舍审查，不替代受控路线图或新增实施批准。

## 2026-09-09 显式推理档位与单链路优先验证

用户随后明确要求尝试能跑通当前链路的配置，并确认“跑通一个就停”。本次范围更新为依次low→high→max，每档一个全新A双轮案例、最多17次请求；沿用同一共享51次账本，开始时已用7次、剩余44次。首个完整链路通过即停止，普通失败保留证据后才进入下一档；严重裁决或安全违规停止整个策略。此单链路目标不替代正式三例可靠性或27例验收。

当前切片只为DeepSeekProvider增加显式reasoning_effort参数，允许low/high/max；实际HTTP body与ProviderConfigSnapshot读取同一校验后的实例值，生成TypeScript同步枚举。生产默认与agent.get_provider()保持high；隔离执行器维持草案high、Run选择案例档位。P0、工具、模型、语义评分、输出/时间预算不变；上轮待批固定low架构补丁未应用，受控文档未改。后续外发限原授权合成资料、原Provider及剩余额度，交付仍停在codex/g1-semantic-review本地提交。

必要验证：Provider相关61项、运行与回退19项、前端49项通过；锁定UV同步、Python编译、OpenAPI生成及漂移、TypeScript、Vite构建通过。新增3个测试方法包含6个真实spawn/loopback请求子例（3档×2响应模式），验证子进程发送的body准确；另6个注入Provider的草案Store子例验证成功/失败收据重启回读、终态worker不重复发送及未知配置拒绝。这些离线证据不代表真实Run成功。独立代码复核未发现阻断项；执行器阶段隔离和首成功即停另行冻结检查。

SQLite DDL不变，旧high收据由新版原样读取；新low/max收据需要保留新版读取能力。回退旧high-only代码时不得用它打开含新档位的实验库；保留隔离库与证据，不改写历史收据。真实双轮与浏览器证据待后续执行，人验PENDING。

## 75a36f2双轮执行结果：high链路跑通，按用户要求停止

实际调用固定实现75a36f2ba48435df74f6d7975c214ba96c343a84、模型deepseek-v4-flash及原材料、P0、工具和预算。执行器15项离线检查通过后，依次运行low和high；草案始终使用high。用户当前优先级为“跑通一个就停”：high已到达两轮产物持久化和待人澄清终点，因此停止档位探索，max为NOT RUN。原冻结语义门独立保留FAIL，不修改评分规则、候选或产物来取得PASS；本次停止不等于旧三例可靠性Goal完成。

low共3次请求（草案1、第一轮2），草案范围核对通过；第一轮未解释一行代表一个客户或一条订单，只报告数量，缺失既定F03粒度事实，主Agent和独立复核一致判定继续门FAIL。另有对ordered_on和display_name用途的无依据推断；没有严重安全违规。第二轮NOT RUN。3条成功收据用量全部已知，输入12426、输出3152 token；没有重试或回退，不能把语义失败写成Provider失败。

high共10次请求（草案1、第一轮5、第二轮4），均成功，全部用量已知：输入113149、输出32232 token。第一轮约38.854秒，通过来源列举、读取和数据检查形成带证据的公开答复，终态partial；第二轮携带精确的第一轮消息ID与hash，约219.247秒后通过update_definition_draft和create_clarification形成6个字段、1个关系、1份含6个问题的澄清，终态waiting_for_human。两个Run分别执行7次和10次领域工具；模型调用是读取工具结果后的继续推理，没有自动retry或fallback。第二轮4次请求均在原120秒请求时限内完成，整个Run也在原300秒内；这是一例真实成功，不证明历史超时根因已消除。

最终草案版本1、hash为510a2e531445ed3c75a01fb596188071c4c58e3dabc2d226af38b16c4b5c9294，API回读、终态回执和浏览器刷新所见一致。关系保留样本适用范围，窗口、阈值、退款和缺失值政策继续等待业务确认；Mission未完成、semantic_approval仍pending。可确认功能链路PASS及自动浏览器回读PASS，不能据此记录人验或业务口径通过。

严格语义门仍FAIL：f_ordered_on和f_status的订单粒度、缺失值政策理由，以及Q4对缺失值政策的断言，共5处缺少本对象所需的任务说明引用，仅引用orders或customers数组。涉及orders.grain与policy.missing_handling两个既定关键原子，无法满足关键引用100%；其他字段或终态的引用不能替代这些对象自身引用。此外，关系风险把facts.json的partial解释成“限制可观察行数”，但实际错误为json_unsupported_fragment，客户/订单数组均已完整读取，不能推定行数截断。Q1文字字段标识与真实key/name不一致，但结构化定位正确，作为非阻断记录保留。

独立语义审查保留第一轮标注及第二轮确定反例；按本次停止要求不再扩展完整引用分母或比例，scoring_complete=false，裸unresolved_items的引用归属仍待裁决。判定FAIL有确定反例支持，但没有声称完成全量语义评分。后续质量缺口是对象引用完整性及解析元数据解释，本次不再外发验证，也不更改生产默认high。

本轮新增13次请求，共享账本20/51、剩余31；原7次条目、历史未知用量及旧批登记均保持原值，停止后无新增请求。low和high独立实验服务均正常关闭，60304和61762端口无监听，两个临时浏览器标签已关闭。隔离数据库及失败/成功产物留存，关闭后只读完整性检查通过、hash前后一致。high独立操作复核25项终态检查和10项关闭检查通过，明确保留语义FAIL；3/3可靠性未建立，正式27例NOT RUN，人验PENDING。

库外证据位于本任务r1-effort-chain-execution目录：manifest.json绑定实现与资料，stop-decision.json记录停止依据，high/A1/functional-chain-review.json、semantic-evaluation/closeout.json、independent-operations-review.json分别记录功能、语义和操作结论；原始回读与账本继续保留。本记录为执行状态追加，交付只含本地检查点，不push/merge；未创建新分支或worktree，未改受控路线图、架构文档、依赖或锁文件。代码回退边界沿用上一节，新档位实验收据的读取兼容必须保留。

## G2精确引用索引与结构化行完整性修复

用户批准先修复high成功链路中已经定位的两项确定性语义缺口，不调用真实Provider：其一，模型在后续轮次必须能直接取回每个已读片段的精确evidence_handle、来源名、locator与截断状态，才能给字段、关系、问题和终态分别绑定实际支持它们的引用；其二，json_unsupported_fragment只表示非表格JSON成员被忽略，不能自动解释为已识别数组的行数受限。Workbench继续用可点击的“引用证据 N”等简洁标签展示持久EvidenceRef，只有展开时读取片段，不在对象卡片重复全文。

实现先加入两个失败反例：来源目录缺少recognized_table_rows_complete并且读取覆盖记录缺少evidence_handle；含完整orders数组及非表格notice成员的JSON关系检查错误地产生“parse issues limit the observed rows”。修复后，Run第二轮context的coverage保留前一轮已读片段的精确句柄、来源名、locator和truncated，不携带片段正文；来源目录显式区分整个文档parse_status与已识别record arrays是否完整。关系检查仅在所有partial问题都是json_unsupported_fragment时说明“非表格成员被忽略、已识别数组全部行已检查”，遇到其他解析问题仍保守保留行受限提示。

P0_RUN补充同一约束：数据行不能证明只存在于任务说明中的规则，任务说明也不能证明观测数据值；对象引用必须复用实际支持该对象的精确句柄。新提示hash为57a29fb22fc8dcae0be98b8e146e4bac39d1f3e7e8a090d0c6b6cfb63a508528，工具Schema hash保持902fb158bb36fbfdc7bc021db1739400a3ca6f4b2aa87df2dcca0437a29f8c4e；前一提示hash 060e0655b6c000b0d92920aa06f4a81226fbef6c44eb080804d069946c6fc799只以精确历史pair继续支持旧Run读取，不形成交叉组合，也不改写历史收据。

工程验证：新增失败反例修复后通过，引用跨来源及重启公开接缝回归通过；Python标准库测试279/279、compileall、locked UV离线核对、OpenAPI到TypeScript漂移、TypeScript typecheck、前端59/59、Vite build和diff检查通过。OpenAPI首次检查只因默认UV缓存目录无权限失败，使用当前锁定环境并关闭该缓存后通过；生成文件、依赖与锁文件均未变化。仓库外临时数据目录运行烟测通过health、OpenAPI、根静态资源与SSE connected首事件；doctor的Python、依赖、Schema、静态资源和Store检查均ready，总状态按既定语义为partial，因为Provider与customer_data是not_run。

这些检查证明已定位的确定性上下文与解析语义缺口得到回归覆盖，不能证明真实模型一定正确使用引用。真实high双轮复验NOT RUN，原high功能链路PASS与严格语义FAIL均不改写；G2、3/3可靠性、正式27例及人工验收继续PENDING。改动限codex/g2-citation-grounding本地分支，不改公共API、存储Schema、数据、受控路线图或架构文档，不push/merge；回退只撤销本节对应代码、测试与记录，不删除既有真实运行证据。

## b451433 high复验：候选生成再次触及120秒总时限

用户批准冻结b45143351c62730a13c5dd5ad12df6e84ec97d0c，以原合成材料、原high配置、原严格语义门和共享51次账本新开一个双轮案例；单例最多17次，成功或失败即停，不尝试其他档位，不改评分标准。新策略r1-g2-high-recheck在离线门禁8/8及脚本语法、冻结哈希、材料、构建、Prompt和工具Schema核对通过后启动；调用前账本20/51。

草案1次请求成功，候选保持原范围并按精确版本/hash确认。第一轮3次请求全部成功，约34.34秒后以合法partial和finish_run结束；完整读取任务说明1–7行与facts.json根，F01–F07和关键引用通过继续门。非阻断记录保留一项表述风险：答复说样本中客户“可有零或多条订单”，但当前样本没有零订单客户，不能作为样本观测事实。

第二轮精确携带第一轮两条持久消息。前三次请求成功，依次完成两份来源读取、customers/orders表检查和customer_id关系检查，共5个成功领域工具。第4次流式请求在原120000毫秒总时限失败为provider_timeout_unknown；诊断为mode=stream、phase=total、origin=supervisor。最后快照observed_elapsed_ms=119575，wire=4466523字节、14022个有效SSE事件、reasoning=56898字节、content=253字节、tool arguments=2997字节，未见finish_reason或[DONE]。未触及8 MiB wire边界，持续有有效流量，不能解释为首响应或idle超时；现有证据只支持high候选生成在该次运行超过单请求总时限，不能恢复未完成的工具参数或推定再需多少时间。

本例未持久化draft、relationship、field、clarification、final_output或terminal receipt，Mission回读为失败状态。因此功能链路FAIL，新的对象引用与json_unsupported_fragment解释均NOT ASSESSABLE；不能用已执行到读取和检查路径推定语义修复真实PASS，也不覆盖上一批成功链路或失败评分。

本例共8次Provider入场：草案1、第一轮3、第二轮4；7条成功且用量已知，合计输入44828、输出7293 token，失败收据1条usage missing，不记0。没有自动retry或fallback。共享账本更新到28/51、剩余23；失败即停后无新增请求。隔离服务和55794端口已关闭，SQLite integrity_check=ok，关闭前后数据库hash一致，记录为1个attempt、1个Mission、2个Run、8条Provider收据和9条工具收据。

库外证据保存在r1-g2-high-recheck，closeout hash为4c53b169c4dfb674814fd5e432fe5176b78c289eb8c9cb5e480fb4a0361e9518。G2真实语义验收仍PENDING，3/3可靠性未建立，正式27例NOT RUN，人工验收PENDING。当前确定的新卡点是生产默认high与单请求120秒总时限的波动性冲突；改变该时限属于新的预算和运行合同决定，应先形成精确差异、历史兼容、回退及新增调用上限，再实施或复验。
