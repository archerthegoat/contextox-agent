# P2 完整评测预算卡（待精确批准）

状态：`PROPOSED / PROVIDER NOT AUTHORIZED`

card_id：`P2-FULL-EVAL-20260912`
基线：待绑定实现提交、安装包 SHA、资料/rubric 指纹和 DeepSeek 价格快照。

本卡只把既有完整质量门转换为当前确定性控制器的请求、token 与费用上限；写入本卡不授权任何 Provider 调用。执行前必须再次展示本卡最终 hash、精确金额和调用范围，并取得当前明确授权。

## 1. 冻结配置

- Provider：唯一 DeepSeek OpenAI Chat Completions Adapter。
- 请求模型名：`deepseek-flash`；必须记录响应可观察到的实际模型映射。不得使用旧别名或自动路由。
- 质量 Run：`demo-fast`，thinking disabled；每个 Run 正常一次请求，且仅在完整响应为可修复格式/Schema 错误时允许一次纠正。
- 每次 Run：输入 JSON 上限 65,536 bytes；输出上限 16,384 tokens；Provider 70 秒，Run 75 秒；HTTP 自动重试 0。
- 任务建立：每案例至多一个 Mission draft 请求。
- 不调用 Profile Interpretation，不增加讨论回合，不调用第二 Provider，不重放未知结果。

## 2. 完整门与请求上限

| 批次 | 案例 | 每案例上限 | 请求上限 | 停止规则 |
| --- | ---: | ---: | ---: | --- |
| 哨兵 | 原始案例 3 次 | 5 | 15 | 未达 3/3，停止后续质量批 |
| 保留集 | 8 案例 × 3 次 | 5 | 120 | 总计低于 22/24 或任一案例低于 2/3，保持 FAIL/PENDING |
| 性能 | 固定慢路径 20 次 | 1 | 20 | 少于 19/20 在 60 秒内结束，性能门 FAIL |
| 最终演示 | 合成任务建立 + 两轮 + 两次纠正余量 | 7 | 7 | 成功即停，不消费剩余额度 |
| **总计** |  |  | **162** | 达到任一停止条件或预算即停 |

“每案例 5 次”由 1 次任务建立 + 2 个交互 Run × 每 Run 最多 2 次请求组成。性能样本与质量样本本卡按保守上限分开计；若最终决定复用同一批样本，必须先减少总授权，不把余额用于其他调用。

## 3. Token 与费用上限

为避免把 byte cap 错当 tokenizer 精确值，本卡使用更保守的 1 byte ≤ 1 input token 记账：

- 输入 token 理论上限：`162 × 65,536 = 10,616,832`；
- 输出 token 理论上限：`162 × 16,384 = 2,654,208`；
- 任何 usage 缺失都保持 unknown，并立即停止新调用；不以 0 填补。

2026-09-12 DeepSeek 官方 Flash 价格快照：cache miss 输入峰时 `$0.30 / 1M tokens`，输出峰时 `$1.20 / 1M tokens`；据此最保守理论费用为 `$6.3700992`（显示为 `$6.37`）。拟议授权上限为 **162 次请求且已知费用不超过 USD 8.00，两者先到即停**。价格、模型映射或计费规则变化时，本卡失效并须重新计算；cache hit 只降低实际费用，不提高额度。

## 4. 质量门

- 严重安全/裁决违规：0；任一次即停止。
- 关键事实覆盖 100%，其他事实 ≥90%。
- 关键引用支持 100%，总体 ≥95%，按 `r1-claims-v1` 原子拆分和精确分数判断。
- 关键未知召回 100%，关键已知误报未知 0。
- 候选产物、澄清可执行性、连续性和 Workspace 隔离分别记录。
- 自动归一/计分不能写人工语义 PASS；每个公开文字位置必须映射原子或说明排除理由。

## 5. 每次请求前后门

请求前：绑定代码/构建、P0、模型、配置、材料、rubric、case/trial、请求序号、剩余请求与费用上限；校验 Key 配置状态但不读秘密。请求后：保存 ProviderReceipt、已知 usage、时延、错误码、候选/澄清/终态 hash；不保存原始 Provider payload、隐藏推理或私有资料正文。

未知结果、权限/来源错误、余额不足、价格变化、严重违规、预算抵达或配置漂移时停止，不自动重试。每完成一个批次先出阶段报告，再决定是否继续下一批。

## 6. 本卡需要的人类确认

执行前需确认精确文本：

```text
确认执行 P2-FULL-EVAL-20260912：最多 162 次 deepseek-flash 请求，已知费用不超过 USD 8.00；仅限冻结的公开/合成评测案例、性能样本和最终 Demo；不含 Profile Interpretation、讨论调用、客户数据或其他用途。
```

没有上述当前确认时，Provider 状态保持 `NOT AUTHORIZED / NOT RUN`。
