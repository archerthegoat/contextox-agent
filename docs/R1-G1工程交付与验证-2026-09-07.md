# R1 G1 工程交付与验证

2026-09-07。工程提交 `e0a312958a52d8bdca7b53c9c6fcf6e112126029`，任务分支 `codex/r1-g1-tool-contract`，统一基线 `main@636adf4e461499ce3e20e74a4c0fe255bb4c80ea`。架构与 G0 合同提交为 `230bdfc`。

## 结果及范围

已批准的 G1 模型工具协议已接入真实 Agent 主循环及现有 Store。工程接缝检查通过，可以按已有授权整合到本地 main。本记录不表示 G2 真实语义质量、R1 全体验收、R2 闭环或人工验收完成。

- 七个工具从独立 Pydantic 模型输入生成 Schema，再确定性转换为原领域模型。字段六维值与未知理由严格互斥，不补业务值。
- source/table/column/evidence 句柄限制在当前 Run 的精确已选来源，draft_token 固定 version/hash。旧 token 不升级；关联列由模型显式选择，校验归属、唯一性和配对长度。
- 整批参数先校验，错误批次零领域执行；最多两次纠正，第三次拒绝终止。拒绝提议计入 24 次工具上限，已拒绝 call_id 也不能复用。实际 ToolReceipt ordinal 连续且不为预拒绝伪造收据。
- 每轮替换当前应用快照，保留 Provider 所需的 assistant/tool 与 reasoning 历史。目录使用实际范围，记录读取成功、截断或失败；未读取不等于已覆盖。
- Workbench 对两个预拒绝码显示“已拒绝，未执行”。无新事件类型、数据库迁移、依赖、业务默认值或审批权限。
- 诊断仅记录受限阶段、次数、字节、时长、finish reason、是否观察到响应 ID 和用量是否已知；不记录 ID 原值、原始参数或推理正文。旧精确 P0/工具指纹配对保留。

## 验证证据

| 层级 | 结果及边界 |
| --- | --- |
| Python 回归 | 标准库 unittest 全量 200 项通过。最后仅改进拒绝提示及关联列错误路径后，重新运行新接缝 11 项，全部通过；compileall 通过。 |
| 新模型输入组件 | 7 项：无损六维转换、非法形状/Key、句柄隔离与固定 token、累计候选上限、禁止未选或不存在表列、真实目录/证据、七行和空文本范围。 |
| 新完整调用接缝 | 11 项：真实 Store，模拟 Provider 仅使用提供的上下文与工具输出；覆盖七工具调用、整批零副作用、纠正次数、拒绝 ID 去重、提议预算、旧 token、伪造句柄、显式关联列、澄清/送审终态、错误列归属、失败覆盖及 Store CAS 竞态。部分测试包含多场景。 |
| 原有测试边界 | 旧 test_agent、test_field_contract、test_source_bounds 的手写领域调用通过仅测试用 LegacyFixtureAdapter 保留回归；不将其算成新协议模型能力证据。test_dialogue 已改用新 finish 输入，并使用真实适配器。 |
| 安装及生成 | UV locked sync 通过；npm ci --ignore-scripts 通过，锁文件未变；OpenAPI 生成漂移检查通过。 |
| 前端 | TypeScript typecheck、38 项测试、Vite build 通过。 |
| HTTP 临时运行 | 仓库外临时数据；仅绑定 127.0.0.1。health、20 条 OpenAPI 路径、根页面及构建 JS、真实终态 SSE 读取通过。运行内 Provider 为模拟实现；临时服务已关闭。 |
| doctor | 指定临时数据后，Python/包版本、资源、Store 配置、打开、Schema、读写检查 ready；Provider 和客户数据 not_run，总体产品状态仍 partial。 |
| 差异及隐私 | diff --check 通过；9 个所属代码/测试文件的常见私钥和令牌模式扫描无命中。无 .env 内容读取、真实 Provider 请求或客户数据导入。模式检查不是全面安全审计。 |
| 浏览器与人 | 本批未做浏览器交互验收，未重启现有产品服务；人工验收 PENDING。 |

## 审查发现与处理

独立只读审查发现历史字段投影能够注册未选来源的列，主 Agent 已收紧为当前目录内已注册列，并用跨 Workspace、未选版本、不存在表/列验证。

集成测试发现终态时间字段不能直接 JSON 序列化，已使用类型化 JSON 投影修复。审查又发现终态预校验中的真实 Store CAS 冲突直接失败；现在只在 Run 仍运行、草案仍可编辑、精确版本确已变化、当前权限与证据只读复核均通过后，才生成无执行反馈并消耗恢复额度。它不静默改写原提议，也不泛化其他 state_conflict 或未知副作用。

竞态测试由测试夹具在校验临界点写入一份有效的新草案版本；Provider 本身不读取 Store。旧 review-2 无收据，下一轮 review-3 才执行。独立审查复核相关三项测试通过，未发现新增明确阻塞；主 Agent 已复核最终差异。

## 质量关卡与下一步

合法 partial 只证明这次执行有合法终止收据。例如新接缝中的合成答复“Needs business evidence.”可以合法 partial，但缺少单位、关系、完整字段和具体业务澄清，按 R1 完整交付要求必须判为不合格，不能因工程测试通过而升为语义 PASS。

G2 真实校准与正式 27 案例均 NOT RUN。下一步需要基于本提交准备精确材料哈希、模型身份、请求与费用上限的执行卡，并取得真实调用授权。当前 G1 授权不扩展到 Provider 调用、远端推送、部署或 R2 新对象；人此前确认的整份回答审批及未知问题继续保留责任方和待解决状态。

## 整合与回退

本批批准范围包含本地 checkpoint 与检查通过后的本地 main 合并。合并前已 fetch 确认 origin/main 仍为 `79f7e3b551d24b805b60a452c4349d74d1e248e8`，无意外远端分歧；执行完成后的本地分支/祖先关系以最终 Git 回读为准。本批不 push，不删旧分支或 worktree，不发布或部署。

无数据库迁移，但新 Run 会记录新协议指纹。新数据产生后不能保证旧二进制可打开该库；回退应使用保留两代指纹支持的修正版本，或另行批准的数据恢复方案，禁止删除新回执来强行回退。临时合成数据和服务器已按测试生命周期清理，历史真实失败证据保留。
