# R1 任务草案修复与真实验证

2026-09-07。修复代码提交 `e08b349`，分支 `codex/r1-draft-output-contract`，基线 `main@79f7e3b`。用户已批准精确提示修复、本地测试与提交，以及额外一次完整 R1；不包含推送、合并或继续扩大验证次数。

## 修复与自动验证

`src/contextox/agent.py` 的 P0_DRAFT 补齐既有四字段的类型、长度和数组数量约束。`completion_criteria` 明确为1–20个字符串的数组，`scope_notes` 为0–20个字符串的数组。Pydantic严格校验、原始输入、模型参数和重试策略不变。历史P0 hash与失败收据保留。

`tests/test_agent.py` 新增真实Store与fake Provider的回归：两个列表字段分别返回字符串或对象时，仍拒绝候选、保存失败收据、不创建Mission、不重复调用，重启后保持同一失败状态。合法数组的既有生成/确认/回放/重启测试继续通过。

验证结果：Agent 46/46、Provider 35/35；Python compile、git diff检查、新增行敏感模式检查通过。前端、HTTP合同和依赖均未改变，复用R0已验证构建；不将81项离线测试记为真实模型或人工验收通过。

## 真实验证结果

从用户明确指定的仓库 `.env` 仅加载运行凭据，不输出、不复制密钥。使用全新仓库外合成库、原批准材料与hash；两份来源分别为1006字节JSON、822字节Markdown。JSON顶层说明字段仍标为partial，客户与订单两个数组可解析；没有改文件消除解析提示。

任务草案这次成功返回四字段候选并完成确认，证明本次真实响应满足了修复后的合同。随后通过正常消息API开始第一轮资料解释，使用原定问题和两份授权来源。

第一轮在第3次模型调用中终止：`blocked:context_budget_exceeded`，安全诊断为 `stage=sse_wire used_bytes=262318 limit_bytes=262144`。尚无DefinitionDraft、ClarificationRequest、公开final_output或TerminalReceipt；第二轮用户交互未开始，没有自动重跑。

本次额外验证共有4次真实调用：1次任务草案、第一轮Run的3次调用。3份收据用量完整，已知合计输入17067、输出1441 tokens；最后1份收据输入/输出均未知，不能把已知合计当作总消费或把未知记作0。前一次失败验证的223/759 tokens不包含在本段合计内。

领域工具共6次：1次列来源、5次读来源，其中一次因定位超界被拒绝，之后有成功读取。没有任意路径读取、SQL或其他领域之外的执行。

## 浏览器与失败检查

加载期间一度显示对话读取待核对；终态刷新后恢复消息列表，并显示“本轮触及上下文预算，已停止”，诊断展示同一错误码。刷新后仍是1个attempt、1个Mission、1个Run和4份ProviderReceipt，无重复执行。此证据未证明加载期间的读取竞争已消除。

R1整体 `blocked`；两轮交互、关系/字段候选与澄清产出 `NOT RUN`。人工验收 `PENDING`，用户价值 `NOT VERIFIED`。

## 新发现与后续门槛

现有Provider把完整请求上限同时用作累计SSE网络上限。纯合成传输可复现：2000字节正文分为多个事件后，网络数据384287字节，触发同样的sse_wire上限；这不是实际Provider payload的回放，不推断真实最终正文长度。

待人批准的方向为分开请求、网络总量、单事件缓冲和聚合正文限制，保留所有有界接收、取消、超时和未知用量保护。具体提案在本任务仓库外验证工件中；本提交没有改变接收预算，也没有增加真实调用次数。

回退边界：可回退提示修复提交，不涉及schema或迁移；不得删除、改写这两次真实验证的失败/阻塞收据。仅本地提交，`main`与远端交付状态未改变。验证服务在取证后停止，合成库与证据保留。

## 追加：SSE 接收边界修复与新一次验证

用户随后明确批准接收预算修正、本地测试/提交与额外一次原定R1；本节记录新的独立尝试，不覆盖上面的失败证据。

代码提交 `59482ad`：累计SSE网络接收独立上限4 MiB；外发请求、单个事件（含尚未完成的行/UTF-8字节）和聚合content/reasoning/工具参数继续受256 KiB约束。事件计数在空行边界重置。固定4096输出token、每Run模型/工具/时间预算、取消、超时、重试0及未知用量阻断保持原值。无公开模型、API、数据库或依赖变更。

离线验证：Provider 38/38、Agent 46/46，compile与diff检查通过。新增覆盖小正文多事件成功、4 MiB累计网络超限、单事件/多行未完成事件/UTF-8残片超限、聚合正文超限、批量事件与跨块UTF-8正确解析；原请求超限、取消、超时、缺失用量和关闭响应检查继续通过。

本次真实验证从全新合成库开始，沿用原文件/hash、原三条用户输入与模型参数：

| 阶段 | 实际结果 |
| --- | --- |
| 任务草案 | 四字段候选成功生成并确认 |
| 第一条用户消息 | 7次模型调用后正常finish_run，Run=partial；公开答复、来源引用与TerminalReceipt可回读 |
| 第二条用户消息 | 仅带第一轮两条历史与原两份来源；第4次模型调用后Run=failed，错误provider_protocol_error |
| 关系/字段/澄清产出 | 0份DefinitionDraft、0份ClarificationRequest；第二轮无final_output或TerminalReceipt |
| 模型用量 | 本次12次调用（1+7+4），输入199549、输出15838 tokens；12份收据用量均已知；不包含前两次验证 |
| 刷新/历史 | 刷新后仍1个attempt、1个Mission、2个Run、12份ProviderReceipt；第一轮精确快照保持不变，完整答复仍可见，无重复调用 |

这一次没有再触发SSE接收上限；第一轮真实答复已完成。但这不证明任意响应都可完成，也不证明R1整体通过。第二轮最后一份ProviderReceipt为succeeded，只表示响应与用量已接收；随后Agent协议检查失败。该响应输出恰为4096 tokens，提示可能发生长度截断，但finish_reason未持久化，现有证据不足以确认唯一根因，不能直接把错误更名为长度超限。

运行效率与语义限制：第一轮18次工具操作含6次拒绝，第二轮8次含2次拒绝，主要是来源定位；均在各自24次上限内。第一轮没有填入时间窗口、阈值或退款默认口径，但关系基数用语“0..N”不能当作样本已证明零订单客户或生产约束；初始任务草案曾举“品类结构”示例，实际答复明确指出资料没有商品/品类证据。当前输出仍需业务审阅。

R1整体仍为failed：仅第一轮取得真实可见答复，关系/字段候选与澄清目标未达到。人工验收PENDING，用户价值NOT VERIFIED。没有额外重跑、扩大模型预算或核销此前未知用量。当前建议先补充第二轮协议失败的安全阶段诊断，区分停止原因、工具结构错误与finish_reason不一致；此后再决定候选生成与验证方案，不能仅凭4096这个数继续提高预算。

本批仅本地提交。验证进程在终态取证后停止，三次合成库与证据均保留。回退本次接收修改可回退`59482ad`，不删除历史记录、不影响已保存的来源或运行数据。R2实现合同仍待确认，不能将本次协议层修复记录为R2完成。


## 追加：协议拒绝诊断与第四次真实验证

用户批准精确协议诊断差异、相关回归与本地提交，并单独批准再进行一次原定R1。代码提交 `f798199`，只增加三个协议拒绝分支的有限枚举日志，未知停止原因统一映射为other。公开合同、状态、收据、prompt、预算及重试0未变。

Agent 47/47、Provider 38/38通过，Python compile、diff及新增行敏感模式核对通过。新增生产路径回归包含length、未知/缺失停止原因、带调用但stop、tool_calls却无调用，确认原失败状态、已知用量收据和单次调用保持不变，合成敏感标记不进入日志。本批没有前端或依赖变更，使用原R0构建进行浏览器回读。

本次真实验证使用全新合成库、原材料/hash、原输入与模型参数。任务草案成功生成并确认；第一轮在第6次模型响应后failed，错误为 `tool_arguments_invalid`。这次没有进入前批新增的三个协议拒绝分支；事件为model_completed后直接run_failed，没有该轮tool_requested。第二轮前提未满足，因此NOT RUN；无自动重跑。

本次共7次模型调用（草案1次、第一轮6次），7份收据均succeeded且用量已知；合计输入80632、输出8168 tokens，最后一份输出3262 tokens。此合计仅属于本次，不能核销此前未知用量。领域工具共7次，4次成功、3次locator_out_of_bounds拒绝；没有DefinitionDraft、ClarificationRequest、final_output或TerminalReceipt。

浏览器刷新后显示“分析失败”、tool_arguments_invalid与“本轮未形成可读取的公开答复”。刷新后仍1个attempt、1个Mission、1个Run、7份ProviderReceipt，未产生重复执行。浏览器取证后关闭本次专用页面，验证服务在无活动Run/attempt时收到SIGINT，退出码130且PID已不存在；合成库及证据保留。

本次错误可定位到工具调用规范化入口，但未保存原参数，不能继续判断重复调用ID、JSON、调用ID或具体字段校验；不得把超长reason或4096输出上限当作已确认根因，也不得反推上一次provider_protocol_error。后续工具校验脱敏诊断已形成仓库外精确提案；9类反例分别使用当前模块和内存提案共18次离线运行通过，提案尚未应用，未新增真实调用授权。

当前R1仍failed，人工验收PENDING，用户价值NOT VERIFIED；R2正式功能尚未实施。当前分支仅本地提交，main与origin/main仍79f7e3b，未推送或合并。回退诊断可撤销f798199，不涉及schema、迁移或历史记录清除。


## 追加：工具调用校验脱敏诊断（仅本地实施）

用户批准工具校验诊断差异、生产回归、相关检查和本地提交，本批不包含新增真实模型调用。新增日志覆盖未知工具、重复调用ID、JSON解析、Pydantic调用合同校验及终止工具混批。仅输出有限阶段、既有工具名或unknown、最多5条安全字段及错误类别；未知键名、调用ID值、输入、异常正文和隐藏推理不进入日志。原失败状态、权限和重试策略保持不变。

首次生产回归发现提案直接访问model_fields不兼容inspect_dataset的联合参数类型，可能让诊断本身抛AttributeError。已在本次诊断范围内修正：从既有InspectTableArguments与InspectRelationshipArguments读取字段白名单，并跳过固定的联合类型标签。补充表、关系、未知标签、额外键的生产路径反例，确认原错误继续进入受控失败流程。未修改这两个参数模型或任何公开合同。

最终检查：Agent 49/49、Provider 38/38，Python compile、git diff及新增行敏感模式核对通过。新增两个测试方法覆盖14类拒绝输入和1个合法边界输入：失败时只调用一次Provider，无工具请求或领域写入，已知用量收据保留，重启后状态一致；4096字符reason可正常partial，4097字符被原合同拒绝；多条错误日志最多保留5项，合成敏感标记未泄漏。既有成功执行、取消与协议失败回归继续通过。

与本批之前模块比对，P0_DRAFT/P0_RUN、工具定义/schema hash和历史支持hash对完全一致。无Provider实现、API、模型、数据库、预算、依赖或前端变更；没有新增真实调用、浏览器验证或部署。本批只证明诊断实现和离线反例通过，不能确认第四次真实运行的具体非法参数，也不能将R1记为通过。

原四次真实验证及未知用量记录保留。R1仍未通过，R2正式功能尚未实施，人工验收PENDING、用户价值NOT VERIFIED。当前分支仅本地提交，main与远端main仍79f7e3b；可单独回退本批提交，不涉及schema迁移或数据清除。


## 追加：完整诊断后的第五次真实验证

用户批准一次原定R1和脱敏记录的本地提交。代码c0a094b，原两份材料/hash、原三条输入、原模型配置与预算，独立合成库；R0静态构建hash与前次一致。从用户指定.env仅加载运行凭据，不输出值。没有产品代码变更或自动重跑。

任务草案成功确认。第一轮7次模型调用后finish_run，Run=partial，公开答复与4个来源定位可回读。第二轮仅携带第一轮user/assistant两条历史和原来源，第3次模型响应后failed:provider_protocol_error。安全日志明确为 `stage=finish_reason_rejected finish_reason=length turn_index=3`，该响应输出4096 tokens。本次终于能确认该响应的停止原因，不再仅凭token数猜测；推理、正文和参数的各自占比仍未知，不能反推之前几次失败。

本次共11次模型调用（1+7+3），11份收据均succeeded且用量完整，输入173118、输出15273 tokens；仅属于本次，不能核销此前未知用量。第一轮12次领域工具含3次定位拒绝，第二轮7次含1次定位拒绝；没有DefinitionDraft或ClarificationRequest，第二轮无公开final_output和TerminalReceipt。

第一轮内容保留时间窗口、阈值、退款口径等未知，也明确样本关联不等于生产保证。不过答复称Mission保持active，而第一轮终态持久Mission为blocked；应以Store事实和页面“待继续分析”为准，不采信模型对运行状态的自由表述。该措辞没有改变实际Mission状态。

第二轮失败后，第一轮精确Run快照不变，公开答复与来源按钮仍可见。刷新后仍1个attempt、1个Mission、2个Run、11份ProviderReceipt，无重复执行。取证后关闭本次页面；服务在无活动任务时SIGINT停止，退出码130，PID不存在。合成库与全部证据保留。

R1仍failed，人工验收PENDING，用户价值NOT VERIFIED。下一步提示修复提案改为现有upsert的分步生成，不增加预算：合成生产路径已核对关系、字段逐项保存及终止澄清，4次fake调用产生3个草案版本、2字段1关系1澄清，未知保留且重启一致。这只是现有合同可行性证据，提示提案未实施、真实效果未验证；精确diff和额外一次复验范围在仓库外待批。主分支和远端未改变，本批只提交本地脱敏记录。


## 追加：增量输出提示与第六次真实验证

用户确认精确增量输出提示差异、相关回归、本地提交和一次原范围R1。代码提交 `dd75e1a`：P0_RUN明确现有按key upsert会保留未提交项，要求一次保存一条关系或一至两个字段，并使用返回的version/hash继续；保留未知与必要证据，finish_run.reason遵守原4096字符限制。工具schema、状态合同、数据库、Provider配置和固定输出4096 tokens均未改变。历史P0/schema精确配对继续可读，拒绝错误交叉配对。

Agent 50/50、Provider 38/38，Python compile、diff和本次差异敏感模式核对通过。新增真实Store/fake Provider回归以4次调用产生3个不可变草案版本、2字段1关系及绑定最终版本的澄清；未知语义保留、重启一致。这证明生产保存路径支持增量更新，不证明真实模型会遵循提示。

本次使用全新仓库外合成库、原两份材料/hash、原三条输入与原模型预算，静态构建hash与R0一致。任务草案成功生成并确认。第一轮6次模型调用后正常partial，1269字符公开答复与4个来源引用可回读，时间窗口、阈值、退款口径及生产约束仍未替业务确定。第二轮只携带第一轮user/assistant两条精确历史和原资料，第4次模型响应后failed:provider_protocol_error；安全诊断为 `stage=finish_reason_rejected finish_reason=length turn_index=4`，该响应输出4096 tokens。0份DefinitionDraft、0份ClarificationRequest，第二轮无公开final_output或TerminalReceipt。增量提示不足以解决本次输出截断；没有原始响应各部分长度证据，不能断言是推理、正文或工具参数单独占满预算。

共11次模型调用（1+6+4），11份收据均succeeded且用量完整，输入151422、输出15073 tokens；只属于本次，不核销此前未知用量。第一轮12次领域工具含3次定位拒绝，第二轮8次含2次定位拒绝。ProviderReceipt成功表示接收和用量已知，不等于Agent任务成功。

第二轮失败后第一轮精确快照保持不变。浏览器刷新显示分析失败和provider_protocol_error，第一轮完整公开答复及4个来源按钮保留；仍1个attempt、1个Mission、2个Run、11份ProviderReceipt，无重复执行。未逐一点击来源按钮，不把页面读取算作人工验收。取证后关闭本次页面；无活动Run/attempt时SIGINT停止服务，退出码130，PID不存在，合成库和证据保留。

R1仍failed，R2正式功能NOT RUN，人工验收PENDING，用户价值NOT VERIFIED。当前一次复验额度已使用，没有自动第七次尝试。下一步应先离线审查输出预算与候选合同的匹配，再形成具体执行策略提案；预算、模型和外发范围的变更仍须明确决定。本批仅本地提交，main与实时远端main仍79f7e3b；未推送、合并或修改受控路线图/架构。可回退dd75e1a撤销本次提示与回归，不涉及迁移或历史证据删除。


## 追加：用户指定16,384输出上限的本地实施

用户将待批8192提案直接改为“8192*2”，本批按16,384实施；此前8192仅存在仓库外提案，从未成为产品Run预算。新建消息Run和整任务Run由Store显式保存max_output_tokens=16384；模型严格接受历史4096与新16384两值。RunBudget兼容默认值、MissionDraftAttempt、Provider未显式指定输出上限时仍为4096。既有Run重放/执行及ContextPacketManifest按持久预算核对，不批量改写历史。

实现涉及models.py、provider.py、store.py、路径二共享契约及自动生成的api.ts；新增Agent/Provider回归并补消息Run断言。默认模型、thinking enabled/high、P0提示与工具schema hash不变；每Run8次模型/24工具/5分钟、重试0、请求与接收字节预算、公开文本4096字符等限制不变。没有新增预算配置入口、依赖、数据库迁移或受控路线图/架构报告修改。

复核用户选定的16,384差异：只将此前方案的新预算值替换为16,384，不引入8192中间运行状态；严格拒绝8192、浮点、布尔、字符串及非法值；Provider容量上限同步。新输出额度可能增加消费、耗时和后续上下文，也仍可能命中原有其他上限。若后续授权一次完整R1，17次调用的请求输出额度上限将是4096+16×16384=266240 tokens；这不是实际用量、成功保证或账单金额上限。

验证：Python全套179/179、前端38/38；Python compile、Pydantic/OpenAPI生成与漂移、TypeScript typecheck、Vite build、diff及新增差异敏感模式检查通过。回归包含旧4096/新16384的Store创建、同请求重放、Agent参数、manifest预算、终态和重启一致；Provider接受16384且超限请求不外发；原任务草案4096、length拒绝、取消、未知用量及无自动重试测试继续通过。

正常本地HTTP检查使用全新仓库外合成库与注入fake Provider，验证草案生成/确认/消息发送/SSE/根资源/OpenAPI：实际传参依次4096、16384；Run=partial且公开答复可回读，同消息重放仍同一个Run，不重复调用。服务取证后停止。Doctor为partial：本地运行环境、资产和Store检查通过，真实Provider与客户数据NOT RUN。该检查不是浏览器或真实模型验收。

本批未读取.env、未新增真实模型调用；16,384对原R1案例的真实效果NOT RUN，R1仍未通过，R2正式实现NOT RUN，人工验收PENDING，用户价值NOT VERIFIED。仅本地检查点提交，main与实时远端main保持79f7e3b，没有推送/合并或服务部署。

回退边界：本次只产生可丢弃的合成验证库，没有修改用户已有业务库。含16,384记录的库不能直接交给只认识4096的旧程序；若未来需回退新建预算策略，应保留读取16,384记录的兼容模型，不改写或删除历史Run/manifest/收据。
