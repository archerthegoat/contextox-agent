# R1 任务草案修复与真实验证

2026-09-07。修复代码提交 `e08b349`，分支 `codex/r1-draft-output-contract`，基线 `main@79f7e3b`。用户已批准精确提示修复、本地测试与提交，以及额外一次完整 R1；不包含推送、合并或继续扩大验证次数。

## 修复与自动验证

`src/contextox/agent.py` 的 P0_DRAFT 补齐既有四字段的类型、长度和数组数量约束。`completion_criteria` 明确为1–20个字符串的数组，`scope_notes` 为0–20个字符串的数组。Pydantic严格校验、原始输入、模型参数和重试策略不变。历史P0 hash与失败收据保留。

`tests/test_agent.py` 新增真实Store与fake Provider的回归：两个列表字段分别返回字符串或对象时，仍拒绝候选、保存失败收据、不创建Mission、不重复调用，重启后保持同一失败状态。合法数组的既有生成/确认/回放/重启测试继续通过。

验证结果：Agent 46/46、Provider 35/35；Python compile、git diff检查、新增行敏感模式检查通过。前端、HTTP合同和依赖均未改变，复用R0已验证构建；不将81项离线测试记为真实模型或人工验收通过。

## 真实验证结果

从用户明确指定的仓库 `.env` 仅加载运行凭据，不输出、不复制密钥。使用全新仓库外合成库、原批准材料与hash；两份来源分别为1006字节JSON、822字节Markdown。JSON顶层说明字段仍标为partial，客户与订单两个数组可解析；没有改文件消除解析提示。

任务草案这次成功返回四字段候选并完成确认，证明本次真实响应满足了修复后的合同。随后通过正常消息API开始第一轮资料解释，使用原定问题和两份授权来源。

第一轮在第3次模型调用中终止：`blocked:context_budget_exceeded`，安全诊断为 `stage=sse_wire used_bytes=262318 limit_bytes=262144`。尚无DefinitionDraft、ClarificationRequest、公开final_output或TerminalReceipt；第二轮用户交互未开始，没有自动重跑。

本次额外验证共有4次真实调用：1次任务草案、第一轮Run的3次调用。3份收据用量完整，已知合计输入17067、输出1441 tokens；最后1份收据输入/输出均未知，不能把已知合计当作总消费或把未知记作0。前一次失败验证的223/759 tokens不包含在本段合计内。

领域工具共6次：1次列来源、5次读来源，其中一次因定位超界被拒绝，之后有成功读取。没有任意路径读取、SQL或其他领域之外的执行。

## 浏览器与失败检查

加载期间一度显示对话读取待核对；终态刷新后恢复消息列表，并显示“本轮触及上下文预算，已停止”，诊断展示同一错误码。刷新后仍是1个attempt、1个Mission、1个Run和4份ProviderReceipt，无重复执行。此证据未证明加载期间的读取竞争已消除。

R1整体 `blocked`；两轮交互、关系/字段候选与澄清产出 `NOT RUN`。人工验收 `PENDING`，用户价值 `NOT VERIFIED`。

## 新发现与后续门槛

现有Provider把完整请求上限同时用作累计SSE网络上限。纯合成传输可复现：2000字节正文分为多个事件后，网络数据384287字节，触发同样的sse_wire上限；这不是实际Provider payload的回放，不推断真实最终正文长度。

待人批准的方向为分开请求、网络总量、单事件缓冲和聚合正文限制，保留所有有界接收、取消、超时和未知用量保护。具体提案在本任务仓库外验证工件中；本提交没有改变接收预算，也没有增加真实调用次数。

回退边界：可回退提示修复提交，不涉及schema或迁移；不得删除、改写这两次真实验证的失败/阻塞收据。仅本地提交，`main`与远端交付状态未改变。验证服务在取证后停止，合成库与证据保留。
