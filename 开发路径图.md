# ContextOx Agent 开发路径图

> 状态：`APPROVED — 2026-09-01`
>
> 本文件是产品方向与开发顺序的人工控制文件。代码完成、测试通过或任务结束都不会自动修改本文件。任何新增、修改、移动、替换或删除，都必须先展示精确内容或 diff，并获得人的明确批准。

## 一、产品目标

ContextOx Agent 面向 FDE 和工程师，把散落的公司业务知识、任务证据与人的决策组织成一个可追溯、可恢复、可验收的单任务闭环。

当前进入路径是个人开发者和一线工程师可直接使用的本地 CLI 产品；它不是通用聊天助手，也不是先做企业管理后台。首个可验证任务是：基于一个公司的授权资料，产出带来源、未知项、例外和反例的业务定义 Contract，并在人工审批后继续完成任务。

## 二、已确认的产品与技术基础

1. 新仓库名为 `contextox-agent`，使用 TypeScript。
2. Agent Runtime 使用发布版 `@earendil-works/pi-agent-core`；ContextOx 不 fork、vendor、复制或修改 Pi 源码，也不依赖 Pi Coding Agent/TUI。
3. Pi 只负责模型—工具循环、事件、取消和运行期状态；Mission、Workspace Context、审批、权限、证据、持久化与完成语义归 ContextOx 所有。
4. 一个 Agent Run 是临时执行；一个 Mission 是可跨进程恢复的持久任务。`agent_end` 不等于 Mission 完成。
5. V0 不做跨任务的个人对话记忆、偏好记忆或人物长期记忆。
6. 公司业务知识作为跨任务的 `Workspace Context` 持久存在。它必须有来源、版本、时间、权限状态与人工批准，不能由聊天内容自动晋升为事实。
7. 本地单用户 V0 仍支持多个 Workspace；所有 Context、Mission、Run、Approval、Artifact 和检索都必须绑定 `workspace_id`。
8. V0 使用本地 SQLite；检索先做 Workspace/权限/版本过滤，再做 FTS5 关键词检索。暂不引入向量数据库或知识图谱。
9. V0 只有 CLI。Web、SSO、多用户、云同步、MCP 服务与分布式工作流均不在当前范围。
10. 项目初期整体开源，许可证为 MIT；客户资料、运行数据库、凭证、原始日志和评测私有数据不进入 Git。
11. 只有当某项能力在真实用户任务中形成可重复、可衡量、难复制且可独立隔离的优势时，才讨论未来私有实现。已经发布的开源版本不收回授权。

## 三、V0 完成标准

一个新用户能够在本地完成以下闭环：

1. 创建两个相互隔离的公司 Workspace。
2. 向其中一个 Workspace 显式导入受支持的本地文本资料。
3. 由 Agent 起草带引用的 Company Brief，并由本地用户批准为一个不可变的 Workspace Context 版本。
4. 在该 Workspace 中创建 Mission，并把它绑定到一个不可变 Context Snapshot。
5. 启动一个新的 Pi Agent Run；它只能看到当前 Snapshot 和当前 Mission 获准的工具。
6. Agent 产出完整的 Contract 提案，包含规则、来源、未知项、例外、正例、反例和影响。
7. 用户针对提案版本与哈希作出批准、要求修改或拒绝的显式决定。
8. 批准或要求修改会原子地生成可重试的恢复作业；恢复时创建新的 Agent Run，不复用旧 Pi 会话。
9. 只有恢复后的 Run 产出规定的成功结果与证据，Mission 才能进入 `completed`。
10. 进程在等待审批、恢复派发或工具执行附近退出后，重启可以读出真实状态，并安全地继续、阻塞或要求核对。
11. 任意跨 Workspace 检索或引用尝试都被拒绝，并留下不含敏感正文的审计记录。
12. 自动测试、真实模型试跑和人工产品验收分别记录；任何一项未运行都不得写成通过。
13. 除账户级 `doctor`、`workspace create`、`workspace list` 外，所有读取或改变既有对象的 CLI 命令都显式接收 `--workspace`，并在 domain/store 层验证对象与 Workspace 的复合关系。

## 四、V0 明确不做

- Pi 仓库 fork、Pi 源码复制、`patch-package`、Git/本地 workspace 依赖。
- Pi Coding Agent、Pi TUI 或旧仓库的 `.pi` 扩展体系。
- 通用个人助手、跨任务聊天历史、自动偏好学习、自动事实沉淀。
- 向量检索、知识图谱、RAG 平台、独立 Memory 服务。
- MCP Server；Context Store 先是进程内 TypeScript 模块。只有出现第二个真实 Agent Host 后才评估 MCP 暴露。
- Web UI、浏览器验收、SSO、RBAC、多用户协作、云托管、远程同步。
- Temporal、DBOS、消息队列、微服务或自研工作流引擎。
- 自动执行外部写操作，或在副作用结果不明时盲目重试。
- 把旧 PRD、旧架构报告或静态 fixture 的历史状态重写成当前产品事实。

## 五、开发路径

### 路径 0：建立干净且可审计的仓库基线

目标：让新仓库先表达自己的产品边界，而不是继承 Pi monorepo 的形状。

交付：

- MIT `LICENSE`、简洁 `README.md`、`.gitignore`、项目级 `AGENTS.md`。
- 经人批准的本文件与 `docs/架构与迁移报告.md`。
- 旧仓库材料迁移清单，保留来源提交、原状态与未验证项。
- 拟公开复制的每个 legacy/fixture 文件先进入 publication manifest，逐项记录固定来源提交/路径/hash、目标路径、rights owner、版权/许可证证据、第三方内容、secret/PII/客户私有数据/evaluator-only 分类、人决定与目标 hash；只有 `allow` 才复制，其余留在仓库外。
- 不创建远端、不发布包，直到获得独立授权。

通过门：精确文件内容获人批准；逐文件公开 allowlist 可回读；新仓库仍无产品代码和依赖安装副作用。

### 路径 1：TypeScript 单包骨架

目标：用最少依赖建立可运行、可检查、可发布的基础。

交付：

- Node `>=22.19.0`、npm、ESM、严格且仅使用可擦除语法的 TypeScript。
- 开发期由 Node 直接运行 `.ts`；发布前由 `tsc` 生成 `dist/*.js`。
- 运行依赖仅为精确锁定且同版本的 Pi Agent Core 与 Pi AI；其余能力优先使用 Node 标准库。
- `node:test`、`node:assert`、临时目录和内存/临时 SQLite 测试。
- `package-lock.json` 只在依赖审查与安装获批后生成。
- `scripts/test.mjs` 只用 Node 标准库枚举显式 `test/**/*.test.ts` 范围、运行 TAP reporter，并在匹配文件或实际 test count 为零时退出非零；必须在最低 Node 22.19 环境实际运行。

通过门：类型检查、最小构建和最小测试通过；从仓库外安装构建产物的 smoke test 能启动 CLI。

### 路径 2：持久 Mission 与审批状态机

目标：先把不依赖模型的正确性做实。

交付：

- Workspace、Mission、AgentRun、ApprovalRequest、Decision、ContractVersion、ResumeJob 与 AuditEvent 的最小 SQLite schema。
- 提案规范化与 SHA-256 身份；审批绑定 `workspace_id`、Mission、版本、哈希与固定本地 actor。
- Mission 状态：`active | waiting_for_human | blocked | completed | cancelled`。
- Mission 的状态版本和条件更新决定终态竞争；`finish_mission`、审批请求与 cancel 只有一个能在匹配的旧状态/版本上提交。cancel 赢时同事务写 Mission/Run/ResumeJob/Approval 的取消状态与 Run terminal receipt，再尽力发送 AbortSignal。
- AgentRun 显式记录 queued/running/terminal、heartbeat 与 terminal receipt；崩溃恢复不能把无 terminal receipt 的 Run 当成成功。
- 恢复采用至少一次投递：claim、lease generation、attempt token、稳定幂等键、接收方去重与失败原文；任何 worker heartbeat、receipt 或 Mission 转换都拒绝过期 token。lease reaper 只能在受控事务中先凭已过期旧 fence 封存旧 Run，再递增 generation；旧 worker 不能越过新 attempt 写状态。
- `partial` 是 AgentRun/CLI 结果，不是 Mission 成功状态；它把 Mission 置为 `blocked` 并保留缺口。对应 ResumeJob 的 `succeeded` 只表示派发得到了该 terminal receipt，不表示 Mission 成功；V0 只能显式 retry/reopen，不提供未建模的 stale 例外捷径。
- 批准不完成 Mission；完成只来自显式成功转换。

通过门：错误 actor、旧版本、旧哈希、重复决定、cancel terminal receipt、终态竞态、fenced reaper、lease/heartbeat 崩溃恢复、旧 worker 在新 generation 后恢复写入、重复派发、partial 映射和未知副作用测试均按关闭失败策略通过。

### 路径 3：Pi Agent Core 薄适配

目标：只复用 Pi 的运行能力，不让 Pi 类型与会话语义扩散到产品域。

交付：

- 一个 `PiRuntime` 适配模块，把 ContextOx 的 `RunRequest` 转为新建的 Pi `Agent`。
- 每个 Run 一个新 Agent；系统提示、模型、工具、预算、截止时间和 `AbortSignal` 由 ContextOx 注入。
- 工具采用显式 allowlist；审批、完成与外部副作用工具强制串行。
- Pi 事件归一化为有界 Run/Tool receipts；原始敏感正文默认不写日志。
- 缺少显式终止结果、运行错误、超时或取消都不能把 Mission 标为完成。

通过门：使用 fake stream 的确定性测试覆盖事件顺序、工具阻断、取消、预算、无终止结果和新 Run 恢复；不调用真实模型。

### 路径 4：受治理的 Workspace Context

目标：让 Agent 理解公司业务，但不把“记得聊天”误当成公司知识治理。

交付：

- V0 仅支持用户显式导入的 UTF-8 Markdown、纯文本和 JSON 文件；每次导入生成不可变 SourceRevision 与内容哈希。
- Agent 基于指定 SourceRevision 起草 Company Brief；人批准后生成不可变 WorkspaceContextVersion。
- Mission 创建时冻结 ContextSnapshot；已运行 Mission 不随 Workspace Context 自动漂移。
- 检索顺序固定为 Workspace、Snapshot、权限与状态过滤，然后 FTS5；返回来源 revision、chunk、hash 和时间。
- 来源状态区分 `draft | verified | stale | conflict | unknown | revoked`。未授权、冲突、缺失或未知权限关闭失败；过期证据不能支撑 `verified` 结论。V0 必须更新来源并 retry/reopen，不提供 freshness 例外审批。
- Mission 新发现只形成 `KnowledgeCandidate`；只有人工批准才生成新的 WorkspaceContextVersion。

通过门：双 Workspace 泄漏测试、撤销/冲突/过期/缺失来源测试、引用完整性测试与 FTS5 可用性自检通过。

### 路径 5：首个 FDE 单任务闭环

目标：交付一个真实可用而不是通用框架的任务模板。

交付：

- “从公司资料定义高潜客户”任务：读取授权 Context、聚合事实、提出 Contract、等待审批、恢复并产出最终 Contract 与证据包。
- CLI 能创建/查看 Workspace，导入/查看 Context，创建/查看/运行 Mission，查看/决定 Approval，重试/核对 ResumeJob；除账户级 `doctor`、`workspace create/list` 外，既有对象命令全部显式带 `--workspace` 并验证复合关系。
- 文本和 `--json` 输出具有稳定字段、明确状态与退出码；状态不依赖颜色。
- 沿用旧验证包的事实语义与隔离边界，但不沿用旧运行实现和错误的验收结论。

通过门：确定性 fixture 只证明工程控制；随后分别记录一次真实模型试跑和人的端到端验收。两者在运行前保持 `NOT_RUN` / `PENDING`。

### 路径 6：开源发布与真实用户验证

目标：用实际 FDE/工程师任务验证价值，而不是先扩展架构。

交付：

- 公开安装、数据边界、威胁模型、限制与贡献说明。
- 可复现的确定性 eval；真实模型 eval 与成本/延迟记录独立保存。
- 至少若干真实用户任务的失败原因、人工介入点、节省步骤与复用率记录；私有业务内容不进入公共仓库。
- Pi 升级走独立分支和兼容性 fixture，不跟随 upstream 自动升级。

通过门：自动检查、真实运行证据和人工接受分别可追溯；发布范围与秘密扫描通过人审。

### 路径 7：Edge 决策

目标：只在证据出现后决定是否形成私有产品边界。

候选但未承诺的 Edge：

- Company Brief 编译质量与业务上下文冲突/缺口/时效处理。
- Knowledge Candidate 的治理、评测数据与晋升阈值。
- FDE 高频任务模板、连接器和真实部署反馈闭环。
- 可量化降低返工、澄清轮次或错误定义发布的评测与运营方法。

进入私有边界的必要条件：真实用户重复使用；效果可测；优势主要来自独有数据、评测或运营闭环；边界可从公共内核中清楚隔离。否则继续开源。

## 六、实现与 Git 原则

1. `main` 是稳定默认分支；文档小改可在精确内容获批后直接进入 `main`，代码使用能提高审查/回滚安全性的 `codex/` 功能分支。
2. 每个 checkpoint 只表达一个可独立验证、审查和回滚的意图；通过后只提交本 checkpoint 的文件。
3. 不自动创建远端、推送、发布 npm、发版或部署；这些动作分别需要明确授权。
4. 依赖变更按代码审查：精确版本、许可证、发布内容、生命周期脚本和 lockfile diff 都要核对。
5. 自动测试通过不代表真实模型通过；真实模型通过不代表人工产品验收通过；人工验收只有人在指定版本上记录 `PASS` 才成立。
6. 每条开发路径结束时检查本文件是否仍与人的方向一致；如需修改，只在对话中提出精确 diff，不能自动同步状态。

## 七、后续方向（不是当前承诺）

- 更多本地文件格式、Git/文档系统/工单系统连接器。
- 经过权限与新鲜度设计后的远程 Workspace Context。
- 第二个真实 Agent Host 出现后，将 Context 接口暴露为 MCP。
- 有真实团队需求后再做认证、多用户审批与组织权限。
- CLI 闭环稳定且浏览器确有价值后，再设计 Web UI 与完整浏览器验收门。
- 有质量证据后再评估向量召回、重排或图结构；它们不能替代来源与审批。

## 八、批准记录与持续执行边界

2026-09-01，人已分别批准：

1. 本文件的原始精确全文；
2. `docs/架构与迁移报告.md` 的原始精确全文；
3. 架构报告第 16 节的初始 TypeScript 文件树、候选依赖/config 与测试范围；
4. 只把上述两份精确文档写入新仓库；
5. 只在仓库外起草其余路径 0 文件与逐文件 publication manifest，供再次精确审批。

两份批准文档已经写入新仓库，并与批准草案逐字节核对。该执行没有创建提交、remote、产品代码、依赖或 lockfile。

上述批准不自动授权：

- 把 README、LICENSE、AGENTS、publication manifest、legacy/fixture 或任何代码写入仓库；
- 把任何 legacy/fixture 候选标为 `allow` 或公开复制；
- 安装依赖、创建实现分支、提交、创建 remote、push、运行真实模型、发布或部署；
- 自动修改本文件或架构报告以同步后续实现状态。

每一项后续动作仍需按其精确内容、范围与副作用单独批准。产品实现、自动验证、真实模型运行与人工验收继续分别保持 `NOT STARTED` / `NOT RUN` / `PENDING`，直到对应证据与人工 Gate 完成。
