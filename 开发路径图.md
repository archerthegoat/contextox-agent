# ContextOx Workbench 开发路径图

> 状态：`APPROVED — 2026-09-02`
>
> 本文件是产品方向与开发顺序的人工控制文件。代码完成、测试通过或任务结束都不会自动修改本文件。任何新增、修改、移动、替换或删除，都必须先展示精确内容或 diff，并获得人的明确批准。

## 一、产品目标

数契（ContextOx Workbench）是一个在个人电脑上部署和使用、帮助用户借助 Agent 理解和规范业务与数据知识的工作台。它围绕明确授权的文档、样例数据、数据库结构和人的经验，帮助用户梳理概念、字段、指标与规则，发现信息缺口，并将经过确认的定义沉淀为有来源、可追溯、可恢复、可验收、可复用的业务知识。

近期以“业务定义交付”为产品切口：帮助有具体业务与数据整理任务的用户，把含糊或冲突的字段、实体与指标需求，整理成有证据、经人确认、可交给业务和数据团队验收的业务定义包。先证明一个完整任务及第二个任务的批准知识复用；减少遗漏、人工整理与返工是待验证目标，不作为已实现的收益声明。

第一阶段不尝试接入客户完整办公环境，也不把 Agent 做成通用 Coding Agent。用户在本地 Workbench 中为客户、自有业务或明确标记的练习案例创建隔离的 Workspace、批量导入资料、观察 Agent 的证据与工具调用；当业务意图或数据语义不能从证据中确定时，Agent 生成结构化澄清表单，由使用者转交给具备对应业务或数据裁决权的负责人。获批回答被编译为字段、实体、指标、映射、规则、例外与验收条件，并进入可复用的 Workspace Context。

远期目标是让 Agent 持续推动企业业务定义、数据库整理与人机交接，但每一步都必须保留来源、权限、版本、责任人与人工裁决，不能把模型推断伪装成企业事实。

### 目标用户与共同任务

- 实际项目交付者：包括 FDE、Agent 开发者、实施顾问、数据工程师和数据分析师，围绕具体项目理解业务与数据、澄清缺口并交付定义。
- 现有业务整理者：包括业务负责人、产品经理、运营人员、创业者，以及维护内部数据库或业务系统的人，梳理表结构背后的业务含义、指标口径、流程规则和经验，供人和后续 Agent 使用。
- 学习、转型和求职者：包括希望进入 FDE 或 Agent 交付岗位的开发者、数据从业者、学生及其他学习者，通过公开或合成案例练习业务理解、数据梳理、澄清和定义交付，并能够解释判断依据与取舍。

三类用户共用“带入资料—理解业务与数据—发现缺口和冲突—澄清并确认—形成定义—后续任务复用”的产品主线。FDE 保留为首个实际交付试点场景。公开练习、分步提示和反例反馈是学习入口的候选，具体功能另行对齐；实际任务价值和学习价值分别验证。练习材料及模拟裁决必须明确标记，不得冒充真实企业事实或业务负责人的批准。

近期“规范数据库”指梳理数据库背后的业务含义、关系与定义，产出整理建议和可确认的规范；实时数据库连接、生产写入和数据修复仍受第四节及独立能力决策约束。

### 与通用 Agent 的差异化目标

Codex、Claude Code 配合合适的 Skills、项目上下文、模板和工具，是正式比较基线。数契需要证明：将业务定义任务所需的对象、证据、澄清、确认、版本和复用整合为可直接使用的产品，能减少用户自行组织和维护流程的工作，并改善任务结果。专业提示词、资料读取、项目记忆、工具接入和聊天界面本身不构成已验证的独占优势。

- 业务对象与证据：围绕实体、字段、指标、规则和关系工作，能够从定义定位来源，并清楚区分事实、候选、冲突和未知。
- 定义缺口与澄清：系统检查粒度、身份、时间、口径和例外，把缺口组织成具体、可回答、可交给适当负责人的问题，展示回答对定义的影响。
- 确认、版本与复用：将来源、人的决定、版本、适用范围和变更记录保存在明确的产品状态中；未经确认或已失效内容不得静默作为获批事实复用。领域状态校验由程序执行，不能仅依赖模型遵循提示词。
- 可交接的成果：形成包含定义、证据、例外、未知和验收条件的业务定义包，供业务与数据人员核对，也可作为 Codex、Claude Code 等后续执行工具的输入。当前不据此承诺新增自动集成。

上述内容是产品目标，具体实现与验收以对应路径为准，不作为已实现优势。路径 5 沿用相同材料、任务和明确预算的比较，并记录配置与资料准备、有效澄清、关键遗漏、人工修改、主动操作时间、等待和后续复用；学习价值另看用户能否解释判断，并在新案例中识别问题和独立完成关键步骤。只有单次回答更长或界面更丰富，不足以证明差异化；若合理配置的通用 Agent 在结果与总投入上相当，应保留该结果并重新评估独立产品的增量价值。

## 二、已确认的产品与技术基础

1. 产品名为 `ContextOx Workbench`，中文名为“数契”；目标用户包括实际项目交付者、现有业务整理者及学习、转型和求职者，FDE 为首个实际交付试点场景。
2. V0 是本地单用户 Web Workbench，CLI 只保留启动、doctor 和必要的自动化入口，不是主要产品界面。
3. 后端使用 Python；UV 管理 Python 版本、虚拟环境、依赖、lockfile 和项目命令。
4. 前端使用 React + TypeScript；React 组件统一写为 TypeScript/TSX，不混用手写 JavaScript 业务代码。
5. Pydantic 模型是前后端 API 与事件字段的唯一权威；通过 OpenAPI/JSON Schema 生成 TypeScript 类型和客户端，生成文件不手工编辑。
6. 本地 Python 主服务进程绑定 `127.0.0.1`，提供 HTTP JSON、SSE 和构建后的 Workbench 静态文件；V0 不拆成多个部署服务。单次 Provider 网络调用可以使用一个由主服务监督、无持久状态的短生命周期子进程；该子进程不是独立服务。
7. Agent Runtime 是 ContextOx 自有的薄 Python Loop。主服务负责模型—工具循环、串行工具执行、事件、预算、取消、本次 Run 的临时消息和 Provider 子进程生命周期；子进程只执行一次受限 Provider I/O，不持有 Store、领域工具或业务状态。ContextOx 不复制 Waku 代码，也不依赖 Pi Agent Core 或 Pi Coding Agent。
8. ContextOx 负责 Workspace Context、Mission、Clarification、Approval、Contract、权限、证据、持久化、审计和完成语义。模型停止输出不等于 Mission 完成。
9. Agent 不获得通用 `read/write/edit/bash`、任意文件路径、任意 SQL 或代码执行能力。它只能使用当前 Workspace、当前 Mission 显式允许的领域工具。
10. 一个 Agent Run 是临时执行；需要人回答时当前 Run 结束。回答持久化并获准后，以新的 Run 继续，不恢复旧模型会话。
11. 公司知识作为跨任务的 `Workspace Context` 持久存在。它必须有来源、版本、时间、权限状态和人的批准；聊天、未批准表单回答或 Agent 推断不能自动晋升为事实。
12. V0 支持多个 Workspace；所有 Source、Context、Mission、Run、Clarification、Approval、Contract、Artifact 和检索都必须绑定 `workspace_id`。
13. SQLite 保存产品状态与审计。是否加入 DuckDB 只由真实批量表格或 profiling 性能证据决定，不预先引入数据平台。
14. 项目初期整体开源，许可证为 MIT；客户资料、运行数据库、凭证、原始日志和私有评测数据不进入 Git。

## 三、V0 产品闭环与完成标准

一个本地使用者能够在来源已获授权的实际业务或明确标记的练习场景中，通过浏览器完成以下闭环：

1. 启动 ContextOx，并通过 doctor 明确看到 Python、数据库、前端构建和模型配置状态。
2. 创建两个相互隔离的 Workspace，用于客户、自有业务或明确标记的练习案例。
3. 通过拖拽或文件选择批量导入授权资料，或显式绑定一个只读本地文件夹/Git 仓库根；看到逐文件类型、大小、hash、解析状态、失败原因和来源版本。绑定只授权该规范化根路径，不能扩展到父目录、符号链接目标或其他 Workspace。
4. V0 至少区分文档、结构化数据和 SQL/DDL；支持格式与限制在实现 checkpoint 前根据公开样例精确冻结。
5. 对结构化数据展示表、字段、类型、样例、缺失、唯一性和基础异常；对文档展示可引用的提取片段。任何解析失败保持 `partial` 或 `blocked`，不伪造成功。
6. 创建一个业务定义 Mission；它绑定不可变的来源快照和任务目标。
7. Workbench 以业务定义任务为主线：左侧导航资料与任务，中间查看资料、关系、字段草案与待决问题，右侧与当前任务的 Agent 连续对话。切换任务内的资料或结果不另建对话；引用对象与资料使用范围明确可见。Agent 默认展示解释、证据、澄清、结果入口和简明进度，详细模型/工具事件收进任务执行历史；隐藏模型私有推理，不伪造“思维链”。
8. Agent 只能读取授权来源、检查结构化数据、搜索已批准 Context、创建澄清、更新结构化定义草案和提交审核。
9. 遇到证据不足或业务选择时，Agent 创建澄清表单，至少说明问题、为什么要问、期望回答结构、建议回答角色、相关证据、示例/选项、阻塞影响和关联定义。
10. 使用者可以在 Workbench 填写，也可以复制或导出澄清包交给对应人员；V0 不依赖对方安装 ContextOx，也不要求飞书集成。
11. 回答先成为带来源与回答者声明的候选，不自动成为事实。本地使用者在既定 `local-owner` 信任边界内，针对精确版本和哈希批准、要求修改或拒绝；职位名称不授予业务裁决权，练习中的确认不代表真实企业批准。
12. 获批回答被编译为新的 ContractVersion 和可复用的 Workspace Context 候选；只有再次批准的 Context 版本可供后续 Mission 使用。
13. 最终 Contract 至少包含业务目的、实体、粒度、身份、时间语义、来源、字段映射、计算规则、未知、例外、正反例、责任人、验收条件和变更记录。
14. 只有显式完成转换引用获批 Contract 和所需证据时，Mission 才能进入 `completed`；`partial`、`blocked`、`failed`、`cancelled` 与 `PENDING` 保持区分。
15. 同一公开样例由 ContextOx 与 Codex/Claude Code 在相同材料、目标和时间边界下分别完成；结果记录遗漏、错误、澄清质量、人工修改量、总耗时和批准知识复用，不能只比较文案观感。
16. 自动测试、真实模型试跑、浏览器人工验收和用户价值验证分别记录；任何一项未运行都不得写成通过。

## 四、V0 明确不做

- 通用个人助手、通用 Coding Agent、跨任务人物记忆或自动偏好学习。
- 通用 `read/write/edit/bash`，任意路径读取，模型直接执行 Python、Shell、代码或任意 SQL。
- 生产数据库写入、自动修改客户仓库或在外部副作用未知时盲目重试。
- 飞书、钉钉、企业微信、Slack、Notion 等办公系统集成；飞书是本地闭环证明后的第二阶段候选。
- 实时生产数据库连接器；V0 先用授权文件、导出数据、Schema/DDL 和受控公开样例证明闭环。
- SSO、RBAC、多用户协作、云托管、远程同步或分布式工作流。
- 向量数据库、知识图谱、独立 Memory 服务、MCP Server、队列或自研工作流引擎。
- Streamlit、Gradio 或 Python 桌面 UI 作为长期 Workbench 主界面。
- 把 Agent 的隐藏推理过程作为产品输出；可视化只展示可审计的阶段、工具、输入摘要、结果、证据和状态。

## 五、开发路径

### 路径 0：冻结新架构并保留历史证据

目标：在改动实现前，明确新方向如何取代旧 TypeScript CLI + Pi Core 决策，同时不抹掉 C1 历史。

交付：

- 经人精确批准的本文件、`README.md` 和 `docs/架构与迁移报告.md`。
- 旧 C1 提交、检查和人工验收继续作为历史证据，但明确不覆盖新架构。
- Python/前端迁移清单：保留、替换、删除和新增路径逐项列明；删除旧代码前必须有可回滚提交和新 scaffold 验证结果。
- 旧仓库 legacy/fixture 仍受 publication manifest 逐文件 `allow` 门约束。

通过门：三份文档无 CLI/Pi/TypeScript 后端残余矛盾；精确文本获人批准；仓库产品实现仍未被误报为新架构完成。

### 路径 1：Python 后端与 React/TypeScript Workbench 骨架

目标：用最少依赖建立一个可启动、可检查、可生成接口类型的本地单进程产品外壳。

交付：

- 根 `pyproject.toml`、`.python-version`、`uv.lock` 和 Python package；精确 Python 版本与依赖在安装前审查。
- 最小本地 API、SSE、静态文件服务和 `contextox doctor/start` 入口。
- `web/` 下的 React + TypeScript + Vite 前端；不加入 Next.js、SSR、Redux 或 UI 大框架。
- Pydantic → OpenAPI/JSON Schema → TypeScript 类型/客户端生成与 drift check。
- SQLite 自检、临时数据目录和最小后端/前端测试。
- 在新 scaffold 验证前不删除旧 C1；验证后以独立、可回滚 checkpoint 替换旧运行入口。

通过门：UV clean sync、Python 检查与测试、前端 typecheck/test/build、Schema 生成无漂移、仓库外启动 smoke test 通过；浏览器只能证明骨架可打开，不代表产品验收。

### 路径 2：首个可用业务定义 Agent——资料理解、任务对话与关系/字段草案

目标：用户在 Workbench 中导入明确授权的样例，围绕同一个业务定义任务与 Agent 提问和追问，获得有证据的资料解释、关系说明、字段定义草案和待确认问题。Agent 帮助区分技术事实、关系候选与尚待人裁决的业务语义。

交付：

- 复核并整合已有 Workspace 实现，保留已认可的资料预览与字段检查体验；调整任务页、Agent 对话与执行历史的分工，避免以运行控制表单代替业务定义工作页。
- 实现本案例所需的最小来源准入、不可变 SourceRevision、Schema、样例和基础 profiling；首批格式与限额在实施前明确。
- 按已批准架构实现任务草案、人工确认、正式 Mission、来源快照和显式启动 Run。
- 实现单 Provider 薄 Agent Loop、必要的领域工具、预算、取消、公开事件、持久状态与终止收据。
- 持久化当前任务中明确发送的用户消息、受控公开答复及结果引用，使刷新后可回读并支持后续追问；产品对话与单次 Run 的临时模型消息分开，不保存原始 Provider transcript 或隐藏推理。消息/执行的关联、幂等、有限上下文和权限合同在实现前精确评审。
- 在 Workbench 联动资料、实际关系图、字段草案和任务对话；用户可显式引用字段或关系提问，并从答复定位证据。无草案时显示明确空状态；关系与业务语义分别标明已知、候选、冲突和未知。
- 任务对话连续，执行仍有独立预算和终态；运行中可编辑未发送草稿，不热注入或自动排队。每次明确发送按获准范围发起新一轮分析；请求结果未知时先核对，不重复执行。详细运行事件保留在执行历史，窄窗口仍须能够查看结果与使用 Agent。
- 共享接口确定后，Agent 运行、来源证据和 Workbench 交互可以并行开发。

推进顺序：保留既有来源、状态、权限与 Workbench 基础，将 R1 按“G0 模型可执行工具合同、证据覆盖与评测冻结；G1 证据工作集、候选提交、风险分类恢复、联合预算与观测的成套工程实现；G2 固定版本的真实模型语义与重复案例验收”推进。不以逐错增加提示词或单独扩大预算替代系统验证。具体检查点、分母、阈值及停止规则见 `docs/R1系统重规划与验收指标.md`；精确接口、兼容性、迁移和调用封顶仍须按重大决策独立评审。

通过门：在获准输入与调用预算内，真实模型在同一任务中完成至少两轮用户可见交互，并产出可追溯至实际来源的关系说明、字段定义草案，以及存在未决问题时的具体澄清；资料、答复和结果引用可相互定位，刷新不丢已发送对话、不重复启动，跨 Workspace 保持隔离。fake model 测试是回归检查，不能代替该里程碑；仅有输入框、流式文字或工具日志不算任务对话通过。存在未决业务问题时保持待确认或 partial，不声明 Mission 完成。

补充验收要求：关键事实覆盖、关键主张的证据支持、关键未知识别、候选可用性及具体澄清分别评分；安全违规为零。原始案例与未用于调优的保留案例按冻结版本重复验证。合法 partial、自动测试、构建及浏览器可用不能替代真实语义质量；数字阈值与真实试验材料、总预算按上述验收稿另行冻结。

### 路径 3：跑通高潜客户定义的完整闭环

目标：把关系与字段草案推进为经人确认、可交给业务或数据团队使用的业务契约。

依赖安排：G0 可同步对齐 R2 整份回答批准合同；G1 通过且 R2 精确接口与迁移已独立获批后，可开展其局部 Store/API/Workbench 实现。G3 完整真实任务验收必须等待 G2 通过，不能因局部工程并行宣布路径 2 完成。整份回答批准、批准未知记录但问题仍未解决、卡点解决方，以及原请求新版本重新批准和历史 Run 保持旧版本等既定裁决继续有效。

交付：

- 沿用已选样例，明确客户身份、粒度、时间窗口、来源映射、计算规则、例外和未知。
- 在同一任务对话中至少完成一次结构化澄清、回答候选、精确版本/hash 批准和新 Run 继续，并能查看获批回答对关系或字段草案造成的具体变化；自然语言答复不自动成为业务批准。
- 形成可查看、比较和导出的 ContractVersion，包含字段定义、证据、正反例、责任人与验收条件。
- 只有满足已批准完成条件、收据和证据要求时，才显式完成 Mission。

通过门：人在精确构建上走通“导入—分析—澄清—确认—契约”流程；不得硬编码样例答案，也不得把数据中不存在的企业或门店关系当作事实。

### 路径 4：批准知识复用，来源扩展按实际阻塞推进

目标：先让第二个任务能够复用第一次已批准的定义；更多来源按案例或试点中可观察的阻塞分别推进。

交付：

- 已批准 Workspace Context 的发布、检索、版本失效与 MEMORY.md 投影。
- 在新 Mission 中复用仍有效的来源和批准定义，避免重复询问已确认事项。
- 先以显式选择和确定性检索验证复用；未批准、失效、撤销或冲突内容不得静默进入后续任务。
- XLSX、SQL/DDL、批量处理及显式只读文件夹/Git 根绑定保留为来源扩展候选；只有已选案例无法由现有格式表达，或试点反复受阻时，按一次一项的 checkpoint 提出。
- 每次来源扩展分别确认格式、大小、解析质量、权限、freshness 和独立验收边界，不与批准知识复用捆绑通过。

通过门：第二个任务实际复用已批准内容；变化、撤销和冲突能被识别；历史证据保留。该通过门不依赖新增来源格式；获准的格式扩展另有独立验证。

### 路径 5：稳定演示与提效验证

目标：在可用闭环上检验它是否减少遗漏、人工整理和沟通往返。

交付：

- 一套可复现、可解释且明确标记数据来源的演示。
- 从路径 2 的首个真实 Agent 案例开始记录材料、目标、人工操作和运行过程；路径 3 闭环可用后，在相同材料、任务与预算下，与 Codex/Claude Code 配合良好模板和人工文档维护的流程比较。
- 分开记录模型与工具差异、关键遗漏、错误事实、有效澄清、人工修改量、资料准备、主动操作时间、等待时间和复用情况；未控制的差异不得归因为单一产品能力。
- 公共或合成案例用于回归，获准的真实案例用于判断实际收益；保留失败、放弃与未产生收益的结果，不混合汇总为已验证的客户收益。

通过门：功能完成、真实模型表现、人工使用判断和提效证据分别记录。比较评测不作为路径 2 开发的前置条件；是否扩展更多集成仍由后续使用证据决定。

### 路径 6：真实 FDE 试点

目标：让若干 FDE 使用自己的非敏感或已授权客户场景，验证工作流与表单是否真的减少沟通成本。

交付：

- 可以在路径 3 开发期间提前确认参与 FDE、具体任务和脱敏材料；完整使用与收益验收仍以实际可用闭环为前提。
- 明确授权、允许发送给 Provider 的内容、保存期限和删除方式的试点说明；本地保存状态不代表模型推理离线。
- 记录资料准备时间、澄清轮次、角色响应、定义修改、返工和复用，不把客户私有正文放入公共仓库。
- 分别验证重复使用、实际收益及后续试点的预算意愿，不把重复使用当成付费证明；失败、放弃和“不比通用 Agent 好”的案例同样保留。

通过门：至少出现重复使用与可量化收益，才进入办公系统或实时数据库集成；否则继续改闭环。

### 路径 7：飞书集成 Decision Gate

目标：只有本地闭环证明后，才决定如何接入国内客户常用的飞书资料与表单。

候选范围：飞书文档/多维表格的只读导入、澄清表单发送/回收、身份与授权映射。任何写入、机器人消息、回调、云端凭证保存和多用户审批都需要新的威胁模型、权限设计、失败矩阵与人工批准。

通过门：真实试点证明飞书是重复瓶颈；精确 API、权限、数据驻留、回滚和验收方案获批。当前不承诺实现。

## 六、实现与 Git 原则

1. `main` 是稳定默认分支；经批准的本轮三份文档可直接进入 `main`，代码与依赖迁移使用短期 `codex/` 分支。
2. 不重写或删除 C1 Git 历史。旧 TypeScript 运行文件只在新 scaffold 有可回滚验证后，以明确迁移提交删除或移动。
3. 每个 checkpoint 只表达一个可独立验证、审查和回滚的意图；只 stage owned paths。
4. 不自动 push、发布包、部署、创建云资源或连接客户系统；这些动作分别需要明确授权。
5. Python、Node 和前端依赖都按代码审查：精确版本、许可证、发布内容、生命周期脚本、完整性和 lockfile diff 必须核对。
6. 自动测试、构建、运行 readback、真实模型、浏览器人工验收和用户价值是不同证据层级。
7. 每条开发路径结束时检查本文件是否仍与人的方向一致；如需修改，先展示精确 diff，不能自动同步。

## 七、批准记录与持续执行边界

2026-09-02，人已确认以下产品与架构方向：

1. ContextOx 第一阶段是本地个人部署的 FDE 业务定义 Workbench；
2. 后端使用 Python 和 UV，前端使用 React + TypeScript；
3. Pydantic/OpenAPI/JSON Schema 作为前后端字段对齐机制；
4. ContextOx 自己实现薄 Agent Loop，默认不提供通用 `read/write/edit/bash`；
5. 第一阶段使用领域工具、批量上传、运行可视化、专业澄清表单和批准经验内化；
6. 飞书是本地闭环证明后的第二阶段候选；
7. Codex/Claude Code 是产品基线对手，不把 Skill 当作产品定位。

本文件的精确全文只有在人再次明确批准后才能写入。批准文档不自动授权修改代码、删除旧 TypeScript 文件、安装依赖、更新 lockfile、创建分支、提交、push、运行真实模型、发布或部署；这些动作必须按 checkpoint 另行展示范围、验证和外部影响。
