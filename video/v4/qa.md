# 数契产品片 V4 A｜45 秒候选 QA

状态：`AUTO PASS / INDEPENDENT PASS / HUMAN PENDING`。A 版方向已经扩展为完整 45 秒候选；本轮没有替换 README、站点、下载包或 GitHub Pages 中的公开稳定版。

## 精确候选

- 成片：`output/video/contextox-demo-1.0.0-product-film-v4-a-candidate.mp4`
- SHA-256：`5f9f7dd6496117b6f99341a6d99d416e799fe7d99d747695fc82594d86a683fc`
- 字节数：`5187586`
- 审片板：`output/video/contextox-demo-1.0.0-product-film-v4-a-review-board.png`
- 审片板 SHA-256：`984c44497fa87f799647aa14aa94b58c15569754622b3338bbe6e8e8aa41bce4`
- 审片板字节数：`1794773`
- 方向来源：用户选中的 `V4 Hook A`；选择只锁定爵士放克方向，不自动构成人工成片验收。

## 这次如何回应上一版问题

- 不再把动态介绍逐页播放一遍：主体连续演出发送目标、Agent 提问、人确认规则、候选定义更新和查看本轮变化。
- 画线只保留在用户已选中的开场证明里；框线、支路和结果连接全部由同一套表格单元格坐标生成。产品段不使用漂浮连线或装饰手绘。
- 每个效果都绑定一个明确动作：资料进入、真实按钮按下、当前问题展开、回答选中、候选成果更新或变化面板打开。
- 音乐使用 A 版 120 BPM 爵士放克段落；发送、三次回答、确认、候选展开和查看变化使用各自克制的界面音效。
- 37–42 秒明确说明和 Codex 等通用执行型 Agent 的默认分工重点，并注明不是绝对能力边界。

## 首次独立审片与修复

- 首次审片针对旧候选 `e3ce121be3faea4e2708029049df49eb22188735ec3ce9f3f763e3e55a620f74`，结论为 `FAIL`，发现两个 P1：发送按钮被底部字幕覆盖；回答与成果段脱离当前 Workbench，并出现不属于当前产品的简化单选确认界面。
- 当前候选已经修复：底部字幕只占左侧与中心，真实发送按钮和光标完整可见；15.5–37 秒始终保留完整 Workbench 坐标系，依次展示当前问题卡、快捷选择、回答、回答来源与依据、仅保存、尚未批准、确认并继续、关系与字段及“查看本轮变化”。
- 复核过程中又关闭了两个精度问题：三次回答光标现在直接落在各自的大白话快捷答案上；候选成果区现在同时可见字段、`orders → customers` 关系候选和三份资料来源，不再只在摘要里声称“1 个关系”。
- 当前精确候选已经由新的只读审阅者重新检查；旧候选的审片结论没有被沿用，最终结果见下方“独立只读终审”。

## 自动与实现检查

- 公开合成数字：`PASS`。从 `orders.csv` 与 `customers.csv` 重新关联出华东四笔订单；全部状态 `689.00`、只算 `paid` 为 `440.00`、再扣 `refunded` 为 `390.00`。
- TypeScript：`PASS`，`npm --prefix video run typecheck`。
- 完整渲染：`PASS`，Composition `ContextOxProductFilmV4A` 完成 `1350/1350` 帧渲染与编码。
- 媒体规格：`PASS`。容器时长 `45.10s`；视频为 H.264、`1920×1080`、`30fps`、准确 `1350` 帧；音频为 AAC、48 kHz、双声道。
- 最终响度回读：`PASS`。最终 AAC 音轨为 `-15.98 LUFS`，True Peak `-1.13 dBTP`，符合约 `-16 LUFS` 且不高于 `-1 dBTP` 的目标。
- 节奏网格：`PASS（实现证据）`。音乐检测为 `120 BPM`，每拍 `15` 帧；全部 11 个段落起止点都在 15 帧整数网格上。前 2 秒四个半秒窗有 `5.78 dB` 的 RMS 强弱差。
- 原创界面音效：`PASS`。`ui-click.wav`、`confirm-tick.wav`、`result-open.wav` 可由标准库脚本确定性重建，生成后 SHA-256 与 `assets.yaml` 一致。
- 最终 MP4 抽帧：`PASS`。审片脚本从精确候选直接抽取 24 个关键时刻，额外覆盖第 770 帧“仅保存”、第 795 帧“确认并继续”和第 1005 帧“查看本轮变化”的精确点击；审片板未见裁切、跨镜残留或旧两文件截图。
- 产品连续性：`PASS（实现审计）`。产品段始终使用 `orders.csv`、`customers.csv`、`notes.md`；发送、仅保存、确认并继续和“查看本轮变化”都落在当前 Workbench 对应控件上，后续状态只重绘当前产品已有的澄清、确认、候选成果与变化面板。
- 产品边界：`PASS`。689／440／390 持续注明不是产品运行结果；最终停在等待人工核对的候选定义，没有正式批准、生产写入或客户价值声明。
- 特效因果：`PASS（实现审计）`。完整片源码没有随机数、震屏、闪白、粒子、装饰泛光或旧候选截图；每个镜头只有一个主要动作。
- 展示包回归：`PASS`，`scripts/check_showcase.py`。现有 12 页动态介绍、链接、清单和公开 Demo 数字没有被候选开发破坏。
- 稳定版保护：`PASS`。公开稳定视频仍为 SHA-256 `976622b0b20c9ed8dfd0797bd9e4d24086ebdad38e42994a83357bd5266b0eb8`；候选使用独立文件名。
- 配音边界：`PASS`。没有收到用户真人录音，所以没有使用 TTS、声音克隆或合成旁白；本候选以音乐、界面音效和屏幕文案完成叙事。

## 独立只读终审

- 状态：`PASS（视觉、产品保真与边界）`。全新只读审阅者核对了精确 MP4 `5f9f7dd6496117b6f99341a6d99d416e799fe7d99d747695fc82594d86a683fc` 与审片板 `984c44497fa87f799647aa14aa94b58c15569754622b3338bbe6e8e8aa41bce4`；未发现 P0、P1 或 P2 阻断项。
- 旧 P1 关闭：第 390 帧发送按钮完整可见；第 465–1109 帧保持 Workbench 坐标空间；第 660／705／750 帧点击对应快捷答案；第 770 帧保存、第 795 帧确认；第 900／989 帧显示字段、关系、资料来源和仍未解决事项；第 1005 帧点击“查看本轮变化”。
- 镜头边界：`PASS`。第 359／360、464／465、629／630、809／810、989／990、1109／1110、1259／1260 帧均有有效画面，没有空白过渡。
- 音效实现：`PASS`。源码中的 cue 帧、时长和增益与分镜逐项一致。
- 音频听感：`NOT COVERED`。音乐吸引力、界面音效质感和小播放器真实观看体验仍需人类观看与试听，不能由静态审片或媒体指标代替。

## 人工验收

- 音乐是否真正抓人且不抢内容：`PENDING`
- 界面音效是否与动作契合：`PENDING`
- 开场画线是否精准、必要且不喧宾夺主：`PENDING`
- 产品操作是否有明显动态感：`PENDING`
- Codex 对比是否准确、易懂：`PENDING`
- 45 秒成片是否可替换公开稳定版：`PENDING`
- 公开发布：`NOT RUN`
